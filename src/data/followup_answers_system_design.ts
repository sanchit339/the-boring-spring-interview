/**
 * Answer bank for followup questions — System Design / Scenarios (Q143–Q152).
 * Keyed by the exact followup `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - Lead with the trade-off; a design answer without one isn't an answer
 */
export const followupAnswersSystemDesign: Record<string, string> = {

  // ===================== Q143: URL shortener =====================
  "How do you generate unique short codes at scale (hash vs base62 counter)?":
    "**Base62-encode an auto-increment id.** It's collision-free by construction — id 1 is `b`, id 125 is `cb`, and no two ids ever produce the same string — so a write is one INSERT with no lookup. Hashing the long URL means you have to check whether the code already exists and retry on collision, adding a read to every write and a loop that gets worse as the table fills. The downside of sequential ids is that codes are **guessable and enumerable**, letting someone walk your entire dataset; if that matters, encode `id XOR secret` or draw from a shuffled id space. At multi-instance scale, don't contend on one sequence — hand each instance a **block of ids** (a Redis `INCRBY 1000`) and let it allocate locally, which also survives a brief registry outage.",

  "How would you handle custom aliases and expiration?":
    "A custom alias is just a user-supplied `code`, so let the **unique constraint arbitrate**: attempt the insert and return **409 Conflict** on violation. Checking availability first and then inserting is a race two concurrent requests both win. Reserve a blocklist for anything that collides with your own routes (`api`, `admin`, `health`) and for offensive strings. Expiration is a nullable `expires_at` column checked on read, plus a background job deleting expired rows — you must check on **read as well**, because a cached entry can easily outlive its own expiry. Return **410 Gone** rather than 404 for a link that existed and expired; it's more useful to the caller and to your own analytics.",

  "What is the read/write ratio, and how does that affect caching?":
    "Heavily read-skewed — a shortener typically sees something like **100:1 reads to writes**, since a link is created once and clicked many times. That shapes the whole design: the redirect path should be a **cache lookup** in Redis with the database only as a fallback, and it's worth denormalising and pre-computing anything that makes that path cheaper. Because the mapping from code to URL is **immutable once created**, caching is unusually easy — there's no invalidation problem for the common case, so you can use long TTLs safely. The distribution is also very skewed (a few links get most of the traffic), so even a small cache gets a high hit rate. Writes are rare enough that the write path can afford to be slower — do validation and safe-browsing checks there rather than on read.",

  // ===================== Q144: Rate limiter =====================
  "Compare token bucket, leaky bucket, and fixed window algorithms.":
    "**Fixed window** counts requests per calendar minute — trivial and cheap, but it allows a **double burst at the boundary**: 100 requests at 11:59:59 and 100 more at 12:00:00 is 200 in one second, twice your intended limit. **Sliding window** fixes that by weighting the previous window's count, costing a little more state. **Token bucket** refills tokens at a steady rate up to a maximum, so it caps the long-run average while permitting a controlled burst — the best fit for real traffic, and what Spring Cloud Gateway's `RequestRateLimiter` implements. **Leaky bucket** drains at a fixed rate and smooths the output completely, so it's what you want in front of a fragile downstream that can't absorb bursts at all. Default to token bucket unless something downstream specifically needs even pacing.",

  "How would you implement rate limiting with Redis in Spring?":
    "Keep the counter in **Redis** so all instances share it, and make the check **atomic**. The simple version is `INCR` on a key like `rl:{apiKey}:{minute}` with an `EXPIRE` on first creation — `INCR` is atomic, where a read-then-write is a race that two concurrent requests both pass. For token bucket you need to read and update several fields together, so use a **Lua script** (`RedisTemplate.execute(RedisScript)`), which Redis runs atomically. Easier still is **Bucket4j** with its Redis backend, which implements token bucket properly and saves you writing the script. Enforce it in a `HandlerInterceptor` or a servlet `Filter`, and return **429** with `Retry-After` plus `X-RateLimit-Remaining`. Decide up front what happens if Redis is down — **fail open** (serve traffic unlimited) is usually right for a public API, fail closed for anything protecting a fragile resource.",

  "Should rate limiting live in the gateway, service, or both?":
    "**Both, doing different jobs.** The **gateway** applies the coarse per-client limit and keeps abusive traffic off your fleet entirely — it's the cheapest place to reject, since nothing behind it spends a thread or a database connection. Individual **services** protect specific expensive endpoints: a report that runs a 30-second query needs its own much lower limit than a cache-backed lookup, and the gateway has no idea which is which. Rate limiting only at the service means the junk traffic already cost you a network hop and a request thread. Only at the gateway means one costly endpoint can still be hammered from within an allowed limit. Keep the counters in shared Redis at both levels, and expose the limit in response headers so well-behaved clients can self-regulate instead of discovering the limit by hitting it.",

  // ===================== Q145: Large file upload =====================
  "Would you use async processing, streaming, or object storage direct upload?":
    "**Direct-to-object-storage** if you can — the client requests a short-lived **pre-signed S3 URL**, uploads straight there, and then calls your API with the key. Your JVM never touches the bytes, so file size stops being your capacity problem, and the client gets multipart upload and resume for free. If it must transit your service, **stream** it (`MultipartFile.getInputStream()`, never `getBytes()`) so you're not holding the whole file in heap. Either way the **processing is asynchronous**: return **202 Accepted** with a job id and let a worker do the parsing, because holding an HTTP connection open for a multi-minute job means any deploy or timeout loses the work. These aren't alternatives — the real design is pre-signed upload *plus* async processing, with streaming as the fallback for the transit case.",

  "How do you report progress and failures to the client?":
    "Return **202 Accepted** with a **job id** and a `Location` header pointing at a status resource. The client polls `GET /api/imports/{id}`, which returns a state — `PENDING`, `RUNNING`, `COMPLETED`, `FAILED` — plus progress if you can compute it (rows processed out of total) and, on failure, a **per-row error report** rather than one opaque message. Polling every few seconds is simple, cache-friendly, and survives reconnects; use **Server-Sent Events** or WebSockets only when the UX genuinely needs live progress, since they add connection state you now have to manage across instances. Keep the job record after completion so a client that lost the connection can still retrieve the outcome, and make the worker **idempotent on job id** so a redelivered queue message doesn't reprocess the file.",

  "What timeouts and size limits would you configure?":
    "Start with `spring.servlet.multipart.max-file-size`, because the default is **1MB** and it will reject your first real upload. Set `max-request-size` slightly higher to cover multipart overhead, and `file-size-threshold` so anything larger spills to disk rather than buffering in heap. Raise `server.tomcat.connection-timeout` for the upload endpoint only, not globally, and set `server.tomcat.max-swallow-size: -1` so a rejected upload's body isn't truncated mid-stream, which otherwise shows up to the client as a connection reset instead of your 413. Enforce the size limit at the **edge too** — the gateway or ingress — so an oversized body is rejected before it reaches your pod at all. And validate the real content type by inspecting the bytes rather than trusting the declared `Content-Type` header, which any client can lie about.",

  // ===================== Q146: Notification service =====================
  "How do you ensure delivery retries without spamming users?":
    "Idempotency first, backoff second. Record every send keyed by **event id plus channel** with a unique constraint, and claim that row before sending — at-least-once delivery means the same event *will* arrive twice, and without the claim each redelivery is another email. Then separate **retryable** failures (a 503 from the provider, a timeout: retry with exponential backoff and jitter) from **permanent** ones (invalid address, hard bounce, unsubscribed: don't retry, mark failed, suppress the address). Cap attempts and route the remainder to a **dead-letter queue** with an alert rather than retrying forever. On top of that add a **per-user rate cap**, because a bulk job touching 10,000 orders shouldn't put 200 emails in one inbox — that's a separate concern from provider retries and needs its own limit.",

  "What role do message queues play in this design?":
    "The queue is what **decouples the sender from the send**. The order service publishes `OrderPlaced` and commits; it never waits on SMTP, so a broken email provider can't fail checkout. That's the single most important property here. The queue also **buffers**: if the provider is down or rate-limiting you, messages accumulate and drain when it recovers instead of being lost. It gives you **retries and dead-lettering** as infrastructure rather than code, **fan-out** so adding an analytics consumer needs no change to the producer, and **load levelling** so a spike of 50,000 orders doesn't have to become 50,000 simultaneous API calls. The cost you accept is eventual consistency — the email arrives shortly after the order, not atomically with it — plus duplicate delivery, which is why the consumer has to be idempotent.",

  "How do you template and personalize notifications at scale?":
    "Keep templates **out of the code** — in the database or object storage — so copy changes don't need a deploy, and render with an engine like Thymeleaf or Handlebars. Key them by **type plus locale plus channel**, so `order-confirmation/fr-FR/email` resolves independently of the sending logic, with a fallback locale when a translation is missing. Pass the template a **small explicit context object**, never your domain entity, or you'll eventually leak an internal field or a price you didn't intend into a customer email. **Version** the templates so a send record can say which version produced a given message, which matters when someone asks what exactly a customer received. Keep a preview or test-send endpoint, because a broken template found in production is one that already reached customers. At real volume, pre-render where you can and let the provider handle per-recipient substitution for bulk sends.",

  // ===================== Q147: Caching =====================
  "What is the difference between `@Cacheable`, `@CachePut`, and `@CacheEvict`?":
    "**`@Cacheable`** checks the cache first and only runs the method on a miss, storing the result — that's the read path. **`@CachePut`** *always* runs the method and writes the result into the cache, so it's for updates where you want the new value cached immediately rather than evicted and re-fetched. **`@CacheEvict`** removes an entry (or everything, with `allEntries = true`) and is what you put on deletes. The mistake is using `@Cacheable` on an update method, which returns the **stale cached value** and never runs your update at all. Rule of thumb: `@Cacheable` on reads, `@CachePut` on updates where the method returns the updated object, `@CacheEvict` on deletes and on updates whose return type doesn't match the cached type. Use `beforeInvocation = true` on evict if the method might throw and you'd rather drop the entry regardless.",

  "How do you choose TTLs and cache keys?":
    "Pick the TTL from a **staleness budget**: how out of date may this data be before someone is harmed? Product descriptions tolerate an hour, stock levels maybe seconds, an authorization decision arguably nothing. That question gives a better answer than a round number, and it's what an interviewer is listening for. Always set *some* TTL, so a bad entry heals itself rather than persisting until the next deploy. For keys, include **everything that changes the result** — a per-user response keyed only by product id will serve one user's data to another, which is a data-leak bug rather than a performance one. Prefer an explicit `key = \"#id + ':' + #locale\"` over the default key generator, which silently changes meaning when someone adds a parameter to the method. And add **jitter** to TTLs on hot keys so they don't all expire at the same instant.",

  "What is cache stampede, and how do you mitigate it?":
    "A **stampede** (or dogpile) is what happens when a hot key expires and every concurrent request misses at once — a hundred threads all run the same expensive query simultaneously, so peak database load arrives at exactly the moment the cache stopped protecting you. It's worst for the keys you most needed cached. Three mitigations. **Jitter the TTLs** so keys don't expire in lockstep — a random spread of a few percent removes most of the synchronised behaviour. **Single-flight**: let one caller recompute while the others wait briefly on a lock or serve the stale value, which Caffeine's `refreshAfterWrite` does by refreshing asynchronously while continuing to return the old entry. **Pre-warm** genuinely critical keys on startup or on a schedule so they never expire under live traffic. The related failure is a **cache penetration** attack — repeated requests for keys that don't exist, bypassing the cache entirely — which you handle by caching the negative result briefly.",

  // ===================== Q148: Scaling =====================
  "What is the difference between vertical and horizontal scaling?":
    "**Vertical** is a bigger machine — more CPU and RAM for one instance. It's the fastest fix, needs no code change, and is sometimes exactly right, but it **caps out** at the largest instance available, needs a restart to change, and leaves you a single point of failure. **Horizontal** is more instances behind a load balancer: effectively unlimited headroom, no restart to scale, and redundancy included. Horizontal is the default for microservices and what Kubernetes is designed around, but it has a hard prerequisite — the service must be **stateless**, so any instance can serve any request. Vertical still wins in specific cases: a database primary, or anything where the workload genuinely can't be partitioned. In practice most systems do both — scale up until it's uneconomic, scale out after that.",

  "How do stateless services + load balancers enable scale-out?":
    "If a service holds no request-specific state between calls, **any instance can serve any request**, so a load balancer can distribute freely and you add capacity by adding pods. That's the whole mechanism. State doesn't disappear — it moves to somewhere shared: sessions to **Redis** or a JWT the client carries, uploaded files to **object storage**, rate-limit counters to Redis, caches to a shared cache. Once that's true, instances become interchangeable and disposable, which is also what makes rolling deploys, autoscaling, and spot instances safe. The usual offenders that break it are in-memory HTTP sessions (fix with Spring Session), local file writes, in-memory caches that must agree, and `@Scheduled` jobs that assume they're the only instance. **Sticky sessions** are the workaround people reach for, but they undermine the property — traffic stops balancing evenly and losing one instance loses its users' state.",

  "What bottlenecks appear first (DB, pool, GC, external APIs)?":
    "Almost always the **database**, and usually as a missing index or an N+1 query rather than raw capacity — neither improves at all when you add instances. Next is the **connection pool**: requests queue waiting for a connection while CPU sits idle, and that pairing (rising latency, flat CPU) is diagnostic. Watch out that pool size is **per instance**, so 20 pods × 10 connections is 200 against a Postgres whose default `max_connections` is 100 — scaling out can take the database down. Then **external API calls** without timeouts, where one slow downstream parks every request thread. **GC and CPU** come last for a typical CRUD service, despite heap tuning being the most tempting place to start. Check them in that order and you'll usually find it within two steps.",

  // ===================== Q149: Debugging a slow API =====================
  "What metrics and logs would you check first?":
    "**`http.server.requests`** first — which endpoint, which percentile, and did the change coincide with a deploy or a traffic shift? A big gap between p50 and p99 means a *subset* of requests is slow, which points at particular data or a particular tenant rather than the whole service. Then **`hikaricp.connections.pending`**, because pool exhaustion is the most common cause of \"everything got slow at once\", and **`jvm.gc.pause`** to rule memory in or out. The most useful early signal is the **latency-versus-CPU relationship**: latency up with CPU flat means you're *waiting* on something (database, downstream, lock), while latency up with CPU pinned means you're *computing*. Those two point in completely different directions, and establishing which one you're in saves the most time. Then error rate, and logs filtered by the trace id of a known-slow request.",

  "How do distributed traces help isolate the slow hop?":
    "A trace turns \"the request took 3 seconds\" into a **waterfall showing where those 3 seconds went** — 40ms in the gateway, 60ms in your service, 2.8s waiting on the inventory call. That single view replaces an afternoon of correlating logs across five services by timestamp. Because each hop is a **span with a parent**, you see not only which service was slow but which *call* within it, including individual database statements when JDBC is instrumented. It also exposes structural problems a single-service view hides: the same downstream called eleven times in one request, or two calls that run sequentially but could run in parallel. In Boot 3 that's **Micrometer Tracing** exporting to Zipkin, Tempo, or Jaeger, with the trace id in your log lines via MDC — so you find the slow hop in the trace, then read the logs for that exact trace id to learn why.",

  "How would you safely profile or thread-dump a live JVM?":
    "**Thread dumps** are cheap and safe: `jcmd <pid> Thread.print`, or `/actuator/threaddump` if it's exposed to your internal network. Take **two or three a few seconds apart** — threads that appear stuck in all of them genuinely are, where one dump can't distinguish stuck from busy. Read for patterns, not individual threads: many threads in `SocketRead` on one host means a missing timeout; many in `HikariPool.getConnection` means pool exhaustion. For CPU and allocation profiling, **Java Flight Recorder** is designed for production and typically costs under 2% — `jcmd <pid> JFR.start duration=60s filename=/tmp/rec.jfr`, then analyse the file offline in JDK Mission Control. What you avoid on a live instance is a **heap dump** (it pauses the JVM for seconds and writes gigabytes), fleet-wide DEBUG logging, and any high-overhead sampling profiler. If you need a heap dump, take it from **one pod removed from the load balancer** — which is exactly what readiness probes make easy.",

  // ===================== Q150: Data consistency =====================
  "What consistency models exist (strong, eventual)?":
    "**Strong consistency** means every read returns the latest write — what a single database gives you inside a transaction. It's required where a stale read is genuinely unsafe: an account balance during a withdrawal, seat or stock reservation, an authorization check. **Eventual consistency** means replicas converge given time, with a window in which different services legitimately disagree. That's acceptable far more often than people assume — a confirmation email arriving 200ms later, a search index updating within a second, an analytics dashboard a minute behind. There are useful points in between: **read-your-own-writes** (you see your own changes immediately, others may lag) is often the practical requirement behind what people call \"strong\". The design question isn't which is better but **how long a window the business tolerates** — and then making that window visible in the UI as \"processing\" rather than pretending it isn't there.",

  "How do outbox pattern and idempotent consumers help?":
    "They close the two gaps that make event-driven consistency unreliable. The **outbox** solves the dual-write problem: saving to your database and publishing to a broker are two systems with no shared transaction, so a crash between them either loses the event or publishes one for a change that rolled back. Writing the event as a **row in the same transaction** and relaying it after commit makes the pair atomic — either both happen or neither does. The relay can crash and republish, which gives you **at-least-once** delivery, and that's exactly what **idempotent consumers** handle: dedupe on the event id, or make the write an upsert, so processing the same event twice has the same effect as once. Together they get you **effectively-once** semantics without needing distributed transactions — the outbox guarantees the event is never lost, idempotency guarantees a duplicate is harmless.",

  "When is a saga preferable to a distributed transaction?":
    "Essentially whenever the work spans services. **2PC** holds locks through a network round trip, so throughput collapses under contention; its **coordinator is a single point of failure** that can leave participants in-doubt holding locks until a human intervenes; and it requires **XA support from every participant**, which rules out Kafka, REST APIs, Stripe, and most NoSQL stores. A **saga** keeps each transaction local, short, and independently committable, trading atomicity for **eventual consistency plus compensating transactions you write yourself**. You pay in visible intermediate states (the order sits in `PENDING`) and in the real work of making compensations idempotent and retryable. The genuine exception is a single service talking to two resources that both support XA inside one data centre, where 2PC can be reasonable. And within one service and one database, plain `@Transactional` is still correct — don't reach for a saga where a transaction works.",

  // ===================== Q151: @Async =====================
  "How do you configure the executor for `@Async` methods?":
    "Define a `ThreadPoolTaskExecutor` bean and name it in the annotation — `@Async(\"notificationExecutor\")` — rather than relying on whatever's ambient. Set `corePoolSize`, `maxPoolSize`, and a **bounded** `queueCapacity`, because an unbounded queue hides overload until you run out of heap. Set a `threadNamePrefix` so thread dumps are readable, and a `RejectedExecutionHandler`: `CallerRunsPolicy` runs the task on the calling thread when the queue is full, which slows the caller but never silently drops work. Boot auto-configures an `applicationTaskExecutor` that `@Async` will pick up if you define nothing, and in plain Spring the fallback is `SimpleAsyncTaskExecutor`, which creates a **new thread per call** and doesn't pool at all — so naming your own executor is the safe habit. Use separate executors for separate workloads, or a slow email backlog will starve your audit writes.",

  "What are the limitations of `@Async` (proxy, return types, error handling)?":
    "Three, and all three fail quietly. It's **proxy-based**, so calling an `@Async` method from another method of the same class runs it **synchronously** — the call never leaves the object, there's no error, and it simply looks like it works. The **return type** must be `void`, `Future`, or `CompletableFuture`; anything else returns `null` immediately. **Error handling** is the sharpest edge: an exception thrown from a `void` async method **disappears entirely** unless you register an `AsyncUncaughtExceptionHandler`, and with `CompletableFuture` it only surfaces when someone calls `get()` — if nobody does, it's lost the same way. On top of that, the `SecurityContext`, the request scope, and MDC trace ids **don't propagate** to the new thread by default, so `SecurityContextHolder` is empty and your logs lose their trace id unless you configure a delegating executor.",

  "When should you use a message queue instead of `@Async`?":
    "Whenever losing the task is unacceptable. `@Async` state lives in your JVM's heap, so a restart, a deploy, or an OOM **discards every queued task with no record it ever existed** — fine for warming a cache, unacceptable for a payment confirmation. Use a broker when the work must survive a crash, needs **retries with backoff and a dead-letter queue**, should be **distributed across instances** rather than pinned to the one that received the request, needs to be observable (queue depth, consumer lag, failure counts), or when the consumer is a different service entirely. The queue also gives you **backpressure**: a spike lands in the broker instead of exhausting your thread pool. `@Async` is the right tool for short, cheap, best-effort work inside one process, and the wrong one for anything a customer would notice going missing.",

  // ===================== Q152: @Scheduled =====================
  "What is the difference between fixedRate, fixedDelay, and cron?":
    "**`fixedDelay`** waits N milliseconds *after the previous run finishes*, so runs never overlap and the actual period is job duration plus the delay — the safe default for a poller. **`fixedRate`** starts every N milliseconds *regardless* of how long the last run took; if a run exceeds the period the next is already due, and with a pool size above 1 they'll overlap and double-process. Use it only when you need a steady cadence and the job is reliably short. **`cron`** takes a six-field expression (second, minute, hour, day, month, weekday) for calendar scheduling — `0 0 2 * * *` is 2am daily. Always set the `zone` attribute on cron, or you inherit the server's timezone and your job shifts by an hour twice a year at DST. Both `fixedRate` and `fixedDelay` accept `initialDelay`, which is worth using so every job doesn't fire simultaneously at startup.",

  "How do you prevent overlapping executions of the same job?":
    "`fixedDelay` prevents overlap within a single instance by construction, since the next run is measured from the end of the previous one. `fixedRate` and `cron` don't — with a scheduler pool larger than 1 a long run can be re-entered while still working. The robust answers are to hold a **lock** (ShedLock, or a row-level lock in your own database) or to make the job **idempotent** so a concurrent second run is harmless. A lock needs an expiry — ShedLock's `lockAtMostFor` — so an instance that dies mid-run doesn't hold it forever and block the job permanently. It's also worth guarding on **job state**: mark the run as `RUNNING` in a table and skip if one is already active, which gives you a visible run history as a side effect. Remember that a single-instance guard is not a multi-instance guard; those are two different problems.",

  "How would you run scheduled jobs in a multi-instance deployment (ShedLock)?":
    "By default **every instance runs every job**, so three replicas means three invoice runs and three sets of emails — the classic production surprise. **ShedLock** is the lightweight fix: annotate the method with `@SchedulerLock(name = \"nightlyBilling\", lockAtMostFor = \"30m\")` and it takes a lock row in your existing database or Redis, so only the winning instance executes while the others skip. It's a few lines and no new infrastructure. **Quartz** in clustered mode is the heavier option, and it earns its keep when you need persistent job state, misfire handling, or jobs scheduled dynamically at runtime. For anything genuinely important, the strongest answer is to take the job **out of the application** — a **Kubernetes CronJob** running a separate pod gives you isolation, its own resource limits, retries, and an inspectable run history, none of which `@Scheduled` provides. Set `lockAtMostFor` longer than the job's worst-case runtime, or a slow run gets its lock stolen and executes twice.",
};
