/**
 * Answer bank for followup questions — System Design / Scenarios (Q143–Q152).
 * Keyed by the exact followup `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - Lead with the trade-off; a design answer without one isn't an answer
 */
export const followupAnswersSystemDesign: Record<string, string> = {

  // ===================== Q143: URL shortener =====================
  "Sequential ids make codes guessable. Does that matter?":
    "It depends what's behind the links, but often yes — anyone can walk `b`, `c`, `d` and enumerate your entire dataset, which for a shortener holding private documents or invitation links is a real leak rather than a theoretical one. Encode `id XOR secret`, or draw from a shuffled id space, and you keep the collision-free property without the enumeration.\n\nThat property is why you use a counter at all. **Base62-encoding an auto-increment id** is collision-free by construction — id 1 is `b`, id 125 is `cb`, and no two ids ever produce the same string — so a write is one INSERT with no lookup. Hashing the long URL means checking whether the code already exists and retrying on collision, which adds a read to every write and a loop that degrades as the table fills.\n\nAt multi-instance scale, don't contend on a single sequence. Hand each instance a **block of ids** (a Redis `INCRBY 1000`) and let it allocate locally, which also survives a brief outage of whatever hands the blocks out.",

  "How would you handle custom aliases and expiration?":
    "A custom alias is just a user-supplied `code`, so let the **unique constraint arbitrate**: attempt the insert and return **409 Conflict** on violation. Checking availability first and then inserting is a race two concurrent requests both win. Reserve a blocklist for anything that collides with your own routes (`api`, `admin`, `health`) and for offensive strings. Expiration is a nullable `expires_at` column checked on read, plus a background job deleting expired rows — you must check on **read as well**, because a cached entry can easily outlive its own expiry. Return **410 Gone** rather than 404 for a link that existed and expired; it's more useful to the caller and to your own analytics.",

  "Why is a URL shortener unusually easy to cache?":
    "Because the mapping from code to URL is **immutable once created**. There's no invalidation problem for the common case, so you can use very long TTLs safely — and most caching questions are really invalidation questions, which this one isn't.\n\nThe traffic shape makes it worth doing. A shortener is heavily read-skewed, something like **100:1 reads to writes**, since a link is created once and clicked many times, so the redirect path should be a Redis lookup with the database only as a fallback. The distribution is skewed too — a few links take most of the traffic — so even a small cache gets a high hit rate.\n\nThe corollary is that the **write path can afford to be slow**. Do validation, safe-browsing checks, and anything else expensive there, where it costs you once, rather than on the read path you're trying to keep at a few milliseconds.",

  // ===================== Q144: Rate limiter =====================
  "What's wrong with counting requests per calendar minute?":
    "The **boundary burst**. A fixed window resets at :00, so 100 requests at 11:59:59 and 100 more at 12:00:00 is 200 in one second — twice the limit you thought you set, and exactly the pattern a client retrying on a schedule falls into naturally.\n\n**Sliding window** fixes it by weighting the previous window's count into the current one, at the cost of a little more state. **Token bucket** takes a different route: tokens refill at a steady rate up to a maximum and each request spends one, so the long-run average is capped while a controlled burst is still allowed. That's the best fit for real traffic, and what Spring Cloud Gateway's `RequestRateLimiter` implements.\n\n**Leaky bucket** drains at a fixed rate and smooths the output completely, which is what you want in front of a fragile downstream that can't absorb bursts at all. Default to token bucket unless something downstream specifically needs even pacing.",

  "How would you implement rate limiting with Redis in Spring?":
    "Keep the counter in **Redis** so all instances share it, and make the check **atomic**. The simple version is `INCR` on a key like `rl:{apiKey}:{minute}` with an `EXPIRE` on first creation — `INCR` is atomic, where a read-then-write is a race that two concurrent requests both pass. For token bucket you need to read and update several fields together, so use a **Lua script** (`RedisTemplate.execute(RedisScript)`), which Redis runs atomically. Easier still is **Bucket4j** with its Redis backend, which implements token bucket properly and saves you writing the script. Enforce it in a `HandlerInterceptor` or a servlet `Filter`, and return **429** with `Retry-After` plus `X-RateLimit-Remaining`. Decide up front what happens if Redis is down — **fail open** (serve traffic unlimited) is usually right for a public API, fail closed for anything protecting a fragile resource.",

  "Should rate limiting live in the gateway, service, or both?":
    "**Both, doing different jobs.** The **gateway** applies the coarse per-client limit and keeps abusive traffic off your fleet entirely — it's the cheapest place to reject, since nothing behind it spends a thread or a database connection. Individual **services** protect specific expensive endpoints: a report that runs a 30-second query needs its own much lower limit than a cache-backed lookup, and the gateway has no idea which is which. Rate limiting only at the service means the junk traffic already cost you a network hop and a request thread. Only at the gateway means one costly endpoint can still be hammered from within an allowed limit. Keep the counters in shared Redis at both levels, and expose the limit in response headers so well-behaved clients can self-regulate instead of discovering the limit by hitting it.",

  // ===================== Q145: Large file upload =====================
  "How do you report progress and failures to the client?":
    "Return **202 Accepted** with a **job id** and a `Location` header pointing at a status resource. The client polls `GET /api/imports/{id}`, which returns a state — `PENDING`, `RUNNING`, `COMPLETED`, `FAILED` — plus progress if you can compute it (rows processed out of total) and, on failure, a **per-row error report** rather than one opaque message. Polling every few seconds is simple, cache-friendly, and survives reconnects; use **Server-Sent Events** or WebSockets only when the UX genuinely needs live progress, since they add connection state you now have to manage across instances. Keep the job record after completion so a client that lost the connection can still retrieve the outcome, and make the worker **idempotent on job id** so a redelivered queue message doesn't reprocess the file.",

  "What timeouts and size limits would you configure?":
    "Start with `spring.servlet.multipart.max-file-size`, because the default is **1MB** and it will reject your first real upload. Set `max-request-size` slightly higher to cover multipart overhead, and `file-size-threshold` so anything larger spills to disk rather than buffering in heap. Raise `server.tomcat.connection-timeout` for the upload endpoint only, not globally, and set `server.tomcat.max-swallow-size: -1` so a rejected upload's body isn't truncated mid-stream, which otherwise shows up to the client as a connection reset instead of your 413. Enforce the size limit at the **edge too** — the gateway or ingress — so an oversized body is rejected before it reaches your pod at all. And validate the real content type by inspecting the bytes rather than trusting the declared `Content-Type` header, which any client can lie about.",

  // ===================== Q146: Notification service =====================
  "Which delivery failures should you not retry?":
    "**Permanent ones** — an invalid address, a hard bounce, an unsubscribe. Retrying those never succeeds, and with an email provider it actively damages your sender reputation. Mark them failed, suppress the address, and stop.\n\nRetry the **transient** failures: a 503 from the provider, a timeout, a connection reset. Exponential backoff with jitter, a capped attempt count, then a **dead-letter queue with an alert** rather than retrying forever.\n\nSeparately from provider retries you need **idempotency**, because at-least-once delivery means the same event *will* arrive twice — record every send keyed by **event id plus channel** with a unique constraint, and claim that row before sending. Then add a **per-user rate cap** on top: a bulk job touching 10,000 orders shouldn't put 200 emails in one inbox, which is a different problem from retrying and needs its own limit.",

  "Beyond not blocking checkout, what does the queue buy you?":
    "**Buffering.** If the provider is down or rate-limiting you, messages accumulate and drain when it recovers, where a synchronous call would simply have failed. That's the property people forget when they argue a direct API call would be simpler.\n\nYou also get **retries and dead-lettering as infrastructure** rather than code you maintain, **fan-out** so adding an analytics consumer needs no change to the producer, and **load levelling** so a spike of 50,000 orders doesn't become 50,000 simultaneous provider calls.\n\nWhat you pay is eventual consistency — the email arrives shortly after the order rather than atomically with it — plus duplicate delivery, which is why the consumer has to be idempotent. Both are usually fine for notifications, which is exactly why this is the standard shape for them.",

  "How do you template and personalize notifications at scale?":
    "Keep templates **out of the code** — in the database or object storage — so copy changes don't need a deploy, and render with an engine like Thymeleaf or Handlebars. Key them by **type plus locale plus channel**, so `order-confirmation/fr-FR/email` resolves independently of the sending logic, with a fallback locale when a translation is missing. Pass the template a **small explicit context object**, never your domain entity, or you'll eventually leak an internal field or a price you didn't intend into a customer email. **Version** the templates so a send record can say which version produced a given message, which matters when someone asks what exactly a customer received. Keep a preview or test-send endpoint, because a broken template found in production is one that already reached customers. At real volume, pre-render where you can and let the provider handle per-recipient substitution for bulk sends.",

  // ===================== Q147: Caching =====================
  "What happens if you put `@Cacheable` on an update method?":
    "It returns the **stale cached value and never runs your update at all**. `@Cacheable` checks the cache first and only invokes the method on a miss, so on a hit the body is skipped entirely and the write you thought you were making silently doesn't happen.\n\nUse **`@CachePut`** there instead: it *always* runs the method and writes the result into the cache, so the new value is cached immediately rather than evicted and re-fetched. **`@CacheEvict`** removes an entry (or everything, with `allEntries = true`) and is what goes on deletes, and on updates whose return type doesn't match what's cached.\n\nRule of thumb: `@Cacheable` on reads, `@CachePut` on updates that return the updated object, `@CacheEvict` on deletes. Add `beforeInvocation = true` to an evict if the method might throw and you'd rather drop the entry regardless.",

  "How do you choose TTLs and cache keys?":
    "Pick the TTL from a **staleness budget**: how out of date may this data be before someone is harmed? Product descriptions tolerate an hour, stock levels maybe seconds, an authorization decision arguably nothing. That question gives a better answer than a round number, and it's what an interviewer is listening for. Always set *some* TTL, so a bad entry heals itself rather than persisting until the next deploy. For keys, include **everything that changes the result** — a per-user response keyed only by product id will serve one user's data to another, which is a data-leak bug rather than a performance one. Prefer an explicit `key = \"#id + ':' + #locale\"` over the default key generator, which silently changes meaning when someone adds a parameter to the method. And add **jitter** to TTLs on hot keys so they don't all expire at the same instant.",

  "What is cache stampede, and how do you mitigate it?":
    "A **stampede** (or dogpile) is what happens when a hot key expires and every concurrent request misses at once — a hundred threads all run the same expensive query simultaneously, so peak database load arrives at exactly the moment the cache stopped protecting you. It's worst for the keys you most needed cached. Three mitigations. **Jitter the TTLs** so keys don't expire in lockstep — a random spread of a few percent removes most of the synchronised behaviour. **Single-flight**: let one caller recompute while the others wait briefly on a lock or serve the stale value, which Caffeine's `refreshAfterWrite` does by refreshing asynchronously while continuing to return the old entry. **Pre-warm** genuinely critical keys on startup or on a schedule so they never expire under live traffic. The related failure is a **cache penetration** attack — repeated requests for keys that don't exist, bypassing the cache entirely — which you handle by caching the negative result briefly.",

  // ===================== Q148: Scaling =====================
  "What is the difference between vertical and horizontal scaling?":
    "**Vertical** is a bigger machine — more CPU and RAM for one instance. It's the fastest fix, needs no code change, and is sometimes exactly right, but it **caps out** at the largest instance available, needs a restart to change, and leaves you a single point of failure. **Horizontal** is more instances behind a load balancer: effectively unlimited headroom, no restart to scale, and redundancy included. Horizontal is the default for microservices and what Kubernetes is designed around, but it has a hard prerequisite — the service must be **stateless**, so any instance can serve any request. Vertical still wins in specific cases: a database primary, or anything where the workload genuinely can't be partitioned. In practice most systems do both — scale up until it's uneconomic, scale out after that.",

  "If the service is stateless, where does the state actually go?":
    "Somewhere shared. Sessions move to **Redis** (Spring Session) or into a JWT the client carries, uploaded files to **object storage**, rate-limit counters to Redis, caches to a shared cache. The state doesn't disappear — it stops living in one instance's heap.\n\nOnce that's true, **any instance can serve any request**, so a load balancer distributes freely and you add capacity by adding pods. That's the whole mechanism, and it's also what makes rolling deploys, autoscaling and spot instances safe, because instances become interchangeable and disposable.\n\nThe usual things that break it are in-memory HTTP sessions, local file writes, in-memory caches that must agree across instances, and `@Scheduled` jobs assuming they're the only one running. **Sticky sessions** are the workaround people reach for, but they undermine the property — traffic stops balancing evenly, and losing one instance loses its users' state.",

  "Scaling out took the database down. How?":
    "Connection maths. Pool size is **per instance**, so 20 pods × 10 connections is 200 against a Postgres whose default `max_connections` is 100 — the new pods start, exhaust the server's limit, and now nothing can connect, including whatever was working fine before you scaled.\n\nThat's the sharp version of a general truth: the **database is almost always the first bottleneck**, and usually as a missing index or an N+1 query rather than raw capacity — neither of which improves at all when you add instances.\n\nAfter the database it's the **connection pool** (requests queue waiting while CPU sits idle — rising latency with flat CPU is the diagnostic pairing), then **external calls without timeouts**, where one slow downstream parks every request thread. **GC and CPU** come last for a typical CRUD service, despite heap tuning being the most tempting place to start.",

  // ===================== Q149: Debugging a slow API =====================
  "Latency is up and CPU is flat. What does that tell you?":
    "That you're **waiting**, not computing — on the database, a downstream call, or a lock. Latency up with CPU pinned would mean the opposite. Establishing which of the two you're in is the most useful early move there is, because they point in completely different directions and it costs one glance at a dashboard.\n\nBefore that, **`http.server.requests`**: which endpoint, which percentile, and did the change coincide with a deploy or a traffic shift? A big gap between p50 and p99 means a *subset* of requests is slow, which points at particular data or a particular tenant rather than the whole service.\n\nThen **`hikaricp.connections.pending`**, since pool exhaustion is the most common cause of \"everything got slow at once\", and **`jvm.gc.pause`** to rule memory in or out. After that, error rate, and logs filtered by the trace id of a known-slow request.",

  "What does a trace show that per-service metrics can't?":
    "**Where one request's time actually went**, hop by hop — 40ms in the gateway, 60ms in your service, 2.8s waiting on inventory. Per-service metrics tell you each service's average health; they can't attribute a single slow request across five of them, which is why the alternative is an afternoon correlating logs by timestamp.\n\nBecause each hop is a **span with a parent**, you also see which *call* inside a service was slow, including individual database statements when JDBC is instrumented. It exposes structural problems a single-service view hides, too: the same downstream called eleven times in one request, or two calls running sequentially that could run in parallel.\n\nIn Boot 3 that's **Micrometer Tracing** exporting to Zipkin, Tempo or Jaeger, with the trace id in your log lines via MDC — so you find the slow hop in the trace, then read the logs for that exact trace id to find out why.",

  "How would you safely profile or thread-dump a live JVM?":
    "**Thread dumps** are cheap and safe: `jcmd <pid> Thread.print`, or `/actuator/threaddump` if it's exposed to your internal network. Take **two or three a few seconds apart** — threads that appear stuck in all of them genuinely are, where one dump can't distinguish stuck from busy. Read for patterns, not individual threads: many threads in `SocketRead` on one host means a missing timeout; many in `HikariPool.getConnection` means pool exhaustion. For CPU and allocation profiling, **Java Flight Recorder** is designed for production and typically costs under 2% — `jcmd <pid> JFR.start duration=60s filename=/tmp/rec.jfr`, then analyse the file offline in JDK Mission Control. What you avoid on a live instance is a **heap dump** (it pauses the JVM for seconds and writes gigabytes), fleet-wide DEBUG logging, and any high-overhead sampling profiler. If you need a heap dump, take it from **one pod removed from the load balancer** — which is exactly what readiness probes make easy.",

  // ===================== Q150: Data consistency =====================
  "What consistency models exist (strong, eventual)?":
    "**Strong consistency** means every read returns the latest write — what a single database gives you inside a transaction. It's required where a stale read is genuinely unsafe: an account balance during a withdrawal, seat or stock reservation, an authorization check. **Eventual consistency** means replicas converge given time, with a window in which different services legitimately disagree. That's acceptable far more often than people assume — a confirmation email arriving 200ms later, a search index updating within a second, an analytics dashboard a minute behind. There are useful points in between: **read-your-own-writes** (you see your own changes immediately, others may lag) is often the practical requirement behind what people call \"strong\". The design question isn't which is better but **how long a window the business tolerates** — and then making that window visible in the UI as \"processing\" rather than pretending it isn't there.",

  "Is there ever a case for 2PC?":
    "Narrowly, yes — a **single service talking to two resources that both support XA inside one data centre**, a database and a JMS broker say, where the coordinator is local and the latency is small. That's the honest exception, and it's worth naming rather than claiming 2PC is always wrong.\n\nEverywhere else the costs dominate. 2PC holds locks through a network round trip, so throughput collapses under contention. Its **coordinator is a single point of failure** that can leave participants in-doubt holding locks until a human intervenes. And it needs **XA support from every participant**, which rules out Kafka, REST APIs, Stripe and most NoSQL stores.\n\nA **saga** keeps each transaction local, short and independently committable, trading atomicity for eventual consistency plus compensations you write yourself. The other direction matters too: inside one service and one database, plain `@Transactional` is still correct — don't reach for a saga where a transaction works.",

  // ===================== Q151: @Async =====================
  "How do you configure the executor for `@Async` methods?":
    "Define a `ThreadPoolTaskExecutor` bean and name it in the annotation — `@Async(\"notificationExecutor\")` — rather than relying on whatever's ambient. Set `corePoolSize`, `maxPoolSize`, and a **bounded** `queueCapacity`, because an unbounded queue hides overload until you run out of heap. Set a `threadNamePrefix` so thread dumps are readable, and a `RejectedExecutionHandler`: `CallerRunsPolicy` runs the task on the calling thread when the queue is full, which slows the caller but never silently drops work. Boot auto-configures an `applicationTaskExecutor` that `@Async` will pick up if you define nothing, and in plain Spring the fallback is `SimpleAsyncTaskExecutor`, which creates a **new thread per call** and doesn't pool at all — so naming your own executor is the safe habit. Use separate executors for separate workloads, or a slow email backlog will starve your audit writes.",

  "Your `@Async` method threw and nothing appeared in the logs. Why?":
    "Because an exception from a **`void`** async method **disappears entirely** unless you register an `AsyncUncaughtExceptionHandler`. Nobody is holding a result to observe it, so it's dropped. With `CompletableFuture` it survives, but only surfaces when someone calls `get()` — and if nobody does, it's lost the same way.\n\nThe other two limits fail just as quietly. It's **proxy-based**, so calling an `@Async` method from another method of the same class runs it **synchronously** — the call never leaves the object, and it simply looks like it worked. And the **return type** must be `void`, `Future`, or `CompletableFuture`; anything else returns `null` immediately.\n\nOne more that bites in practice: the `SecurityContext`, the request scope, and MDC trace ids **don't propagate** to the new thread by default, so `SecurityContextHolder` comes back empty and your async log lines lose their trace id unless you configure a delegating executor.",

  "What does a broker give you that a thread pool can't?":
    "**Durability, first and foremost.** `@Async` state lives in your JVM's heap, so a restart, a deploy, or an OOM discards every queued task with no record it ever existed. Fine for warming a cache; unacceptable for a payment confirmation.\n\nIt also gives you **retries with backoff and a dead-letter queue** as infrastructure rather than code, **distribution across instances** instead of the task being pinned to whichever pod received the request, and **observability** — queue depth, consumer lag and failure counts are all things you can alert on, where a thread pool's backlog is invisible. Plus **backpressure**: a spike lands in the broker instead of exhausting your pool.\n\n`@Async` is the right tool for short, cheap, best-effort work inside one process. It's the wrong one for anything a customer would notice going missing.",

  // ===================== Q152: @Scheduled =====================
  "Your nightly job shifts by an hour twice a year. Why?":
    "No `zone` on the cron expression, so it inherits the **server's timezone** and follows that zone's daylight-saving transitions. Set `@Scheduled(cron = \"0 0 2 * * *\", zone = \"Europe/London\")` explicitly and the 2am run stays at 2am. The same transitions can also make a run happen twice or not at all on the changeover night, which matters for anything billing-related.\n\nCron takes a **six-field** expression — second, minute, hour, day, month, weekday — so `0 0 2 * * *` is 2am daily. The other two modes are relative rather than calendar-based. **`fixedDelay`** waits N milliseconds *after the previous run finishes*, so runs never overlap and the real period is duration plus delay — the safe default for a poller. **`fixedRate`** starts every N milliseconds *regardless* of how long the last run took, so an overrunning job means the next one is already due.\n\nBoth relative modes accept `initialDelay`, which is worth setting so every job doesn't fire simultaneously at startup.",

  "How do you prevent overlapping executions of the same job?":
    "`fixedDelay` prevents overlap within a single instance by construction, since the next run is measured from the end of the previous one. `fixedRate` and `cron` don't — with a scheduler pool larger than 1 a long run can be re-entered while still working. The robust answers are to hold a **lock** (ShedLock, or a row-level lock in your own database) or to make the job **idempotent** so a concurrent second run is harmless. A lock needs an expiry — ShedLock's `lockAtMostFor` — so an instance that dies mid-run doesn't hold it forever and block the job permanently. It's also worth guarding on **job state**: mark the run as `RUNNING` in a table and skip if one is already active, which gives you a visible run history as a side effect. Remember that a single-instance guard is not a multi-instance guard; those are two different problems.",

  "When is ShedLock not enough?":
    "When you need more than \"only one instance runs this\". **Quartz** in clustered mode is the next step up, and it earns its keep when you want persistent job state, misfire handling, or jobs scheduled dynamically at runtime rather than fixed at compile time.\n\nFor anything genuinely important, the strongest answer is taking the job **out of the application entirely** — a **Kubernetes CronJob** runs it as a separate pod with its own resource limits, retries and an inspectable run history, none of which `@Scheduled` provides. It also stops a heavy nightly job competing for threads with live traffic.\n\nShedLock is still the right first move for the common case: `@SchedulerLock(name = \"nightlyBilling\", lockAtMostFor = \"30m\")` takes a lock row in your existing database or Redis, so the winning instance executes and the others skip — a few lines, no new infrastructure. Set `lockAtMostFor` longer than the job's worst-case runtime, or a slow run gets its lock stolen and executes twice.",

};
