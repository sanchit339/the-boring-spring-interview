/**
 * Answer bank for follow-up questions — Behavioral / Project-Based (Q153–Q158).
 * Keyed by the exact follow-up `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * NOTE ON THIS CATEGORY (differs from every other bank):
 * These answers deliberately give STRUCTURE and criteria, not a scripted story.
 * The candidate's own projects and incidents are the content — inventing a
 * history here would be worse than useless, since they'd have to defend it under
 * follow-up questioning. `answer_rules.md` Rule 2 ("no LARPing, no implied war
 * stories") is the reason. Examples that appear are marked as examples.
 */
export const followupAnswersBehavioral: Record<string, string> = {

  // ===================== Q153: Project walkthrough =====================
  "What was your specific contribution vs the team's?":
    "Be precise and be honest — say \"I owned X, and Y was someone else's work\". That reads as confident rather than diminished, and inflating your scope collapses the moment they ask a detailed question about a part you didn't write. Name the components you built, the decisions you personally made, and anything you drove that wasn't code — a migration, an incident, onboarding someone. If you contributed to something jointly, say so and describe your half. The pattern to avoid is answering entirely in \"we\", which is the most common way a strong project answer gets scored as weak: the interviewer literally cannot tell what you can do. Where your part was small, say what you learned from the parts you watched — that's a legitimate answer at 2 YoE and far better than implied ownership.",

  "What architecture decisions did you make, and what would you change?":
    "Give the **trade-off, not the conclusion**. \"We used a database table for the job queue instead of Kafka, because we had one consumer and no ops capacity to run a broker\" demonstrates reasoning; \"we used Kafka\" is just a noun. Pick two or three decisions where there was a genuine alternative — sync versus async, one service versus two, SQL versus NoSQL, build versus buy — and say what you optimised for and what you gave up. For **what you'd change**, choose something real and technical with a reason attached: \"we split the services too early and the deploy coupling hurt more than the isolation helped\", or \"we should have added tracing on day one instead of after the first incident\". Avoid fake humility like \"I'd write more tests\", and avoid anything implying you didn't understand the problem at the time — the goal is showing you've reflected, not that you were careless.",

  "How did you handle auth, data, and deployment in that project?":
    "Have one crisp sentence ready for each, because this trio is the fastest way for an interviewer to check you actually saw a system end to end rather than one class in the middle. **Auth**: who issued the token, where it was validated, how roles were enforced — for example a JWT issued by an identity provider, signature verified at the gateway, method-level checks in the service. **Data**: which database and why, how the schema evolved (Flyway or Liquibase migrations in CI), and anything notable about volume or retention. **Deployment**: how code reached production — a Docker image built in CI, deployed to Kubernetes, with whatever gate existed before production. Be ready for one level deeper on each, and be honest about what someone else handled: \"the platform team owned the cluster; I owned the manifests\" is a perfectly good answer.",

  // ===================== Q154: Production bug =====================
  "What tools and signals led you to the root cause?":
    "Name the **specific signal and what it told you**, because the reasoning is what's being assessed. \"CPU was flat while latency climbed, so we were waiting on something rather than computing\" demonstrates diagnosis in a single sentence. The usual cast is a metrics dashboard (latency percentiles, error rate, connection pool), a distributed trace to find the slow hop, thread dumps to see where threads were parked, GC logs, and the slow-query log. Describe the **narrowing**: what you ruled out and why, not just what you eventually found — an interviewer is listening for whether you formed hypotheses and tested them or changed things until the symptom moved. If the honest answer is that you added logging and found it that way, say so, but explain **how you decided where to add it**, because that's the same skill.",

  "How did you mitigate impact while investigating?":
    "Say plainly what you did to stop the bleeding **before** you understood the cause — a rollback, a feature flag, scaling up, disabling the endpoint, failing over, or shifting traffic. The point being tested is whether you know that restoring service and understanding the problem are two different jobs with different urgency, and that the first one wins during an incident. It's a genuinely senior instinct and many candidates skip straight to the clever diagnosis, which reads as not having been responsible for a live system. Mention how you communicated too — who you told and when — since keeping people informed is part of the job. If mitigation wasn't possible, say why (a data-corrupting bug you had to stop, not just route around) and what you did instead.",

  "What process changes did you introduce to prevent recurrence?":
    "Keep it **proportionate and concrete**: the alert that would have caught it twenty minutes earlier, the regression test for that exact case, the timeout that was missing, the dashboard panel that made the signal visible. One specific, well-chosen change is far more convincing than a policy. Grand proposals — \"we introduced a company-wide incident review process\" off the back of one bug — sound invented, and interviewers hear that a lot. It's also fine and honest to say the fix was purely technical and needed no process change; not every bug is a systemic failure. If your team does run post-incident reviews, mention what came out of yours, and if a follow-up action **didn't** happen because of priorities, saying so is more credible than implying every action item was completed.",

  // ===================== Q155: Code reviews =====================
  "How do you give constructive feedback without blocking the team?":
    "Separate **blocking from optional explicitly** — many teams prefix throwaway comments with `nit:` — because an author who can't tell which comments are mandatory will either argue about all of them or silently accept all of them, and both waste time. **Ask rather than assert** when you might lack context: \"what happens if this list is empty?\" invites an answer, where \"this will NPE\" is simply wrong if there's a guard you missed. Comment on the **code, not the person**. Weigh the cost of delay honestly: for a small issue, approving with a comment beats another full round trip, and a PR sitting for two days usually costs the team more than the comment saves. If something needs a genuine redesign, that's a five-minute call, not fifteen review comments — a long thread is a signal the medium has stopped working.",

  "What correctness, security, and readability checks do you prioritize?":
    "In that order, because a beautifully formatted race condition is still a bug. **Correctness**: does it do what the ticket says, are null and empty and boundary cases handled, is there a test covering the behaviour that changed, and is there shared mutable state or a non-atomic check-then-act. **Security**: secrets or PII in logs, unvalidated input, SQL built by string concatenation, and — the one most often missed — whether **authorization is checked in the service** rather than only in the controller. **Performance** next, mostly N+1 queries, a missing index on a new lookup column, and unbounded queries with no pagination. **Readability** last but genuinely: will someone understand this in six months, and is the complexity essential or accidental. **Formatting** shouldn't be in a human review at all — put Spotless or Checkstyle in the build and stop spending attention on it.",

  "How do you handle disagreements in review?":
    "State the concern once with a reason, then actually listen to the response — the author has usually thought about it and may know a constraint you don't. If it still matters after that, move it **out of the comment thread to a call**, because text makes technical disagreement feel more adversarial than it is and a fifteen-reply thread helps nobody. Escalating to a third person on the team when you're genuinely deadlocked is normal, not a failure. Distinguish what's **worth holding the line on** — correctness, security, data integrity — from preference, and let preference go; not every disagreement is worth spending credibility on. Be willing to be wrong out loud: \"fair enough, you're right about the ordering\" is cheap and buys a lot of goodwill for the times it does matter.",

  // ===================== Q156: Optimization =====================
  "How did you measure before and after?":
    "Same endpoint, same data volume, same percentile — and use **p95 or p99 rather than the mean**, because an average hides exactly the slow tail users complain about. For a query, `EXPLAIN ANALYZE` gives both a number and the *reason*: a sequential scan, a nested loop over a large set, a sort spilling to disk. For an endpoint, `http.server.requests` from Actuator or your APM before and after the deploy is the honest comparison. Watch the **query count** as well as total time — 200 fast queries is an N+1, and no index will fix it. The critical part is measuring against **realistic data volumes**: a plan against 50 rows in a test database tells you nothing about a table with a million. If you can quote the actual before-and-after numbers in an interview, do — it's the most convincing thing in the whole answer.",

  "Was the fix caching, indexing, query rewrite, or architecture?":
    "Say which, and more importantly **why that one**. The sensible order is: fix the **query** first (an N+1 resolved with a join fetch or `@EntityGraph`, a missing index, a projection that stops loading a column you never read), then **pagination**, since an endpoint returning 50,000 rows is slow regardless of indexing. **Caching** comes after the query is sane — it's right for hot, rarely-changing, expensive-to-compute data, and wrong as a substitute for an index, because caching a bad query just hides it until the cache misses. **Architecture** changes (read replicas, denormalisation, a search index) come last because they cost the most and are hardest to undo. An answer that jumps straight to \"we added Redis\" without establishing why the query itself couldn't be fixed invites exactly that follow-up.",

  "What trade-offs did the optimization introduce?":
    "Every optimisation has one, and naming it unprompted is what separates a good answer here. An **index** costs write throughput and disk, and an unused index is pure overhead. A **cache** costs staleness and invalidation complexity, and adds a component that can fail. **Denormalisation** costs consistency and update complexity. A **join fetch** can produce a cartesian product if you fetch two collections at once, and eager loading someone adds \"to be safe\" pulls half the database into every query. **Read replicas** introduce replication lag, so a read-after-write can return stale data. Also worth mentioning: whether the optimisation made the code **harder to understand**, since a hand-tuned native query is faster and also the thing the next developer will be nervous about touching. Interviewers ask this to find out whether you understood what you gave up or just watched a number improve.",

  // ===================== Q157: Keeping updated =====================
  "What resources do you follow (docs, blogs, release notes)?":
    "Be **specific and checkable** — \"the Spring Boot release notes on the GitHub wiki\" is verifiable in a way \"various blogs\" isn't, and interviewers hear the vague version constantly. The highest-value sources are the ones describing **change**: Boot release notes and migration guides, the Spring blog, the JEP list for each Java release, and a library's own changelog when you upgrade it. Add something with editorial depth (Baeldung, InfoQ, a conference talk) and, if you use one, a newsletter for breadth. The stronger half of the answer is **how you use them**: reading the release notes for the version you're actually on, so you learn what's deprecated before it's removed rather than during a painful upgrade. Mentioning that you read the **deprecation** notes specifically is a small detail that lands well.",

  "Have you adopted a recent Java or Spring feature in a project?":
    "Pick one you genuinely used and give the **reason it mattered**, not just the name. Good candidates at 2 YoE: **records** for DTOs, removing fifty lines of boilerplate per class and making them immutable; **`RestClient`** (Boot 3.2+) instead of `RestTemplate`, which is in maintenance mode; **`@ServiceConnection`** for Testcontainers, which deleted the `@DynamicPropertySource` boilerplate from every integration test; **pattern matching for switch** replacing an instanceof chain; **text blocks** for embedded JSON or SQL in tests. If you haven't adopted anything recent, say that honestly and name something you've read about and would use, with the reason — that's a much better answer than claiming production experience you can't defend under one follow-up question. Be careful specifically with **virtual threads**: don't claim them in production unless you can discuss pinning and `synchronized` blocks.",

  "How do you evaluate whether to upgrade major versions at work?":
    "Lead with the **trigger**, because the most credible reason is rarely enthusiasm: the current version approaching **end of security support** turns an upgrade from a nice-to-have into a date on the calendar, and a CVE in a dependency does the same. Then the process, which should sound procedural rather than heroic: read the migration guide and the deprecations, do it in a branch, and let the **test suite** find what broke — which is the real-world argument for having one. Go **one major version at a time** (2.7 → 3.0 → 3.1, not 2.5 → 3.2), since the guides are written pairwise. Check **dependency compatibility first**, because a library that never made the Jakarta namespace move is a blocker no amount of your own effort fixes. And weigh the cost honestly against the benefit — \"we stayed on the old version for two quarters because the upgrade wasn't worth the disruption mid-project\" is a legitimate engineering answer.",

  // ===================== Q158: Technical disagreement =====================
  "How did you present alternatives with data or prototypes?":
    "A **prototype ends more arguments than an opinion does**, and it's usually cheaper than the debate it replaces — half a day spiking both approaches beats three meetings. Where you can't measure, name the **specific failure mode** rather than a general worry: \"if the payment provider is slow, this holds a database connection for the whole call and exhausts the pool\" is something the other person can engage with, where \"this feels fragile\" isn't. Bring numbers when they exist — a benchmark, a query plan, an error rate — and be explicit about what you measured, since a benchmark on unrealistic data is worse than none. Write it down for anything significant, even a short decision record, so the reasoning survives the meeting and nobody relitigates it in six months having forgotten why.",

  "What did you do when the team chose a different path?":
    "**Commit properly** — that's the whole point of the question, and the interviewer is checking you're not someone who sulks or relitigates decisions in standup. Say what you actively did to make the chosen path work and to de-risk the part that worried you: tests around that specific area, a metric or alert on the failure mode you'd predicted, a documented note on what would trigger revisiting the decision. That's the professional version of disagree-and-commit, and it gives the story a real ending rather than trailing off. Then say what actually happened. If the concern never materialised, saying so plainly is a **stronger** answer than implying you were secretly right — it shows you update on evidence. If it did materialise, describe how the early warning you'd set up helped catch it.",

  "What did you learn about communication from that situation?":
    "Make it specific, because this is where generic answers cluster. The lessons that actually land are usually about **timing and audience**: raising a concern before the decision hardened rather than after, moving to a call once a written thread started going in circles, or framing the argument in terms the business cared about (downtime, delivery date, on-call load) rather than technical elegance. Another good one is realising you'd been arguing a **conclusion rather than a constraint** — leading with \"we should use Postgres\" instead of \"we need transactional consistency across these four entities\", which is the thing people can actually engage with. Avoid a lesson that's really a complaint about a colleague (\"I learned some people don't listen\"), and avoid learning nothing. The strongest version includes noticing where **you** were wrong or overconfident.",
};
