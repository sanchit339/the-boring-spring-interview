/**
 * Answer bank for followup questions — Spring Core category (Q36–Q52).
 * Keyed by the exact followup `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - BAD/GOOD code only where it proves understanding
 */
export const followupAnswersSpringCore: Record<string, string> = {

  // ===================== Q36: Spring Framework overview =====================
  "How is the Spring ecosystem organized, and where does Boot sit in it?":
    "The Spring ecosystem is organised into **foundational modules** and **integration projects**. **Spring Core** (`spring-context`, `spring-beans`) provides the IoC container, DI, and AOP. **Spring MVC** (`spring-webmvc`) is the web layer for building REST APIs and HTML views. **Spring Data** abstracts repository patterns over JPA, MongoDB, Redis, and others. **Spring Security** handles authentication, authorisation, and filter-chain-based protection. **Spring Boot** sits on top of all of them, providing auto-configuration and starters so you don't wire the modules together manually. **Spring Batch**, **Spring Integration**, and **Spring Cloud** extend the ecosystem for batch processing, messaging, and distributed system primitives respectively.",

  "How does Spring promote loose coupling compared to manual object wiring?":
    "Manual object wiring hard-codes `new ConcreteClass()` inside dependents, so swapping an implementation means editing every caller. Spring flips this: you declare a dependency on an **interface**, the container selects which implementing bean to inject, and you never reference the concrete class name in business code. That means swapping `SmtpEmailClient` for `SendGridEmailClient` is a single annotation change — no business code touched. **Loose coupling** also unlocks unit testing: in a plain JUnit test you pass a mock directly to the constructor without booting a Spring context, making tests milliseconds fast instead of seconds slow.",

  "What is the difference between Spring Framework and Spring Boot?":
    "**Spring Framework** is the raw toolkit — IoC container, AOP, MVC, JDBC template. It provides the building blocks but requires you to manually wire them: configure `DispatcherServlet`, pick a Tomcat version, align Jackson and Hibernate versions in `pom.xml`. **Spring Boot** wraps the Framework with **auto-configuration** (detects what's on the classpath and configures it automatically), **embedded Tomcat/Jetty**, and **starter POMs** that guarantee version compatibility. The result: a `@SpringBootApplication` with a `main()` method is a deployable application — no `web.xml`, no external server, no manual version juggling. Spring Boot doesn't replace the Framework; it drives it with opinionated defaults you can override.",

  // ===================== Q37: IoC and DI =====================
  "Is DI a form of IoC, or are they the same thing?":
    "IoC is the **principle**; DI is one **implementation** of that principle. IoC means: don't let objects control their own dependencies — invert that control to an external mechanism. DI implements this by passing dependencies into a class via constructor, setter, or field, so the class never calls `new` itself. Other IoC patterns exist — the **Service Locator** pattern (where a class asks a registry for its dependency) and the **Template Method** pattern (where a framework calls your hook methods) are also IoC. Spring uses DI specifically because it keeps application classes completely unaware of the container.",

  "What is the Hollywood Principle, and how does it relate to IoC?":
    "The Hollywood Principle is the catchphrase for **Inversion of Control**: your code doesn't reach out and pull in its dependencies — the framework calls your code and provides what it needs. In a classic non-IoC app, `OrderService` calls `new PaymentService()`, actively pulling its dependency. With IoC, Spring **calls** `OrderService`'s constructor and **provides** the `PaymentService` bean — you wrote the slot, the container filled it. This is exactly how `@SpringBootApplication` works: Spring scans your annotated classes, instantiates them, and calls their constructors with the right arguments — your `main()` just starts the process.",

  "How does DI improve testability?":
    "When dependencies are injected through a constructor, you can instantiate the class in a plain JUnit test by passing mock objects directly — no Spring context, no embedded database, no 30-second startup time. `OrderService service = new OrderService(mockRepo, mockEmailClient)` is the entire test setup. With field injection, the `@Autowired` private field has no setter, so you can't inject a mock without either launching a Spring context (`@SpringBootTest`) or using reflection utilities (`ReflectionTestUtils`) — both add significant overhead. Constructor injection is the single biggest enabler of **fast, isolated unit tests** in Spring apps.",

  // ===================== Q38: DI types =====================
  "Compare constructor, setter, and field injection with pros/cons.":
    "**Constructor injection** is the recommended default: dependencies are `final`, guaranteed non-null, and injectable in plain tests without Spring. The downside is verbose constructors for classes with many dependencies — which is actually useful because it signals a violation of Single Responsibility. **Setter injection** is appropriate for optional dependencies with defaults — you can call `setX()` or not, and the bean still works. The risk: the field can't be `final`, and nothing prevents mutation after construction. **Field injection** (`@Autowired` on a private field) is the worst. You can't write a plain unit test (the field is inaccessible without reflection), the dependency is hidden in the implementation, and the class appears to have no dependencies from its public API.",

  "Why is constructor injection recommended for required dependencies?":
    "Constructor injection makes required dependencies **explicit and unforgeable** — you cannot instantiate the class without providing them, and the compiler enforces this. Marking fields `final` means the JVM guarantees they're set exactly once and never null after construction, which gives you thread safety for free. It also surfaces design problems early: a constructor with 7 parameters tells you the class has too many responsibilities. With `@Autowired` field injection, a class can be instantiated with `new` and appear to work until a null pointer fires at runtime on the very line that uses the missing dependency.",

  "When might setter injection still make sense?":
    "Setter injection makes sense for **optional dependencies with a safe default** — for example, a pluggable `MetricsReporter` that can be set in integration tests but falls back to a no-op in unit tests. It's also a stopgap for a **circular dependency** you can't refactor away today, because the bean is fully instantiated before the dependency is set — though on Spring Boot 2.6+ that still needs `spring.main.allow-circular-references=true` to boot at all. Spring's own framework code uses setter injection internally in a few places for optional plugin points. Outside of those two cases, prefer constructor injection.",

  // ===================== Q39: ApplicationContext =====================
  "What responsibilities does the container handle beyond creating beans?":
    "Beyond instantiation and DI, the container handles **bean lifecycle** (calling `@PostConstruct`, `@PreDestroy`, and `DisposableBean`), **AOP proxying** (wrapping beans with transaction or security proxies via `BeanPostProcessor`), **event publication and consumption** (`ApplicationEvent` and `@EventListener`), **message source resolution** for i18n, and **environment and property abstraction** (`@Value`, `Environment`, `@ConfigurationProperties`). It also manages **bean scopes** — spinning up and tearing down request-scoped beans per HTTP request, for example. Every Spring feature you rely on (`@Transactional`, `@Cacheable`, `@Async`) is implemented by `BeanPostProcessor` or `BeanFactoryPostProcessor` hooks running inside the container.",

  "Which `ApplicationContext` implementation does Spring Boot actually create?":
    "**`AnnotationConfigServletWebServerApplicationContext`** for a servlet app on Tomcat, Jetty, or Undertow, and **`AnnotationConfigReactiveWebServerApplicationContext`** for WebFlux on Netty. `SpringApplication.run()` picks between them by looking at what's on the classpath, so you almost never name one yourself. A non-web Boot app gets a plain `AnnotationConfigApplicationContext` instead — that's also what you'd instantiate by hand in a standalone Spring app using `@Configuration` classes. The XML-era `ClassPathXmlApplicationContext` still exists but is legacy. The practical takeaway: the web-server-aware variants are what make the embedded server part of the context lifecycle rather than something you start separately.",

  "When is the ApplicationContext created in a Spring Boot app?":
    "The `ApplicationContext` is created during `SpringApplication.run()`, which is called from your `main()` method. Spring Boot goes through a fixed sequence: prepare the environment (load `application.properties`), create the right `ApplicationContext` implementation, run `BeanFactoryPostProcessors` (including `@ConfigurationClassPostProcessor` which processes all `@Bean` and `@ComponentScan` definitions). Then it instantiates all singleton beans, runs `BeanPostProcessors`, calls `@PostConstruct` methods, and publishes `ApplicationReadyEvent`. This all happens **before** the embedded Tomcat accepts its first HTTP request — which is why a misconfigured bean crashes the startup process, not a user request.",

  // ===================== Q40: BeanFactory vs ApplicationContext =====================
  "Does ApplicationContext eagerly or lazily instantiate singleton beans by default?":
    "`ApplicationContext` **eagerly instantiates** all non-lazy singleton beans during startup. This means every `@Service`, `@Repository`, and `@Component` with singleton scope has its constructor called and dependencies wired before the app is ready to serve traffic. The benefit is **fail-fast** — a misconfigured bean, missing dependency, or invalid `@Value` injection crashes startup immediately rather than failing mid-production-request. You can opt out by annotating specific beans with `@Lazy`, which defers instantiation to first access, but that reintroduces the risk of runtime failures for that bean.",

  "What does `ApplicationContext` give you that `BeanFactory` doesn't?":
    "`ApplicationContext` adds **application event publishing** via `ApplicationEventPublisher` — beans fire and listen to events (`@EventListener`) without tight coupling. It adds **message source** support for internationalized strings, resolving `getMessage('error.notfound', ...)` to locale-specific text. It provides **resource loading** (`getResource('classpath:data.sql')`). Critically, it integrates **AOP** by detecting `@Aspect` beans and applying `BeanPostProcessor` logic to wrap target beans in CGLIB or JDK proxies for `@Transactional`, `@Cacheable`, `@Async`, and custom aspects. `BeanFactory` has none of this — it only creates and retrieves beans.",

  "When would you ever use BeanFactory directly?":
    "Almost never in application code. `BeanFactory` is occasionally used in **memory-constrained embedded or IoT environments** where the overhead of a full `ApplicationContext` matters. Inside Spring's own framework code, `BeanFactory` is the internal API that `ApplicationContext` delegates to for core bean storage and retrieval. You might interact with `ConfigurableListableBeanFactory` in a custom `BeanFactoryPostProcessor` that programmatically registers or modifies bean definitions before the context fully starts. Outside of framework extension code, using `BeanFactory` directly is a code smell.",

  // ===================== Q41: Bean scopes =====================
  "What happens if a singleton bean depends on a prototype bean?":
    "The singleton is created once at startup, and Spring injects the prototype bean instance **at that single point** — the singleton holds onto that one instance forever. Subsequent calls to the singleton's method use the same prototype object, completely defeating the purpose of prototype scope. To get a fresh prototype instance per call, you inject `ApplicationContext` and call `context.getBean(MyPrototype.class)` each time, or use `@Lookup` method injection. Spring overrides the method at runtime using CGLIB and returns a new prototype instance per invocation. The `ObjectProvider<T>` approach is the cleanest modern alternative.",

  "How do you inject a request-scoped bean into a singleton safely?":
    "Use `@Scope(value = WebApplicationContext.SCOPE_REQUEST, proxyMode = ScopedProxyMode.TARGET_CLASS)` on the request-scoped bean. Spring then injects a **CGLIB proxy** into the singleton rather than the real bean. Each time the singleton calls a method on the proxy, the proxy looks up the real request-scoped instance from the active HTTP request's attributes and delegates to it. Without `proxyMode`, Spring injects the actual bean instance at singleton creation time (startup), but there's no active request then — it either crashes or captures the wrong instance forever. `ScopedProxyMode.INTERFACES` works if the bean implements an interface; `TARGET_CLASS` is for concrete classes.",

  "Are web scopes available outside a web ApplicationContext?":
    "No — **request**, **session**, and **application** scopes require a web-aware `ApplicationContext` (`WebApplicationContext`) and an active `RequestContextHolder` bound to the current thread. If you try to use a `@Scope(\"request\")` bean in a plain `AnnotationConfigApplicationContext` (a non-web context), Spring throws `IllegalStateException: No thread-bound request found`. This catches people when they run unit tests that boot a non-web context but instantiate beans that depend on request-scoped beans. The fix for tests is to use `@WebMvcTest`, `MockMvc`, or manually bind a mock request to `RequestContextHolder`.",

  // ===================== Q42: Bean lifecycle =====================
  "Where do `@PostConstruct` and `@PreDestroy` fit in the lifecycle?":
    "`@PostConstruct` runs **after** the constructor executes and after all dependencies are injected — it's the safe place to write initialization logic that needs injected fields to be non-null (opening a connection pool, loading a cache, validating configuration). It runs **before** the bean is made available to the rest of the application. `@PreDestroy` runs **before** the container removes the bean from scope — for singletons this happens on application shutdown, triggered by `context.close()` or a JVM shutdown hook registered by Spring Boot. It's where you release resources: close clients, flush buffers, deregister from service registries.",

  "What is the difference between `InitializingBean` and `@PostConstruct`?":
    "Both achieve the same thing — running code after DI completes — but `InitializingBean` is a **Spring-specific interface** requiring you to implement `afterPropertiesSet()`, which couples your class to the Spring API. `@PostConstruct` is a **standard JSR-250 annotation** from `jakarta.annotation`, meaning your class has no Spring import on it at all — it's portable to any CDI or EE container. Spring's own documentation recommends `@PostConstruct` for this reason. The only time you'd use `InitializingBean` is when writing Spring infrastructure code (a custom `BeanPostProcessor` or starter library) where tight Spring coupling is acceptable.",

  "What do BeanPostProcessors do, and when do they run?":
    "`BeanPostProcessor` is a Spring extension hook that intercepts **every bean** after instantiation and DI, calling `postProcessBeforeInitialization` (before `@PostConstruct`) and `postProcessAfterInitialization` (after `@PostConstruct`). This is where Spring's most important features live: `AutowiredAnnotationBeanPostProcessor` processes `@Autowired`, `CommonAnnotationBeanPostProcessor` processes `@PostConstruct`/`@PreDestroy`, and `AnnotationAwareAspectJAutoProxyCreator` wraps beans in AOP proxies for `@Transactional` and `@Cacheable`. They run during context startup for every singleton bean in sequence. A custom `BeanPostProcessor` lets you add cross-cutting infrastructure (metrics, tracing, custom validation) to all beans without touching each one individually.",

  // ===================== Q43: Stereotypes =====================
  "Are they functionally the same for component scanning?":
    "Yes — for the purpose of **component scanning and bean registration**, `@Service`, `@Repository`, and `@Controller` are identical to `@Component`. Spring's classpath scanner detects any annotation meta-annotated with `@Component` and registers the class as a bean. The differences are **semantic and functional**, not scanning-level: `@Repository` activates persistence exception translation via `PersistenceExceptionTranslationPostProcessor`; `@Controller` signals to `DispatcherServlet` that the class handles HTTP requests; `@Service` is purely semantic but communicates architectural intent to developers. Using the right stereotype makes the codebase self-documenting about which layer a class belongs to.",

  "What breaks if you annotate a DAO with `@Component` instead of `@Repository`?":
    "You lose **persistence exception translation**, and you lose it silently. With `@Repository`, `PersistenceExceptionTranslationPostProcessor` wraps the bean in a proxy that catches vendor-specific exceptions — `HibernateException`, `SQLException`, `MongoException` — and rethrows a uniform `DataAccessException` subclass, like `DataIntegrityViolationException` for a unique-constraint violation. Drop to `@Component` and the raw vendor exception propagates instead, so a service layer catching `DataAccessException` simply stops matching and the error falls through to your generic 500 handler. The bean still registers and the app still starts — nothing tells you it broke. That translation is also what keeps service code free of Hibernate and JDBC imports, so it stays the same if you swap the database technology underneath.",

  "When would you use plain `@Component` vs a stereotype annotation?":
    "Use `@Component` for classes that don't fit cleanly into the MVC layer model — utility beans, third-party adapters, event publishers, file parsers, or infrastructure helpers. Examples: a `CsvParser` that reads files, a `SlackNotifier` that wraps a Slack API client, or a `FeatureFlagEvaluator` that doesn't own HTTP routes, business logic, or DB access. If a class belongs to one of the three tiers (web, service, persistence), always use the specific stereotype — it communicates intent and activates tier-specific framework behaviour. `@Component` is the fallback when the class crosses layers or doesn't belong to any one tier.",

  // ===================== Q44: @Autowired resolution =====================
  "What is the order of type matching, qualifier, and bean name resolution?":
    "Spring resolves `@Autowired` in this order: **1. By type** — collect every bean assignable to the target type; exactly one match and it's injected. **2. By `@Qualifier`** — if the injection point carries one, only beans whose name or `@Qualifier` value matches survive. This filtering runs **before** `@Primary` is considered, which is why a qualifier beats a primary bean. **3. By `@Primary`** — among the surviving candidates, the one marked `@Primary` wins. **4. By `@Priority`** — `jakarta.annotation.Priority`, lowest value wins; rare in application code. **5. By name** — Spring falls back to matching the field or parameter name against the bean name. Still ambiguous after all that and you get `NoUniqueBeanDefinitionException` at startup. The implication: relying on step 5 is fragile — renaming a field silently repoints or breaks the injection with no compiler complaint.",

  "What happens if no matching bean is found and `required=true`?":
    "Spring throws **`NoSuchBeanDefinitionException`** wrapped in `UnsatisfiedDependencyException` during `ApplicationContext` startup — the app never comes up. This is the **fail-fast** design Spring relies on: you discover missing wiring immediately on deployment, not at 2am when a user hits the affected code path. You can make a dependency optional with `@Autowired(required = false)` (injects null if absent) or by changing the injection target to `Optional<T>` (Spring injects `Optional.empty()` if absent). Always prefer the `Optional<T>` form — `required=false` leaves the field null, which still crashes when you use it.",

  "Can you use `@Autowired` on constructors, setters, and fields?":
    "Yes — `@Autowired` works on **constructors**, **setter methods**, **arbitrary methods**, and **fields**. On a class with a single constructor, Spring 4.3+ injects it automatically without any `@Autowired` annotation at all. On multiple constructors, you must annotate exactly one with `@Autowired` to tell Spring which to use. On a setter method, Spring calls the setter after instantiation. On a field, Spring uses reflection to set it directly. Best practice: put `@Autowired` on the constructor (or omit it entirely if there's only one) and avoid field injection — constructor injection keeps the class testable and its dependencies explicit.",

  // ===================== Q45: @Qualifier and @Primary =====================
  "What is the difference between `@Qualifier` and `@Primary`?":
    "`@Primary` is a **global default** — it marks one bean as the preferred choice whenever the type matches and no other resolution rule narrows it further. Every injection point of that type in the whole application gets the primary bean by default. `@Qualifier` is a **local override** — applied at a specific injection point, it explicitly names which bean to inject regardless of `@Primary`. The practical split: use `@Primary` when one implementation is the right choice for 95% of use cases (e.g., the real `PaymentGateway` in production code). Use `@Qualifier` at the few injection points that need the alternate (e.g., a test double or secondary implementation).",

  "How does bean name relate to field name when resolving by name?":
    "By default, Spring names a bean using the **simple class name with its first letter lowercased** — `StripeGateway` becomes `stripeGateway`, `EmailSender` becomes `emailSender`. You can override this with `@Component(\"myCustomName\")`. When type-based resolution finds multiple candidates and no `@Qualifier` is given, Spring uses the **injection point's field or parameter name** as the fallback bean name to disambiguate. So `@Autowired private MessageSender smsSender;` resolves to the bean named `smsSender`. This is fragile — if you rename the field, resolution breaks silently. Always use `@Qualifier` explicitly instead of relying on field name matching.",

  "Can you combine `@Qualifier` with constructor injection?":
    "Yes — place `@Qualifier` on the **constructor parameter** directly: `public AlertService(@Qualifier(\"smsSender\") MessageSender sender)`. This is the cleanest and most explicit form of disambiguation because the injection contract is visible in the constructor signature — anyone reading the code knows exactly which implementation is expected. `@Qualifier` on a constructor parameter works the same as on a field or setter parameter. Avoid the alternative of annotating the constructor itself with `@Qualifier` — that's ambiguous when the constructor has multiple parameters of different types.",

  // ===================== Q46: Constructor vs field injection =====================
  "How does constructor injection help with immutability and required deps?":
    "When you declare injected fields as `final`, the Java compiler enforces that they're assigned exactly once — in the constructor — and never reassigned. Combined with constructor injection, this means a Spring bean is **fully initialised and immutable** the instant it's created. No null fields, no partially-constructed state visible to other threads, no opportunity for external code to swap a dependency later. This gives you thread safety for free in a singleton context. Field injection and setter injection both prevent `final` on the injected field, leaving the door open for null access and accidental mutation.",

  "Why is field injection harder to unit-test without Spring?":
    "With field injection, the annotated field is typically `private`, and Java doesn't let you set a private field directly from outside the class. In a plain JUnit test where you do `new OrderService()`, all `@Autowired` fields are `null` — Spring isn't running to inject them. To set them, you need `ReflectionTestUtils.setField(service, \"repo\", mockRepo)` — reflection-based, brittle to field renames, and IDE refactoring-unsafe. With constructor injection, the test is just `new OrderService(mockRepo)` — type-safe, refactoring-friendly, and requires zero Spring infrastructure.",

  "When do you still have to write `@Autowired` on a constructor?":
    "Only when the class declares **more than one constructor**. Spring won't guess which overload to wire, so you annotate exactly one with `@Autowired` — leave all of them bare and the container falls back to the no-arg constructor, or fails outright if there isn't one. Since **Spring 4.3**, a class with a single constructor is autowired implicitly, which is why modern Spring code has almost no `@Autowired` in it: one constructor taking the required parameters, `final` fields, no annotation noise. Lombok's `@RequiredArgsConstructor` generates exactly one constructor from the `final` fields, so it stays in the implicit case too.",

  // ===================== Q47: @Configuration and @Bean =====================
  "What is the difference between full `@Configuration` and lite `@Bean` methods?":
    "In **full `@Configuration`** mode, Spring subclasses the configuration class with CGLIB, so every `@Bean` method call is intercepted. If `beanA()` calls `beanB()` internally to share a dependency, CGLIB intercepts `beanB()` and returns the **same singleton instance** from the container rather than creating a new object. In **lite mode** (a class annotated with `@Component` or `@ComponentScan` — not `@Configuration` — that has `@Bean` methods), no CGLIB proxy is created. Calling `beanB()` directly from `beanA()` inside a lite-mode class invokes the raw Java method, creating a **new instance** every time — silently breaking singleton semantics for inter-bean dependencies.",

  "When would you define a bean with `@Bean` instead of stereotype annotations?":
    "Use `@Bean` for any class you **cannot annotate directly** — third-party library classes (Jackson's `ObjectMapper`, Stripe's `StripeClient`, Apache's `HttpClient`), classes from a framework you're integrating, or classes generated by a code generator. You can't put `@Component` on `ObjectMapper` because you don't own its source. `@Bean` in a `@Configuration` class lets you construct it, configure it (e.g., `mapper.configure(...)`) and hand it to Spring as a managed singleton. Also use `@Bean` when you need conditional creation logic that can't be expressed with stereotype annotations alone.",

  "How does `@Configuration` use CGLIB proxies?":
    "When Spring processes a class annotated with `@Configuration`, it creates a **CGLIB subclass** of it at startup. The subclass overrides every `@Bean` method to check the container first. If the bean already exists in the context it returns the cached instance; if not, it calls the original method and registers the result. This means `@Bean` methods are idempotent at the container level — calling `stripeClient()` three times from different `@Bean` methods all return the same bean. Without CGLIB (lite mode), `stripeClient()` is just a plain Java method that creates a new `StripeClient` on every call — singletons become inadvertent prototypes.",

  // ===================== Q48: Component scanning =====================
  "What is the default package scanned by `@SpringBootApplication`?":
    "`@SpringBootApplication` includes `@ComponentScan` with no explicit `basePackages`, which defaults to **the package of the class it's placed on** and all its sub-packages recursively. If your main class is `com.acme.billing.Application`, Spring scans `com.acme.billing.*` — every class in that package tree annotated with a Spring stereotype is detected. Anything in `com.acme.shared` or `com.acme.legacy` is invisible unless you add explicit `basePackages`. The strongest convention: place your main class in the **root package** of your application (`com.acme.billing`) so all feature sub-packages fall naturally under it.",

  "How do you include or exclude packages/filters from scanning?":
    "`@ComponentScan` accepts `includeFilters` and `excludeFilters` arrays where you specify `@Filter` entries. You can filter by annotation type (`FilterType.ANNOTATION`), by assignable type (`FilterType.ASSIGNABLE_TYPE`), by regex on class names (`FilterType.REGEX`), or with a custom `TypeFilter` implementation. A common use case: `@ComponentScan(excludeFilters = @Filter(type = FilterType.ANNOTATION, classes = Controller.class))` in a service-layer test context that shouldn't load web controllers. `@SpringBootApplication` carries two `excludeFilters` of its own: **`TypeExcludeFilter`**, the hook that lets test slices like `@WebMvcTest` narrow which beans load, and **`AutoConfigurationExcludeFilter`**, which stops auto-configuration classes from also being registered as ordinary `@Component`s. That's the detail that makes hand-writing the three composed annotations subtly different from using `@SpringBootApplication`.",

  "What happens if a bean is outside the scanned packages?":
    "Spring silently ignores it — no error, no warning. The `@Service` or `@Repository` annotation on the class does nothing if the classpath scanner never visits that package. The failure is discovered at runtime when an injection point for that type throws `NoSuchBeanDefinitionException` on application startup. This is a common problem in multi-module Maven projects where a utility module's classes live in a different root package and nobody notices until a deploy fails. The fix is either adjusting `basePackages` or placing the classes under the correct package hierarchy relative to the main application class.",

  // ===================== Q49: Spring Profiles =====================
  "How do you activate profiles via properties, env vars, and CLI args?":
    "**`application.properties`**: `spring.profiles.active=dev` — lowest priority, checked in. **Environment variable**: `SPRING_PROFILES_ACTIVE=prod` — Docker/Kubernetes standard, overrides properties. **JVM system property**: `-Dspring.profiles.active=staging` — note this goes *before* `-jar` on the command line. **Command-line argument**: `java -jar app.jar --spring.profiles.active=prod` — this one outranks everything else. **Programmatic**: `SpringApplication.setAdditionalProfiles(\"test\")` before calling `run()`. Multiple profiles are comma-separated: `SPRING_PROFILES_ACTIVE=prod,eu-region`. Precedence runs CLI arg > `-D` system property > env var > properties file. But environment-variable activation is still the Kubernetes standard, because it sets the profile without touching either the packaged JAR or the container's command.",

  "Can a bean belong to multiple profiles?":
    "Yes — `@Profile({\"dev\", \"test\"})` registers the bean when **either** `dev` or `test` is active (OR logic). For AND logic (both profiles must be active), use Spring 5.1's expression syntax: `@Profile(\"dev & test\")`. You can also negate with `!`: `@Profile(\"!prod\")` registers the bean in every environment except prod. A class-level `@Profile` applies to all `@Bean` methods inside a `@Configuration` class. Individual `@Bean` methods can also carry their own `@Profile`, which overrides or combines with the class-level one — giving fine-grained control over which beans appear in which environment.",

  "How do profile-specific `application-{profile}.yml` files work?":
    "Spring Boot automatically loads `application-{profile}.yml` (or `.properties`) when a profile is active and **merges** its properties on top of `application.yml`. If `spring.profiles.active=prod`, Spring loads `application.yml` first (base config), then `application-prod.yml`, with prod values overriding base values for matching keys. You don't declare the file in config — Spring discovers it by naming convention. Profile-specific files are the clean way to vary datasource URLs, API keys, log levels, and feature flags per environment without conditionals in code. Never commit secret values in prod profile files — use environment variables or a secrets manager for sensitive overrides.",

  // ===================== Q50: AOP =====================
  "What are cross-cutting concerns? Give 3 examples in a real app.":
    "Cross-cutting concerns are behaviours that apply across many different classes and layers — they **cut across** the normal module boundaries of your object model. **Logging**: every service method should log its input, output, and duration, but the logging code is identical boilerplate. **Transaction management**: every database-touching service method needs begin/commit/rollback — identical structure in every method. **Security/authorisation**: every controller or service method that accesses sensitive data needs a permission check before executing. Without AOP, each of these would be copy-pasted into hundreds of methods. With AOP, you write the concern once in an `@Aspect` and apply it globally via a pointcut expression.",

  "Does Spring AOP use proxies or bytecode weaving by default?":
    "Spring AOP uses **runtime proxies by default** — either JDK dynamic proxies (if the target bean implements an interface) or CGLIB subclass proxies (if it's a concrete class). The proxy intercepts method calls at the point where they cross a bean boundary. This is simpler and requires no special build tooling, but it has one fundamental limitation. **Self-invocation** (a method calling another method on the same class instance via `this`) bypasses the proxy entirely, so AOP advice doesn't run. **AspectJ** (full bytecode weaving at compile time or load time) doesn't have this limitation but requires a dedicated compiler plugin or Java agent. Spring Boot uses proxy-based AOP unless you add `spring-aspects` and configure load-time weaving.",

  "What is the self-invocation problem with Spring AOP proxies?":
    "Spring AOP works by wrapping your bean in a proxy — calls that come **through the proxy** trigger the advice; calls made via `this` inside the bean bypass it completely. So if `OrderService.placeOrder()` calls `this.validateOrder()`, and `validateOrder()` is annotated with `@Transactional`, the transaction advice never fires. The proxy is out of the call path, so `placeOrder()` runs and nothing is transactional. A `@Transactional` **private** method silently does nothing for a related but separate reason — a CGLIB subclass can't override a private method, so there's no place to attach the advice even when the call arrives from outside. The fixes for self-invocation: move the method to a separate `@Service` bean (the call then goes through that bean's proxy), inject the bean into itself, or switch to AspectJ weaving. Understanding this limitation is why Spring explicitly recommends placing `@Transactional` on the outermost public method that crosses bean boundaries.",

  // ===================== Q51: AOP advice types =====================
  "What is the difference between `@After`, `@AfterReturning`, and `@AfterThrowing`?":
    "`@After` is a **finally advice** — it runs after the method exits regardless of whether it succeeded or threw an exception, similar to a `finally` block. `@AfterReturning` runs **only after a successful return** — it has access to the return value via the `returning` attribute and can inspect but not modify it (unless you use `@Around`). `@AfterThrowing` runs **only when the method throws an exception** — you can access the exception object via the `throwing` attribute and log or transform it. The exception still propagates unless you swallow it (which `@Around` lets you do). Use `@After` for unconditional cleanup, `@AfterReturning` for auditing successful outcomes, and `@AfterThrowing` for centralised error logging.",

  "When would you use `@Around` instead of `@Before` + `@After`?":
    "Use `@Around` when you need to **control whether the method executes at all**, **modify its arguments before execution**, **modify or replace its return value**, or **handle exceptions and decide whether to rethrow them**. For example, a caching aspect uses `@Around` to check a cache key before calling `proceed()` — if the cache hits, it returns the cached value directly without ever calling the target method. A retry aspect uses `@Around` to catch exceptions and call `proceed()` again up to N times. `@Before` + `@After` can't do any of this — they observe the method's execution but can't redirect or suppress it. If you only need side-effects (logging, auditing), `@Before`/`@After`/`@AfterReturning` are simpler and less error-prone.",

  "What is a pointcut expression? Give a simple example.":
    "A **pointcut expression** is a pattern that Spring AOP evaluates against every method call to decide whether advice should run. The most common designator is `execution()`: `execution(* com.acme.service.*.*(..))` matches any method (`.*.*`) in any class (`*`) inside the `com.acme.service` package, with any return type (`*`) and any parameters (`(..)`). Other designators include `@annotation(Transactional)` (matches methods annotated with `@Transactional`), `within(com.acme.service.*)` (matches all methods within a package), and `bean(userService)` (matches methods on a specific named bean). Pointcuts can be combined with `&&`, `||`, `!` for complex matching. Store reusable pointcuts in a dedicated class with `@Pointcut` methods to avoid duplicating expressions across multiple aspects.",

  // ===================== Q52: Circular dependencies =====================
  "Does constructor injection allow circular dependencies by default?":
    "No — **constructor injection makes circular dependencies impossible** to resolve. When Spring tries to instantiate Bean A via its constructor, it needs Bean B. To instantiate Bean B, it needs Bean A — but A isn't done being constructed yet. Spring detects this during context startup and throws `BeanCurrentlyInCreationException` immediately. This is a feature, not a bug: constructor injection **forces you to discover and fix design problems at startup** rather than at runtime. Field and setter injection can technically tolerate a cycle — the bean is constructed first with no dependencies, then wired, so the container can hand out a partially-constructed reference to break the loop. But since **Spring Boot 2.6** that's switched off by default too: circular references fail the context for every injection style unless you explicitly set `spring.main.allow-circular-references=true`. So on any current Boot version the honest answer is that no injection style allows cycles out of the box.",

  "How does setter injection or `@Lazy` help break cycles?":
    "**Setter injection** breaks the cycle because Spring can fully instantiate both beans (calling their no-arg constructors) before injecting dependencies via setter calls — at setter-injection time, both objects exist in memory. On **Spring Boot 2.6 and later** that alone isn't enough: the container rejects the cycle regardless of injection style until you set `spring.main.allow-circular-references=true`. **`@Lazy`** on a constructor parameter needs no such flag — it tells Spring to inject a CGLIB proxy placeholder instead of the real bean, so as far as the container is concerned there's no cycle to detect. The proxy satisfies the constructor's type requirement immediately, so instantiation completes. The real bean is created only when the proxy's first method is called. Both are workarounds, not fixes — they let cycles compile and deploy, but the underlying design problem (two services that are tightly coupled to each other) remains and grows more entangled over time.",

  "Is fixing the design (extracting a third bean) better than workarounds? Why?":
    "Yes, always. A circular dependency between `OrderService` and `PaymentService` usually means both classes share a responsibility that neither should own alone — like sending a payment confirmation email. Extract `PaymentConfirmationService` that both can depend on without knowing about each other. The cycle is gone, the new class has a clear single responsibility, and neither `@Lazy` nor setter injection workarounds are needed. Workarounds like `@Lazy` hide the problem: the cycle still exists in the design, and future developers can't see it from the code structure. The proxy overhead also adds a small but real cost to every method call on the lazy bean. Design refactoring costs one afternoon; the design debt from a circular dependency compounds for years.",
};
