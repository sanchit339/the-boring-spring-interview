/**
 * Answer bank for follow-up questions — Spring Boot category (Q53–Q68).
 * Keyed by the exact follow-up `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - BAD/GOOD code only where it proves understanding
 */
export const followupAnswersSpringBoot: Record<string, string> = {

  // ===================== Q53: Spring Boot vs Spring Framework =====================
  "What boilerplate does raw Spring make you write that Boot removes?":
    "In raw Spring you hand-write a `DispatcherServlet` registration (`web.xml` or a `WebApplicationInitializer`), a `@EnableWebMvc` config class, a `ViewResolver`, Jackson `HttpMessageConverter` wiring, a `DataSource` bean, a `LocalContainerEntityManagerFactoryBean`, a `JpaTransactionManager`, and every library version pinned by hand in `pom.xml`. Boot replaces all of it with **auto-configuration plus starters** — one dependency and one `@SpringBootApplication`. You also stop maintaining a separate Tomcat install and WAR deploy pipeline, because the **server is embedded** in the JAR. The Spring programming model doesn't change at all — `@Service`, `@Autowired`, and `@Transactional` work identically; only the setup code disappears.",

  "Can you use Spring without Boot? When might you?":
    "Yes — Boot is a layer on top; the core container, MVC, and Data modules all run standalone. You'd skip Boot when you're maintaining a **legacy WAR** deployed to a corporate app server (WebLogic, WebSphere) whose ops team owns the container. Or when you need a **very small footprint** and don't want auto-config scanning dozens of classes at startup. Some teams also drop Boot when their org mandates an in-house framework that already does the wiring. For any new greenfield service the answer is basically always Boot — going without it means re-solving problems that are already solved.",

  "What does \"opinionated\" mean here, and how do you override an opinion?":
    "Opinionated means Boot **picks sane defaults for you** instead of making you decide: Tomcat on port 8080, HikariCP as the connection pool, Logback for logging, Jackson for JSON. Every opinion is a **default, not a lock-in**. You override by setting a property (`server.port=9090`), by declaring your own bean of that type (`@ConditionalOnMissingBean` makes Boot back off), or by excluding the auto-config entirely (`@SpringBootApplication(exclude = ...)`). That's the whole design contract: convention over configuration, with an escape hatch at every layer. If you find yourself fighting a default, you're usually one property or one `@Bean` away from winning.",

  // ===================== Q54: Starters =====================
  "What does `spring-boot-starter-web` actually pull in?":
    "**Spring MVC** (`spring-web` and `spring-webmvc`), `spring-boot-starter-json` for **Jackson** binding, and `spring-boot-starter-tomcat` for the **embedded server** — that last one is why `java -jar` gives you a listening HTTP port with no Tomcat installed anywhere. Every version comes from the parent POM or the BOM, so you never write a `<version>` tag for any of them. Two things people expect and don't get: **Bean Validation** is its own starter (`spring-boot-starter-validation`) since Boot 2.3, and there's no database support at all — that's `spring-boot-starter-data-jpa`, which adds Hibernate, Spring Data JPA, and a `JpaTransactionManager`. When you need the exact list for your Boot version, `mvn dependency:tree` beats guessing.",

  "What is `spring-boot-starter-parent`, and what does it manage?":
    "It's a **parent POM** that centralises build config so your `pom.xml` stays thin: a `dependencyManagement` block pinning compatible versions for hundreds of libraries, a default Java version, UTF-8 encoding, resource filtering for `application.yml` placeholders, and pre-configured plugins including `spring-boot-maven-plugin`. Because versions are managed, you declare dependencies **without a `<version>` tag** and get a set that Spring actually tested together. If you can't inherit from it (your company already has a corporate parent), import `spring-boot-dependencies` as a BOM with `<scope>import</scope>` instead — you get the version management without the parent slot.",

  "How would you create a custom starter for shared company config?":
    "Build two modules: an **autoconfigure module** holding `@AutoConfiguration` classes guarded by `@ConditionalOnClass` and `@ConditionalOnMissingBean`, plus a **starter module** that's just a POM pulling in the autoconfigure module and its dependencies. Register the config class in `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` so Boot discovers it. Name it `company-spring-boot-starter` — the `spring-boot-starter-*` prefix is reserved for official Spring starters. Always guard your beans with `@ConditionalOnMissingBean` so consuming teams can override your defaults; a starter that can't be overridden becomes the thing everyone works around.",

  // ===================== Q55: Auto-configuration =====================
  "What role do `@ConditionalOnClass`, `@ConditionalOnMissingBean`, etc. play?":
    "They're the **guard clauses** that decide whether an auto-configuration actually contributes beans. `@ConditionalOnClass` fires only if a type is on the classpath — that's how Boot configures Hibernate only when you added the JPA starter. `@ConditionalOnMissingBean` fires only if **you** haven't already defined that bean, which is the back-off mechanism that makes every default overridable. `@ConditionalOnProperty` gates config on a property value (feature toggles), and `@ConditionalOnWebApplication` restricts config to servlet or reactive apps. Without these, every auto-config would apply unconditionally and blow up the context the moment a class was missing.",

  "Where does Boot get the list of auto-configuration classes to evaluate?":
    "From `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` inside each jar — a plain text file listing one fully-qualified auto-config class per line. `@EnableAutoConfiguration` (via `AutoConfigurationImportSelector`) reads every copy of that file on the classpath, merges the lists, filters them against the `@Conditional` guards, and registers whatever survives. Boot 2.7 and earlier used the `spring.factories` file with an `EnableAutoConfiguration=` key; that was **deprecated in 2.7 and removed in 3.0**, so anything you read about `spring.factories` for auto-config is out of date. This file is exactly what your own custom starter has to ship.",

  "How do you find out which auto-configs applied and which backed off?":
    "Start the app with `--debug` (or set `debug=true`) and Boot prints a **Conditions Evaluation Report**: a Positive matches section for configs that applied, Negative matches with the specific reason each was skipped (`@ConditionalOnClass did not find required class ...`), plus Exclusions and Unconditional classes. If Actuator is on, hit **`/actuator/conditions`** for the same report as JSON on a running app. This is the first thing to reach for when a bean you expected doesn't exist or a database connection you didn't want gets created. The report names the exact condition that decided it, so you stop guessing.",

  // ===================== Q56: @SpringBootApplication =====================
  "What breaks if the main class sits in a sub-package instead of the root?":
    "`@ComponentScan` defaults to the **package of the annotated class and everything below it**, so a main class in `com.company.order.config` scans only `...config` and its children. Siblings like `com.company.order.service` and `...repository` never get scanned, and the app dies at startup with `NoSuchBeanDefinitionException` or an unsatisfied constructor dependency. Spring Data repositories and `@Entity` classes get missed the same way, since their scans default to the same base package. The fix is to move the class to the **root package** (`com.company.order`) rather than papering over it with `@ComponentScan(basePackages = ...)`, which just adds a list nobody remembers to update.",

  "Can you replace `@SpringBootApplication` with its composed annotations?":
    "Yes — writing `@SpringBootConfiguration`, `@EnableAutoConfiguration`, and `@ComponentScan` on the class is **functionally equivalent**, and Boot's own tests do exactly that. You'd do it when you need different attributes on the individual pieces, like a `@ComponentScan` with a custom `excludeFilters` alongside a plain `@EnableAutoConfiguration`. One caveat: `@SpringBootApplication` also carries the `@Filter` entries for `TypeExcludeFilter` and `AutoConfigurationExcludeFilter`, so hand-rolling it can subtly change test slicing behaviour. In practice keep the single annotation and use its `exclude`, `scanBasePackages`, and `nameGenerator` attributes.",

  "How do you exclude a specific auto-configuration?":
    "Use the annotation attribute — `@SpringBootApplication(exclude = DataSourceAutoConfiguration.class)` — or the property `spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration` when you want it per-environment. The classic case is having the JPA starter on the classpath (a shared parent brought it in) while a given service has no database. Boot then fails at startup with \"Failed to configure a DataSource: 'url' attribute is not specified.\" Use `excludeName` with a string if the class isn't on your compile classpath. Excluding is a scalpel — if you're excluding half a dozen configs, the real problem is a starter you shouldn't be depending on.",

  // ===================== Q57: Externalized configuration =====================
  "What is the property source precedence order?":
    "Highest to lowest, the ones that matter day to day: **command-line args** (`--server.port=9090`), then `SPRING_APPLICATION_JSON`, then Java **system properties** (`-D`), then OS **environment variables**, then **profile-specific files** (`application-prod.yml`), then plain `application.yml`, then `@PropertySource`, and finally defaults set via `SpringApplication.setDefaultProperties`. Watch the middle two — `-D` outranks env vars, which surprises people because env vars feel more external. Files outside the jar beat files inside the jar at the same level. The practical consequence: a container platform can override anything you baked into the image without a rebuild, which is exactly why you put env-specific values in env vars. Boot logs nothing about which source won, so use `/actuator/env` to see the resolved value and its origin.",

  "When would you prefer YAML over properties?":
    "YAML wins when config is **hierarchical or list-shaped** — `spring.datasource.hikari.*` nests once instead of repeating the prefix on every line, and lists are real list syntax instead of `recipients[0]=`, `recipients[1]=`. It also lets you put **multiple profiles in one file** separated by `---`, which keeps related env config side by side. The costs are real though: YAML is whitespace-sensitive so a stray indent silently changes meaning, and `@PropertySource` **can't load YAML** without a custom factory. Use YAML by default for app config; `.properties` is still fine for flat, short files.",

  "How do you supply a default for a property that might be missing?":
    "With `@Value` use colon syntax — `@Value(\"${payment.timeout-ms:5000}\")` yields 5000 when the property is absent. Without the default, a missing property throws `IllegalArgumentException` at startup and the context fails to load. With `@ConfigurationProperties` you just **initialise the field** (`private int timeoutMs = 5000;`) — plain Java, no annotation syntax. Deliberately omitting the default is a valid choice for things like a DB password: **fail fast at startup** beats booting with a silently wrong value and discovering it during the first request.",

  // ===================== Q58: @ConfigurationProperties =====================
  "How do you register a `@ConfigurationProperties` class as a bean?":
    "Three ways. Put `@Component` (or `@ConfigurationProperties` plus `@Configuration`) on the class so component scanning picks it up. Or add `@EnableConfigurationProperties(MailProperties.class)` on a config class — best when the properties class lives in a library you can't annotate. Or put **`@ConfigurationPropertiesScan`** on the main class, which scans for the annotation the way `@ComponentScan` scans for `@Component`. Forget all three and binding never runs — you get a class full of nulls and zeros with no error, which is the usual reason \"my properties aren't loading.\"",

  "How do nested objects, lists, and startup validation work with it?":
    "Binding is **relaxed and recursive**: `app.mail.smtp.host` binds to `getSmtp().setHost(...)` as long as the nested type has a getter and the setters exist, and `app.mail.recipients` binds straight into a `List<String>`. Relaxed binding also means `read-timeout`, `readTimeout`, and `READ_TIMEOUT` all hit the same `readTimeout` field — that's how env vars map onto YAML keys. Add **`@Validated`** on the class and Jakarta constraints (`@NotBlank`, `@Min`) run **at startup**, so a bad config kills the app immediately with a readable binding error instead of an NPE on the first request. For nested objects you must also annotate the field with `@Valid` or the nested constraints get skipped.",

  "Why is it preferred over many `@Value` injections for groups of related properties?":
    "It gives you **one typed object instead of fifteen scattered strings**: related settings travel together, you get IDE autocompletion and metadata, you can validate them all at startup, and tests can construct the object directly instead of standing up a Spring context. `@Value` has no nesting, no list binding, no validation, and it silently spreads the same property key across several classes so renaming a key means grepping the whole codebase. The rule of thumb: **one or two unrelated values, use `@Value`; a group that belongs to one concern, use `@ConfigurationProperties`**. Making the properties class immutable is the strongest form, since nothing can mutate config after startup. In Boot 3 a record or single-constructor class gets **constructor binding automatically** — `@ConstructorBinding` is no longer used at class level and is only needed to pick between multiple constructors. It also moved to `org.springframework.boot.context.properties.bind` in Boot 3.0, so older examples that put it on the class don't just misbehave — they won't compile.",

  // ===================== Q59: Environment-specific configuration =====================
  "How do profile-specific files and `spring.profiles.active` work together?":
    "`application.yml` always loads as the **baseline**; whatever profiles are listed in `spring.profiles.active` then load on top as `application-{profile}.yml` and override matching keys. Non-matching keys from the baseline survive, so profile files only need to contain the **deltas**, not a full copy of the config. Multiple active profiles are allowed (`spring.profiles.active=prod,metrics`) and later ones win over earlier ones on conflict. Set the value from outside — `SPRING_PROFILES_ACTIVE=prod` as an env var or `--spring.profiles.active=prod` on the command line — so the same artifact runs in every environment. Hardcoding an active profile inside `application.yml` defeats the entire point.",

  "How do you keep prod secrets out of Git?":
    "Never let a real credential reach a committed file: put a **placeholder** in `application-prod.yml` (`password: ${DB_PASSWORD}`) and inject the value at runtime from an env var, a Kubernetes Secret, or a mounted file. For anything beyond basics use a real secret store — **HashiCorp Vault** (Spring Cloud Vault), **AWS Secrets Manager**, or Azure Key Vault — which adds rotation and audit logging that env vars don't have. Add a scanner like gitleaks to CI so a leak fails the build rather than getting discovered later. If a secret does get committed, **rotate it** — deleting the commit doesn't help, the value is in the reflog, on every clone, and possibly already scraped.",

  "What is the difference between multi-document YAML and separate profile files?":
    "Multi-document YAML puts every environment in one `application.yml`, split by `---`, with each block gated by `spring.config.activate.on-profile`. Separate files (`application-dev.yml`, `application-prod.yml`) put each environment in its own file, discovered by naming convention. **Behaviour is identical** — this is purely organisation. One file is convenient when the diffs are three lines each and you want them visible side by side; separate files scale better, keep diffs clean, and let you control access per environment. One gotcha: in a multi-document file, the `spring.profiles.active` property itself can only be set in the **first, unprofiled document**.",

  // ===================== Q60: DevTools =====================
  "Which property defaults does DevTools change in development?":
    "It turns off every **template and resource cache** that would otherwise hide your edits — `spring.thymeleaf.cache`, `spring.freemarker.cache`, `spring.mustache.cache`, and `spring.web.resources.chain.cache` all become `false`. It also enables `spring.h2.console` and bumps `logging.level.web` to `DEBUG` so you see request mappings and dispatch details. These are applied as a **low-priority property source**, so anything you set explicitly still wins. The point is that a template edit shows up on refresh instead of after a restart — and none of it follows you to production, because DevTools is disabled there.",

  "Is DevTools included in production builds by default?":
    "No, and it's belt-and-braces. You mark it `<optional>true</optional>` (Maven) or `developmentOnly` (Gradle) so it isn't a **transitive dependency** for anything depending on your artifact, and `spring-boot-maven-plugin` excludes it from the repackaged fat JAR. On top of that, DevTools **disables itself at runtime** when it detects the app was launched from a packaged JAR — it checks whether the classloader looks like a proper application classloader. So even if a jar sneaks in, restart and LiveReload stay off. That last check matters because DevTools opens a LiveReload server and, with remote debug enabled, is a genuine security exposure.",

  "What is the difference between the base and restart classloaders?":
    "DevTools splits your classpath in two: the **base classloader** holds jars from `/lib` that essentially never change, and the **restart classloader** holds your own compiled classes, which change constantly. On a code change DevTools throws away only the restart classloader and builds a new one, leaving all the third-party jars loaded. That's why a restart takes under a second instead of the several seconds a cold boot needs. It's still a **full context restart**, not hot-swapping: your beans are recreated and in-memory state is lost. Swapping method bodies in place without restarting is what JRebel or the JVM's HotSwap do, and that's a different mechanism.",

  // ===================== Q61: Actuator =====================
  "Which endpoints would you expose in production, and how do you secure them?":
    "Expose the minimum: **`health`** for load balancer and Kubernetes probes, **`info`** for build and version metadata, and **`prometheus`** or `metrics` for your monitoring stack. Keep `env`, `configprops`, `beans`, `mappings`, `threaddump`, and especially **`heapdump`** off the public surface — they leak configuration values, internal structure, and in the case of a heap dump, live credentials sitting in memory. Set `management.endpoints.web.exposure.include` explicitly rather than `*`, require an ADMIN role for everything except the probes in your `SecurityFilterChain`, and better still move Actuator to a **separate port** (`management.server.port=9001`) that's only reachable inside the cluster. Publicly exposed Actuator endpoints are a well-known and routinely scanned attack vector.",

  "What do `/health`, `/info`, `/metrics`, and `/env` show?":
    "**`/health`** aggregates every registered `HealthIndicator` — DB connectivity, disk space, Redis, your custom checks — into one UP/DOWN status; component details only appear when `management.endpoint.health.show-details` allows it. **`/info`** shows arbitrary build metadata — version, Git commit, build time — populated from `info.*` properties and `build-info.properties`. **`/metrics`** lists Micrometer meter names (`jvm.memory.used`, `http.server.requests`) and drills into one via `/metrics/{name}`; `/prometheus` is the same data in scrape format. **`/env`** dumps every property source and resolved value — Boot masks things that look like passwords or keys, but the masking is heuristic, so treat this endpoint as sensitive regardless.",

  "How do you customize health status aggregation?":
    "By default any indicator reporting **DOWN drags the whole app DOWN** (HTTP 503) — that's what `SimpleStatusAggregator` does with the default severity order `DOWN, OUT_OF_SERVICE, UP, UNKNOWN`. Reorder it with `management.endpoint.health.status.order` and map statuses to HTTP codes with `management.endpoint.health.status.http-mapping.*`, so a non-critical status can still return 200. For a genuinely non-critical dependency, the better move is to keep it out of the aggregate entirely. Put it in a **health group** (`management.endpoint.health.group.readiness.include=db,diskSpace`) so your Kubernetes readiness probe only watches what should actually take the pod out of rotation. Otherwise a flaky recommendations API takes down a checkout service that can serve fine without it.",

  // ===================== Q62: Custom health indicator =====================
  "How do you stop a slow dependency check from hanging `/health`?":
    "Give the check its own **short timeout** — a two-second connect/read timeout on the `RestClient` or JDBC probe it uses — because the health endpoint is only useful if it answers faster than the probe that calls it. Boot doesn't time out `HealthIndicator` beans for you, so an indicator that blocks on a dead TCP connection blocks the response, which turns a slow dependency into a failed liveness probe and a **restart loop**. Catch every exception and return `Health.down(ex)` instead of letting it propagate. For genuinely slow checks, cache the last result for a few seconds or move the probe to a scheduled job that the indicator only reads from.",

  "When would you mark a custom check as DOWN vs OUT_OF_SERVICE?":
    "**DOWN** means broken — the dependency is unreachable or erroring and you want traffic diverted and someone paged. **OUT_OF_SERVICE** means intentionally unavailable — the instance is healthy but shouldn't take traffic right now, like a planned maintenance window, a warm-up phase, or a manual drain before deploy. The distinction matters operationally: DOWN should alert, OUT_OF_SERVICE shouldn't wake anyone up. Both count as \"not UP\" in the default aggregation and both return 503, so wire them into the right health group and alerting rules or the distinction stays purely decorative.",

  "How does readiness vs liveness differ in Kubernetes health probes?":
    "**Liveness** answers \"is this process wedged?\" — a failure makes Kubernetes **kill and restart** the container. **Readiness** answers \"can it serve traffic right now?\" — a failure just pulls the pod out of the Service load balancer, no restart. Boot exposes `/actuator/health/liveness` and `/actuator/health/readiness` once you enable the probes (automatic on Kubernetes, otherwise `management.endpoint.health.probes.enabled=true`). The classic production outage: putting the **database check in the liveness probe**. The DB blips, every pod fails liveness, the whole fleet restarts simultaneously, and now you have a thundering herd on a database that was about to recover. Database checks belong in readiness; liveness should only fail when the JVM itself is unrecoverable.",

  // ===================== Q63: Embedded servers =====================
  "What happens if two embedded server starters land on the classpath?":
    "Nothing fails, and that's the trap. `ServletWebServerFactoryAutoConfiguration` imports the Tomcat, Jetty, and Undertow configurations **in that fixed order**, each guarded by `@ConditionalOnClass` plus `@ConditionalOnMissingBean(ServletWebServerFactory.class)`. The first one whose classes are present registers the factory and the rest back off — so **Tomcat silently wins** every time it's on the classpath. Add `spring-boot-starter-jetty` but forget to exclude Tomcat from `spring-boot-starter-web` and you're still running Tomcat: your `server.jetty.*` properties do nothing, and the only hint is the startup log line naming the server. The fix is the **exclusion**, not a hand-declared factory bean. Run `mvn dependency:tree` when you're unsure who dragged the second server in — it's often a shared internal starter rather than your own POM.",

  "How do you switch from Tomcat to Jetty or Undertow?":
    "Exclude `spring-boot-starter-tomcat` from `spring-boot-starter-web` and add `spring-boot-starter-jetty` or `spring-boot-starter-undertow` — auto-configuration detects whichever factory is on the classpath and uses it. Application code doesn't change; `server.port` and most `server.*` properties are portable, though **server-specific tuning** properties (`server.tomcat.*` vs `server.undertow.*`) obviously aren't. Switching is rare in practice — the real reasons are a container-specific tuning need or an org standard, not raw speed. On modern Boot with virtual threads the throughput gap between the three is small enough that it's not worth claiming a win you haven't measured.",

  "When would you deploy as WAR to an external server instead?":
    "When the deployment target isn't yours to choose: a **corporate app server** (WebLogic, WebSphere, a shared Tomcat) that ops owns and monitors, a licensing or compliance rule requiring the certified container, or several apps that must share one server's resources and connection pools. Also legacy migration — you move an existing WAR onto Boot without changing how it's deployed. The costs are real: you give up `java -jar`, lose the guarantee that dev and prod run the same server version, and inherit the container's classloader quirks. For anything containerised, the fat JAR is the right answer.",

  // ===================== Q64: Port / server config =====================
  "How do you set the port via `application.yml`, env var, and CLI?":
    "`server.port: 9090` in `application.yml`, `SERVER_PORT=9090` as an environment variable (relaxed binding maps the underscores onto the dotted key), or `--server.port=9090` as a command-line argument to `java -jar`. Precedence runs **CLI > env var > yml**, so a container platform can override the baked-in value without a rebuild. Two special values: **`server.port=0`** binds a random free port — the standard trick for integration tests with `@SpringBootTest(webEnvironment = RANDOM_PORT)` and `@LocalServerPort` — and **`-1`** tells Tomcat not to open a connector at all. Note that `-1` still builds a **web** application context with the whole servlet stack in it — if you actually want a non-web app, set `spring.main.web-application-type=none` instead.",

  "What happens if the configured port is already in use?":
    "Startup **fails fast**: the embedded server throws, Boot's failure analyzer prints \"Web server failed to start. Port 8080 was already in use,\" and the process exits non-zero rather than running without a listener. That's the behaviour you want — a half-started app that accepts no connections but keeps its health check alive is far worse. In practice it's another instance you forgot to kill (`lsof -i :8080`) locally, and in Kubernetes it means two containers in one pod claiming the same `containerPort`. If you genuinely don't care which port you get, set `server.port=0` and read the assigned one from the event or `@LocalServerPort`.",

  "How do you configure SSL on the embedded server?":
    "Point Boot at a keystore: `server.ssl.key-store=classpath:keystore.p12`, `server.ssl.key-store-password=...`, `server.ssl.key-store-type=PKCS12`, and `server.ssl.key-alias`. Setting any `server.ssl.*` property flips the connector to HTTPS, so `server.port=8443` now serves TLS **only** — one connector can't do both, and serving HTTP too means registering a second connector programmatically. Boot 3.1+ adds **SSL bundles** (`spring.ssl.bundle.pem.*`), which support PEM files directly and reload on change, which suits cert-manager-issued certs. Realistically, most production Boot apps terminate TLS at an ingress or load balancer and speak plain HTTP inside the cluster — embedded SSL matters for internal service-to-service mTLS or when there's no proxy in front.",

  // ===================== Q65: Logging =====================
  "How do SLF4J and Logback relate to each other?":
    "**SLF4J is the facade** — the `Logger` and `LoggerFactory` interfaces your code compiles against — and **Logback is the implementation** that actually formats and writes the output. You always code to SLF4J so the backend stays swappable; `spring-boot-starter-logging` ships Logback as the default binding. Boot also installs **bridges** (`jul-to-slf4j`, `log4j-to-slf4j`) so a library logging through java.util.logging or the Log4j2 API still lands in your Logback config with one consistent format. To swap in Log4j2, exclude `spring-boot-starter-logging` and add `spring-boot-starter-log4j2` — not one line of application code changes, which is exactly the point of the facade.",

  "How do you set package-level log levels in `application.yml`?":
    "Under `logging.level`, key by package or class: `logging.level.root: INFO`, `logging.level.com.company.order: DEBUG`, `logging.level.org.hibernate.SQL: DEBUG`. Resolution is **most-specific-wins**, so a class-level entry overrides its package, which overrides `root`. The same keys work as env vars (`LOGGING_LEVEL_COM_COMPANY_ORDER=DEBUG`) for a container override without a redeploy. Even better on a live incident: **`POST /actuator/loggers/com.company.order`** with `{\"configuredLevel\":\"DEBUG\"}` changes the level on a running instance with no restart. Remember to set it back, because DEBUG on a busy service floods your log bill fast.",

  "How do you use a custom `logback-spring.xml` with profiles?":
    "Name the file **`logback-spring.xml`**, not `logback.xml` — the `-spring` variant is loaded by Boot rather than Logback directly, which is what enables the Spring extensions. Then wrap appenders in `<springProfile name=\"prod\">` blocks: console with a human-readable pattern for dev, JSON encoder to stdout for prod so your log aggregator can parse it. `<springProperty scope=\"context\" name=\"appName\" source=\"spring.application.name\"/>` pulls values out of the Environment into the config. Plain `logback.xml` initialises **before** the Spring context exists, so `<springProfile>` and `<springProperty>` are simply unavailable there — that's the mistake that makes people think profile-based logging doesn't work.",

  // ===================== Q66: CommandLineRunner / ApplicationRunner =====================
  "When do these runners execute in the application lifecycle?":
    "Right at the end of `SpringApplication.run()` — **after the context is refreshed** and all beans (including the web server) are initialised, but **before the method returns** and before `ApplicationReadyEvent` fires. So every bean is injectable and fully usable, and the embedded server is already accepting connections. A runner that throws makes `run()` propagate the exception and the application **shuts down**, which is a useful fail-fast for mandatory startup checks. If you need to run *after* the app is fully ready, use an `@EventListener(ApplicationReadyEvent.class)` method instead.",

  "How do you control order when multiple runners exist?":
    "Annotate each with **`@Order(n)`** or implement `Ordered` — lower values run first, and both `CommandLineRunner` and `ApplicationRunner` beans are sorted into a **single combined list**, so the two types interleave by order value rather than one type going first. With no `@Order`, the sequence is effectively arbitrary; don't rely on class name or declaration order. Better still, don't build a chain of order-dependent runners. If runner B truly needs runner A's output, make that a single runner calling two services so the dependency is explicit in code rather than encoded in integers spread across files.",

  "What are the risks of doing heavy work inside a runner?":
    "The runner blocks startup completion, so slow work delays **readiness** — in Kubernetes that means failed readiness probes and a container the platform may kill and retry, giving you a restart loop that looks like a crash but is really a slow migration. Anything unbounded (a full-table backfill, a warm-up hitting a slow third party) is the wrong fit. There's also a correctness trap: on a **multi-instance deploy every pod runs the runner**, so unguarded seeding double-inserts unless it's idempotent or guarded by a profile. Use runners for cheap, idempotent work — dev data seeding under `@Profile(\"dev\")`, a fail-fast config check. Put real schema changes in **Flyway or Liquibase** and real backfills in a job you can run and monitor independently.",

  // ===================== Q67: Packaging =====================
  "What is an executable fat/uber JAR?":
    "A single archive containing your classes in `BOOT-INF/classes`, **every dependency as a nested jar** in `BOOT-INF/lib`, and Boot's own loader classes at the root, with `MANIFEST.MF` declaring `Main-Class: org.springframework.boot.loader.launch.JarLauncher` and `Start-Class` pointing at your `@SpringBootApplication` — that launcher moved into the `...loader.launch` package in **Boot 3.2**, so guides showing `org.springframework.boot.loader.JarLauncher` predate it. `JarLauncher` installs a classloader that reads those nested jars **in place** — it doesn't unpack them to a temp directory, and it doesn't flatten everything into one namespace the way a shaded jar does. Duplicate resource files like `META-INF/spring.factories` don't clobber each other. That's why `java -jar app.jar` just works with no classpath argument, and why the layout is Boot-specific rather than plain-JAR standard.",

  "What changes are needed to package as a deployable WAR?":
    "Three things: set packaging to `war`, make the main class **extend `SpringBootServletInitializer`** and override `configure(SpringApplicationBuilder)` to point at itself, and mark `spring-boot-starter-tomcat` as **`<scope>provided</scope>`** so the embedded server doesn't collide with the container's own. `SpringBootServletInitializer` implements the servlet `WebApplicationInitializer` contract, which is how the external container bootstraps your Spring context — without it you get a WAR that deploys and serves nothing but 404s. Keep the `main()` method: with tomcat scoped `provided`, the artifact still runs standalone in your IDE while deploying as a WAR in production, which keeps local development sane.",

  "How do you run a Boot JAR with external config?":
    "Simplest: drop an `application.yml` **next to the jar** — Boot's default search path includes `./config/` and `./`, and files outside the jar override the packaged ones. For an explicit path use `--spring.config.location=file:/etc/order-service/application.yml`, or `--spring.config.additional-location=...` to **layer on top of** the packaged config rather than replacing it entirely (replacing is the usual mistake — you lose every baked-in default). Individual overrides go through env vars or `--key=value` args, both of which outrank files. In Kubernetes the normal pattern is a ConfigMap mounted at `/config` with secrets injected as env vars, which needs no code or image change at all.",

  // ===================== Q68: @RestController vs @Controller =====================
  "What happens if a `@Controller` method returns a `String` without `@ResponseBody`?":
    "Spring treats the string as a **logical view name**, hands it to the `ViewResolver`, and tries to render a template — so returning `\"success\"` looks for a `success` template instead of sending the text. With no template engine you get a 404 or `Could not resolve view with name 'success'`. If the string starts with `/` you can even hit **`Circular view path`**, where the resolved view maps back to the same URL and recurses. Adding `@ResponseBody` (or using `@RestController`) switches Spring to `HttpMessageConverter`s and writes the value straight to the body. Non-String returns like a `UserDto` behave the same way — Spring looks for a view, not JSON.",

  "When would you still reach for plain `@Controller`?":
    "When the response is **server-rendered HTML** — Thymeleaf, JSP, FreeMarker — and you're returning view names with a `Model`, which is still normal for admin panels, internal dashboards, and classic multi-page apps. Also for `redirect:` / `forward:` prefixes, and for **file downloads or view-based error pages** where you want the view resolution machinery. You can even mix within one class: a `@Controller` whose individual methods carry `@ResponseBody` for the occasional JSON endpoint. For any API consumed by a SPA, mobile client, or another service, `@RestController` is the right default.",

  "Can you mix both in the same application?":
    "Yes, routinely — they're just stereotypes on top of `@Component`, and `DispatcherServlet` decides per handler method whether to resolve a view or write the body. A typical app has `@RestController` classes under `/api/**` for the SPA and `@Controller` classes serving a Thymeleaf admin UI, with a template engine configured for the latter. The one thing to keep straight is **error handling**: `@RestControllerAdvice` returns JSON bodies while `@ControllerAdvice` can return error views, so scope each with the `basePackages` or `assignableTypes` attribute. Otherwise a browser hitting a broken page gets a raw JSON error blob, or an API client gets an HTML error page it can't parse.",

};
