/**
 * Answer bank for followup questions — Microservices category (Q113–Q124).
 * Keyed by the exact followup `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - BAD/GOOD code only where it proves understanding
 */
export const followupAnswersMicroservices: Record<string, string> = {

  // ===================== Q113: Microservices vs monolith =====================
  "What are the operational costs of microservices that people underestimate?":
    "The code split is the easy part — what people miss is that **every service needs its own everything**: pipeline, dashboards, alerts, secrets, database migrations, on-call rotation, and a version-compatibility story with everyone it calls. Local development stops being `mvn spring-boot:run` and becomes Docker Compose with eight containers, or a shared dev cluster where someone else's broken branch blocks you. Debugging a single user complaint now means correlating logs across five services, which is why distributed tracing isn't optional infrastructure — it's the *minimum* you need before you split anything. And every method call you turned into a network call can now be **slow, duplicated, or lost**, so code that was trivially correct in a monolith needs timeouts, retries, and idempotency to stay correct.",

  "When is a modular monolith a better choice?":
    "Almost always at the start, and specifically when you have **fewer developers than you'd have services** — a team of six running twelve deployables spends more time on YAML than on features. A modular monolith gives you the boundary discipline (separate packages or Gradle modules, no cross-module entity imports, communication through published interfaces) with **one deploy, one database, and real ACID transactions**. The boundaries are the valuable part; the network hop is just the expensive part, and you can add it later per module once traffic or team ownership actually demands it. Splitting before you understand the domain is the classic failure — you cement the wrong boundaries in HTTP, and moving them afterwards means coordinated releases across teams instead of a refactor in your IDE.",

  "How do you decide where one service ends and the next begins?":
    "You draw them around **bounded contexts** — a region of the business where one word means exactly one thing. \"Order\" in the checkout context means a cart being priced; in fulfilment it means a pick list; in billing it's an invoice line. Those are three models, not one shared `Order` entity, and trying to make them one is how you end up with a 60-field class that every team is afraid to change. Two practical tests: a boundary is probably right if **most changes touch one service** and if **one transaction never needs to span two of them**. If you keep needing distributed transactions or every feature requires three services to ship together, you cut in the wrong place — merge them back rather than papering over it with a saga.",

  // ===================== Q114: Service discovery / Eureka =====================
  "What is the difference between client-side and server-side discovery?":
    "In **client-side discovery** the caller pulls the full registry, picks an instance itself, and calls it directly — that's Eureka plus Spring Cloud LoadBalancer, where `http://order-service/orders` gets resolved in-process. One less network hop and the client can do smart things like zone-aware or least-loaded selection. But every client needs the discovery library, so a Python or Node service joining your estate has to reimplement it. In **server-side discovery** the client calls a fixed address and something in the middle (a Kubernetes Service, an AWS ALB, a gateway) does the lookup and forwards. Clients stay dumb and language-agnostic, at the cost of an extra hop and a component that has to be highly available. Kubernetes made the server-side model the default, which is why new Spring services usually just call `http://order-service:8080` and skip Eureka entirely.",

  "How long can a dead instance keep receiving traffic?":
    "Up to about **two minutes**, and the arithmetic is worth carrying. The client heartbeats every **30 seconds**; miss three and the server evicts the lease after **90 seconds**. Then clients **cache the registry locally and refresh on their own 30-second cycle**, so full propagation of a dead instance approaches two minutes.\n\nThat's exactly why discovery isn't reliability. Eureka will hand you an address that stopped answering a minute ago, so you still need a circuit breaker and retries on top of it.\n\nRegistration itself is simple: on startup the client **POSTs its instance metadata** — service id, host, port, health URL — and on graceful shutdown it sends an explicit deregister. Use `server.shutdown=graceful` and handle `SIGTERM` rather than relying on eviction to notice.",

  "What would you use instead of Eureka on Kubernetes, and why?":
    "On Kubernetes you use **Kubernetes Services and cluster DNS** — the platform already tracks which pods are ready, so `http://order-service` resolves through a stable ClusterIP with kube-proxy load balancing behind it, and readiness probes remove a pod from the endpoint list within seconds instead of Eureka's ~90. Running Eureka on top duplicates the same job with worse propagation and one more stateful thing to operate. **Consul** is the choice when you're not on Kubernetes and want discovery plus KV config plus health checking in one agent, especially across VMs or multiple data centres. Service meshes like **Istio or Linkerd** go further and take load balancing, retries, mTLS, and traffic splitting out of your code entirely — worth it at scale, heavy overhead for a handful of services.",

  // ===================== Q115: API Gateway =====================
  "How does Spring Cloud Gateway differ from Zuul?":
    "Zuul 1 is **blocking** — one servlet thread per in-flight request — so a slow downstream service exhausts the thread pool and the gateway stops serving *everything*, not just the slow route. Spring Cloud Gateway is built on **Spring WebFlux and Netty**: a handful of event-loop threads handle thousands of concurrent connections, and a downstream taking 30 seconds parks a cheap continuation instead of pinning a thread. That matters most exactly when you need the gateway to hold up, which is during an incident. Practically, Zuul 1 is **end of life** and Gateway is the Spring-supported option today — its filter model is also declarative (`predicates` and `filters` in YAML) so simple routing and rate limiting need no code. The one real gotcha: it's reactive top to bottom, so **never call blocking JDBC or `RestTemplate` inside a custom filter** — you'll block an event-loop thread and stall unrelated traffic. Worth knowing that Spring Cloud now also ships a **servlet-based variant, Gateway Server MVC**, giving you the same declarative routes without a reactive stack; the WebFlux one remains the default, and the blocking-filter warning applies only to it.",

  "What are the risks of a gateway becoming a bottleneck?":
    "It's a **single point of failure on the hot path** — every request goes through it, so if it's down, your whole API is down no matter how healthy the services are. Run at least two instances behind a load balancer, keep it stateless, and never let it hold in-memory rate-limit counters that force sticky routing (use Redis). It's also an **organizational bottleneck**: if every new endpoint needs a hand-edited route in a repo one team owns, the gateway gates every release. Prefer discovery-based routing (`lb://order-service`) or per-service route config so teams self-serve. And watch what you do *in* it. Response body transformation, aggregation of five downstream calls, or JWT validation that hits the network on every request all turn a cheap proxy into the slowest thing in your architecture. Cache the JWKS, set timeouts per route, and keep the gateway's own logic boring.",

  // ===================== Q116: Spring Cloud =====================
  "Which Spring Cloud projects would you actually reach for today?":
    "Fewer than the catalogue suggests, and it depends on the platform. On **Kubernetes** the platform already does discovery, load balancing, configuration, and health checking, so what's left is the in-process behaviour it can't do: **Spring Cloud CircuitBreaker** over Resilience4j for breakers, retries, bulkheads, and time limiters, plus **OpenFeign** for typed service-to-service calls. That's often the entire list. Off Kubernetes — VMs, ECS without service discovery, a hybrid estate — you add **Eureka** for the registry, **Spring Cloud Gateway** when the edge needs real request logic rather than plain routing, and **Spring Cloud Config** when you want configuration versioned in Git with review. **Spring Cloud Stream** earns its place only if you're genuinely broker-agnostic; otherwise it's a layer over Kafka you don't need. What you don't reach for any more: Ribbon, Hystrix, and Zuul are gone, and **Sleuth ended with Boot 2** — tracing is Micrometer Tracing now. Whichever pieces you take, import the `spring-cloud-dependencies` BOM matching your Boot version, because a mismatch surfaces as `NoSuchMethodError` at startup rather than a build failure.",

  "Is any Netflix component still alive in Spring Cloud?":
    "**Eureka, and that's the whole list.** Everything else was removed.\n\nSpring Cloud started as a Spring-friendly wrapper around Netflix's libraries — Eureka, Ribbon, Hystrix, Zuul and Archaius — which is why so much tutorial content from 2016–2019 is Netflix-flavoured. Netflix moved most of them to **maintenance mode** around 2018–2019 having moved on internally, and Spring Cloud built or adopted replacements: **Ribbon → Spring Cloud LoadBalancer**, **Hystrix → Resilience4j**, **Zuul → Spring Cloud Gateway**, **Archaius → Spring Cloud Config**.\n\nThis is the single most stale area of Spring content on the internet, so knowing what's dead is worth as much as knowing what's current — follow a 2018 tutorial and you'll add three dependencies that no longer resolve.",

  "What has Kubernetes replaced in modern Spring Cloud setups?":
    "Kubernetes absorbed the **platform-level** concerns, so you stop solving them in Java. Service discovery becomes **Services plus cluster DNS** instead of Eureka; load balancing becomes kube-proxy instead of Ribbon; edge routing can be an **Ingress or Gateway API** resource instead of a Spring gateway. Configuration and secrets become **ConfigMaps and Secrets** mounted as files or env vars instead of a Config Server. Health checks move to **liveness and readiness probes** hitting `/actuator/health/liveness` and `/readiness`, and restarts and scaling are the platform's job, not yours. What Kubernetes does *not* give you is **application-level resilience** — it can't do a circuit breaker, a fallback, a retry with jitter, or bulkheads around a specific downstream call, so **Resilience4j stays**. That's the honest split: infrastructure concerns go to the platform, in-process behaviour stays in Spring.",

  // ===================== Q117: Inter-service communication =====================
  "What does going async actually cost you?":
    "**Eventual consistency, duplicate and out-of-order delivery, and much harder debugging.** You stop reading a stack trace and start following a message through a broker, and the user sees a state that isn't final yet.\n\nWhat you buy is worth it when the caller **doesn't need the answer to finish its own job**. Placing an order shouldn't fail because the email service is down, so publishing `OrderPlaced` and letting notification, analytics and loyalty consume it independently is both faster for the user and more available — the broker buffers while a consumer redeploys, and a fourth consumer needs no change to the producer.\n\nSynchronous REST is right when you need the result **now, to decide what to do next** — checking inventory before confirming, fetching a price. The cost there is that the order service knows about and waits on every downstream, so its availability becomes the product of theirs.",

  "What are the trade-offs of gRPC vs JSON REST?":
    "gRPC serializes with **Protobuf over HTTP/2** — a compact binary payload, multiplexed connections, and a generated client from a `.proto` file, so the contract is enforced at compile time and a renamed field is a build error instead of a 3am null. It's typically several times faster and smaller than JSON, and it does streaming in both directions, which REST doesn't. The costs: it's **not human-readable**, so `curl` and browser devtools stop helping; browsers can't call it without a grpc-web proxy; and you now have a schema-and-codegen step in every build. The practical split most teams land on is **gRPC for internal service-to-service** calls on hot paths, **JSON REST at the edge** for browsers, mobile clients, and third parties who want to read what they're sending.",

  "How do timeouts and retries affect cascading failures?":
    "A missing timeout is how one slow service takes down the whole estate: if the payment service starts responding in 30 seconds instead of 200ms, every caller's threads sit blocked, its own thread pool fills, *its* callers block, and the failure walks up the call graph until the front door stops responding. **Always set connect and read timeouts** — Spring's `RestTemplate` and `RestClient` have none by default — and make them shorter than your caller's timeout so budgets nest instead of overlapping. Naive retries make it worse rather than better: three retries against a struggling service **triples the load exactly when it's least able to cope**, which is a retry storm. The safe combination is a **short timeout, a small retry count, exponential backoff with jitter, retries only on idempotent operations**, and a **circuit breaker** on top. Once failures are systemic you stop calling altogether and fail fast instead of queuing.",

  // ===================== Q118: Feign =====================
  "Feign resolves through discovery — does that make the call reliable?":
    "No. Discovery caches stay stale for tens of seconds after a pod dies, so **Feign will hand you a dead instance sometimes** — which is why you pair it with Resilience4j rather than assuming resolution means reachability.\n\nThe resolution itself works off a **service id, not a host**. `@FeignClient(name = \"order-service\")` hands the lookup to **Spring Cloud LoadBalancer**, which asks the discovery client (Eureka, Consul, Kubernetes) for the current instance list and picks one, round-robin by default. Your code calls `orderClient.findById(id)` and never knows which pod answered, so scaling from two instances to twenty needs no config change.\n\nPoint it at a fixed address with `url = \"http://localhost:8081\"` and the load balancer is bypassed entirely — useful for a third-party API, or in tests.",

  "How do you configure timeouts and error decoding?":
    "Timeouts come from properties, per client or globally: `spring.cloud.openfeign.client.config.order-service.connectTimeout=2000` and `readTimeout=5000`, with `default` as the key for a fleet-wide setting. **Set them explicitly** — the underlying client's defaults are far too generous for a service call, and an unbounded read timeout is the classic cascading-failure ingredient. For errors, Feign's default `ErrorDecoder` turns any non-2xx into a generic `FeignException`, which loses the downstream's error body. Supply your own `ErrorDecoder` bean to map a 404 to a domain `OrderNotFoundException` and a 409 to a `DuplicateOrderException`, so callers can catch something meaningful. Return a **`RetryableException` from the decoder** only for genuinely transient statuses like 503 — mapping a 400 as retryable means you'll hammer a downstream with a request it will never accept.",

  "What is the difference between Feign and WebClient/RestClient?":
    "Feign is **declarative**: you write an interface, annotate it, and a proxy is generated — the least code and the most readable call sites, which is why it's popular for straightforward service-to-service calls. `WebClient` is **reactive and non-blocking**, so it's the right tool in a WebFlux app or when you need to fan out ten calls concurrently and combine them without burning ten threads. `RestClient` (Boot 3.2+) is the **modern synchronous** client — the fluent `WebClient`-style API without the reactive types, and the intended replacement for `RestTemplate`, which is in maintenance mode. There's now a fourth option worth naming, because it's Spring's own version of Feign's idea: an **HTTP interface client**, where you annotate an interface with `@HttpExchange` and back it with a `RestClient` or `WebClient` adapter — same declarative style, no Spring Cloud dependency. It matters because **Spring Cloud OpenFeign is feature-complete**: it still gets bug fixes and is entirely fine in existing code, but new declarative clients increasingly get written as `@HttpExchange` interfaces. Rough rule: **Feign or `@HttpExchange` when the call is a plain typed RPC and you want it to disappear**, `RestClient` when you need control over the request, `WebClient` when you're reactive or genuinely need concurrency. All of them plug into Spring Cloud LoadBalancer, so discovery isn't a differentiator.",

  // ===================== Q119: Circuit breaker =====================
  "What happens to the very first request after the circuit has been open for a while?":
    "It becomes a **probe**. After the wait duration expires the breaker moves to **half-open** and lets a small, fixed number of calls through (Resilience4j defaults to 10, and you'll usually lower it) while blocking everything else with the fallback. If those probes succeed above the threshold the breaker **closes** and normal traffic resumes; if they fail it goes straight back to **open** and the wait timer restarts. That half-open gate is the whole point of the pattern — without it, the moment the timer expired you'd slam a recovering service with full production load and knock it straight back over. The states are **closed** (calls flow, failures counted over a sliding window), **open** (calls rejected immediately, no downstream traffic at all), and **half-open** (limited probing).",

  "Why doesn't Resilience4j need a thread pool per dependency?":
    "Because it runs the call **on the caller's thread behind a semaphore bulkhead** by default, where Hystrix gave every dependency its own thread pool. You don't pay a thread hop on each call, and you don't lose your `ThreadLocal`s — security context, MDC trace id — crossing into a pool thread.\n\nIt's lighter throughout: no Archaius or RxJava, built on Java 8 functional composition, and the other patterns ship as separate composable modules — retry, rate limiter, bulkhead, time limiter — that you stack in a defined order rather than one monolithic command class. Configuration is plain Boot properties with Actuator and Micrometer integration, so breaker state lands in your existing dashboards.\n\nThread isolation is still there when you want it (`ThreadPoolBulkhead`, usually with a time limiter) — it just isn't a tax on every protected call. **Hystrix itself is end of life**: Netflix stopped development in 2018 and Spring Cloud removed it, so Resilience4j is what Spring Cloud CircuitBreaker wraps today.",

  "How do fallback methods help degrade gracefully?":
    "A fallback turns a **failure into a worse-but-usable answer**, so a broken recommendation service costs you a personalized carousel instead of the whole product page. The fallback method must have the **same signature plus a trailing `Throwable` parameter**, and it should return cached data, a sensible default, an empty list, or a queued \"we'll process this shortly\". Never fabricate data that the caller will treat as authoritative. Know when *not* to fall back: silently returning `PaymentStatus.SUCCESS` because the payment gateway timed out is how you ship free orders. For anything money- or safety-related, the correct degradation is to **fail loudly or defer the work to a queue**, not to guess. Also make sure the fallback can't fail the same way — a fallback that hits the same database as the call that just timed out isn't a fallback.",

  // ===================== Q120: Distributed configuration =====================
  "What actually happens to a bean when a config refresh fires?":
    "`@RefreshScope` wraps the bean in a **proxy holding a lazily-created target**. Posting to `/actuator/refresh` re-fetches the configuration and **evicts the cached target**; the next method call through the proxy rebuilds the bean from scratch, so it re-reads its `@Value` fields and re-runs its constructor and `@PostConstruct`. Nothing is mutated in place and nothing restarts. But that also means **in-flight requests keep using the old instance** and any state the bean was holding is thrown away, so refresh-scoped beans should be effectively stateless. What refresh will *not* change is anything read once at bootstrap: `server.port`, datasource URLs already used to build a pooled `DataSource`, and `@ConfigurationProperties` beans that aren't themselves refresh-scoped. For fleet-wide refresh you either drive it from your deployment tooling or use **Spring Cloud Bus** over Kafka/RabbitMQ so one `/actuator/busrefresh` fans out to every instance.",

  "What secrets management approaches pair with config servers?":
    "The baseline is **never plaintext in Git** — the whole value of a Git-backed config repo is that lots of people can read it. Spring Cloud Config has built-in **encryption**: you configure a symmetric key or keystore and store values as `{cipher}AQBc...`, which the server decrypts before serving. That's better than nothing but the key still lives on the server and rotation is manual. The stronger answer is an external secret store — **HashiCorp Vault** via Spring Cloud Vault, or your cloud's manager (AWS Secrets Manager, GCP Secret Manager). Those give you **audit logs, per-service policies, rotation, and short-lived dynamic credentials** like a database password minted per instance with a TTL. On Kubernetes the common shape is secrets living in Vault or a cloud store, projected into the pod as files or env vars by an operator, with the Config Server handling only non-sensitive properties.",

  // ===================== Q121: Saga pattern =====================
  "What is the difference between choreography and orchestration sagas?":
    "In **choreography** there's no coordinator — each service publishes an event and the next one reacts. Order publishes `OrderCreated`, payment consumes it and publishes `PaymentCompleted`, inventory consumes that, and so on. It's decoupled and there's no extra component, but **nobody owns the flow**: to answer \"why did order 4711 stall?\" you read code in five repos, and cyclic event chains are easy to create by accident. In **orchestration** a single **saga orchestrator** issues commands and tracks state — call payment, wait, call inventory, wait, on failure run compensations in reverse. The flow lives in one readable place with an explicit state machine, which is worth a lot operationally. The trade is a component that must be highly available and can drift toward becoming a god service. Rule of thumb: **choreography for two or three steps, orchestration once it's four or more or the compensation logic gets conditional.**",

  "What has to be true of a compensation for the saga to be safe?":
    "Two things, and neither is optional. Compensations must be **idempotent**, because the saga retries them after a crash and refunding twice is worse than the failure you were recovering from — key the refund on the saga id. And they must be **retried until they succeed**, because a failed compensation leaves real money or stock in limbo. Anything that exhausts its retries goes to a dead-letter queue with an alert, never a swallowed exception.\n\nMechanically a compensation is a **semantic undo that runs as a new forward transaction**, not a database rewind. You can't un-send an email, so you send a cancellation. You can't un-charge, so you issue a refund that appears as its own ledger entry.\n\nAnd the intermediate states are **visible to users** while all this plays out, so design for it — the order sits in `PENDING`, not `CONFIRMED`.",

  "Why is 2PC often avoided in microservices?":
    "Because it trades **availability for consistency**, and in a distributed system that trade goes badly. Every participant takes locks and holds them through the prepare phase until the coordinator says commit — so a slow service holds database rows locked across a network round trip, and throughput collapses under contention. Worse, the **coordinator is a single point of failure**. If it dies after prepare, participants are stuck holding locks with no idea whether to commit or abort, and a human ends up resolving in-doubt transactions by hand. It also demands **XA support from every participant**, which rules out most of what microservices actually talk to. Kafka, REST APIs, Stripe, and plenty of NoSQL stores simply have no two-phase commit to enrol in. Sagas accept **eventual consistency** in exchange for keeping each service's transaction local, short, and independently available, which is the right trade for business processes that already tolerate a pending state.",

  // ===================== Q122: Logging and tracing =====================
  "What is a correlation/trace ID, and how is it propagated?":
    "A **trace id** is one identifier generated at the edge and carried through every service, queue, and log line for a single logical request, so you can grep one id and see the whole journey. Inside a trace, each unit of work gets a **span id** with a parent pointer. That's what lets a UI draw the waterfall showing that 900ms of a 1s request was one slow database call. Propagation happens over **HTTP headers** — the W3C standard `traceparent`, or the older `X-B3-TraceId`/`X-B3-SpanId` from B3 — injected automatically by instrumented clients (`RestClient`, `WebClient`, Feign) and read by instrumented servers. Locally it rides in **MDC**, which is why `%X{traceId}` works in your logback pattern. The two places it silently breaks: **async boundaries** (a raw `new Thread` or an uninstrumented `@Async` executor loses the context) and **message brokers**. There the id has to be written into message headers by the producer and restored by the consumer.",

  "You're upgrading a Boot 2 app with Sleuth to Boot 3. What changes?":
    "There's no `spring-cloud-starter-sleuth` to upgrade to. **Sleuth was discontinued for Boot 3 and its core donated to Micrometer**, so you swap the dependency for `micrometer-tracing-bridge-brave` (or `-otel`) plus a reporter like `zipkin-reporter-brave`.\n\nThe concepts survive intact — trace ids, spans, MDC population, automatic instrumentation of web clients and servers — so most of the rest is property renames. `spring.sleuth.sampler.probability` becomes `management.tracing.sampling.probability`.\n\nThe reason for the move is that tracing now sits alongside metrics under the same **Micrometer Observation API**: instrument once with `@Observed` or an `Observation` and you get a metric *and* a span out of it. Sleuth being Boot 2 only is the part most online material still gets wrong.",

  "What is the difference between logs, metrics, and traces?":
    "**Logs** are discrete events with full context — \"payment failed for order 4711, card declined\" — so they tell you *what exactly* happened, but they're expensive to store and awkward to aggregate. **Metrics** are numeric time series with low-cardinality tags (request count, p99 latency, error rate, JVM heap). They're cheap, aggregate perfectly, and are what you **alert** on. But a metric can never tell you *which* order broke. **Traces** show one request's path across services with timing per hop, which is what answers *where* the latency or failure is in a distributed call graph. The workflow ties them together: a **metric** alert fires on error rate, a **trace** shows which service and which call is failing, and the **logs** for that trace id tell you why. Any one of the three alone leaves you guessing — that's why observability means all three, correlated by trace id.",

  // ===================== Q123: Message-driven architecture =====================
  "A new team wants to consume events from six months ago. Which broker survives that?":
    "**Kafka**, because it's a durable partitioned log rather than a queue. Consumers track their own offset against retained data, so a new consumer group can start from the beginning and rebuild its state from history. In RabbitMQ a message is gone once acked — there's nothing left to replay.\n\nThat retention is the real dividing line, and with per-partition ordering and very high throughput it's what makes Kafka right for event sourcing, change-data-capture, stream processing, and any pipeline where three teams consume the same stream independently.\n\n**RabbitMQ** wins when the message is a **task for one worker**: flexible routing (direct, topic, fanout), per-message acknowledgement, priorities, TTLs, delayed delivery, and built-in dead-letter queues. It gives you per-message control and easier operations, where Kafka gives you replay and scale but is a heavier thing to run.",

  "What does the listener container do for you beyond calling your method?":
    "More than people expect, and all of it as configuration rather than code: polling, deserialization into your DTO, **acking only if your method returns normally**, a `DefaultErrorHandler` with backoff that routes to a **dead-letter topic** after N failures, concurrency by container count, manual ack modes, and Micrometer metrics.\n\nThat's the receiving half of the shape both projects share — a **template for sending** (`KafkaTemplate`, `RabbitTemplate`) and an **annotated listener** for receiving (`@KafkaListener(topics = \"orders\")`, `@RabbitListener(queues = \"orders\")`).\n\n**Spring Cloud Stream** goes one level further with a binder abstraction: declare `@Bean Function<Order, Receipt>` and bind it to Kafka or RabbitMQ in properties, so switching brokers is a dependency swap. The one thing none of it hides is **duplicate delivery** — the container redelivers after a failure, so your handler still has to be idempotent.",

  "What is at-least-once vs exactly-once delivery?":
    "**At-least-once** means the broker retries until it gets an ack, so a message can arrive twice — the consumer crashing *after* processing but *before* committing the offset is enough. It's the default in both Kafka and RabbitMQ and it's what almost everyone actually runs. **At-most-once** is the opposite failure: ack first, process after, so a crash loses the message — acceptable for metrics, never for orders. **Exactly-once** is genuinely available in Kafka within its own boundary, via idempotent producers plus transactions that commit the produced records and the consumer offsets atomically (`read-process-write`). But it costs throughput and **stops at the edge of Kafka**. The moment your handler calls Stripe or writes to Postgres outside that transaction, the guarantee is gone. So the practical rule: **assume at-least-once and make the consumer idempotent** — dedupe on a message id or business key, or make the write naturally idempotent with an upsert. Effectively-once through idempotency is simpler and more portable than chasing exactly-once delivery.",

  // ===================== Q124: Idempotency =====================
  "Can you keep idempotency keys in Redis?":
    "Not as the source of truth. **A Redis check is a cache, not a constraint** — an eviction, a failover, or the gap between checking and writing turns into a double charge. The guarantee comes from committing the key and the business change **atomically**, so the key belongs in your primary transactional database behind a **unique index**.\n\nThe key itself is a client-generated token identifying **one logical operation**, not one HTTP attempt: generate it when the user clicks Pay and reuse that value for every retry of that click. The row holds the key, the response status and body, and a timestamp so a job can purge after a retention window — a day or two, long enough to outlive any sane retry.\n\nStore a **hash of the request body** alongside it. If the same key arrives with different parameters that's a client bug, and returning **422** beats silently replaying the wrong response. Stripe's API is the reference implementation if you want to point at prior art.",

  "How do retries interact with non-idempotent operations?":
    "Badly, and the failure is invisible: a **timeout doesn't tell you whether the request was processed**. The service may have charged the card and then died before the response reached you, so a retry charges twice. This isn't rare — it's the normal outcome of a network blip on a slow endpoint. It compounds because retries stack at every layer (the client's HTTP library, your Feign config, the load balancer, the user hitting the button again), so one \"retry once\" policy per layer multiplies into six charges. HTTP semantics help you reason about it: `GET`, `PUT`, and `DELETE` are defined as idempotent and safe to retry automatically, while **`POST` is not**. Never turn on blind retries for `POST` without an idempotency key. Where you can't make an operation idempotent, you have two options. **Don't retry — surface the uncertainty** for a human or reconciliation job to resolve, or convert it into a queued command with a deduplicated id so exactly one worker executes it.",
};
