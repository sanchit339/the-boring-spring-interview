/**
 * Answer bank for followup questions — Testing category (Q125–Q132).
 * Keyed by the exact followup `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - BAD/GOOD code only where it proves understanding
 */
export const followupAnswersTesting: Record<string, string> = {

  // ===================== Q125: Unit vs integration testing =====================
  "Where do you draw the line in a Spring Boot app (service unit test vs `@SpringBootTest`)?":
    "Draw it at **whether the correctness lives in your code or in the wiring**. Pricing rules, validation, state transitions, mapping — that's a plain JUnit test with mocked collaborators and no Spring at all, because Spring contributes nothing to whether the arithmetic is right. The moment the thing you're checking is framework behaviour, you need the real context: a `@Query` that has to be valid JPQL, a `@Transactional` boundary that must genuinely roll back, JSON that must deserialize into your DTO, a security rule that must reject an anonymous caller. A mock can't fail any of those, because a mocked repository returns exactly what you told it to no matter what the real query does. The practical shape is a fast unit test for every branch of the logic, plus one slice or full-context test proving the layer is wired correctly.",

  "What is the testing pyramid, and why prefer more unit tests?":
    "The pyramid describes the **proportions**: a wide base of unit tests, a narrower band of integration and slice tests, and a thin cap of end-to-end tests. The reasoning is cost and diagnostic value. A unit test runs in milliseconds and names the broken class; an end-to-end test takes minutes and tells you only that *something* along a five-service path failed. Invert the shape — the \"ice cream cone\" — and you get a suite that takes 25 minutes, so nobody runs it before pushing, and that fails intermittently for reasons unrelated to the change. Slow feedback is the real cost: a test suite you only see results from after lunch has stopped being a development tool. The caveat is that the pyramid is about **proportions, not permission to skip the top** — a base of unit tests with everything mocked can be entirely green while the application refuses to start.",

  "When is an end-to-end test worth the cost?":
    "For the handful of **journeys where being wrong is unacceptable** — checkout and payment, signup and login, anything that moves money or grants access. Those deserve a real test through the real stack because the failure mode is measured in revenue or a security incident, not in developer inconvenience. Everything else is better covered lower down. The cost isn't only runtime: end-to-end tests are the **flakiest** things you own, because they depend on timing, test data, and other people's environments, and a suite that cries wolf gets ignored or retried until green, which is worse than not having it. Keep them few, keep them on the critical paths, make them deterministic with seeded data, and treat a flaky one as a bug to fix rather than a test to rerun. A useful rule of thumb: if you can't name the business consequence of the path breaking, it doesn't need an end-to-end test.",

  // ===================== Q126: JUnit and Mockito =====================
  "What is the difference between JUnit 4 and JUnit 5 annotations?":
    "Different package first — **`org.junit.jupiter.api`** rather than `org.junit`, which is why a stray JUnit 4 import means your test silently isn't collected. The renames: `@Before` becomes **`@BeforeEach`**, `@After` becomes **`@AfterEach`**, `@BeforeClass`/`@AfterClass` become **`@BeforeAll`/`@AfterAll`**, and `@Ignore` becomes **`@Disabled`**. The bigger structural change is the extension model: `@RunWith` allowed exactly one runner, so you had to choose between Spring and Mockito, while **`@ExtendWith` composes** and you can have both. JUnit 4's `@Rule` and `@ClassRule` are gone, replaced by the same extension mechanism. Assertions moved too — `@Test(expected = X.class)` and `@Test(timeout = ...)` became **`assertThrows`** and **`assertTimeout`**, which is a real improvement because you assert on the exception's message and type rather than just its class. Boot ships JUnit 5 in `spring-boot-starter-test`; running old JUnit 4 tests needs the vintage engine added explicitly.",

  "How do you structure Arrange-Act-Assert in a clean test?":
    "Three visually separated blocks. **Arrange** builds the inputs and stubs the collaborators. **Act** is the single call to the method under test — one line, and that constraint is the point. **Assert** checks the outcome. Keeping Act to one call is what makes a failure diagnosable: if a test calls three methods and goes red, you don't know which one broke. Two rules keep it honest. Assert on the **outcome, not the choreography** — that the returned order is `CONFIRMED` and that `paymentGateway.charge()` was called once, rather than that six internal methods ran in sequence, which locks the test to today's implementation and makes every refactor a test rewrite. And keep the **arrange block small**; if it takes fifteen lines of stubbing to reach the method, that's the class telling you it has too many collaborators. The test is a design signal, not just a check.",

  "When do you use `@ExtendWith(MockitoExtension.class)`?":
    "Whenever you want Mockito's annotations to actually do something in a JUnit 5 test — it's what makes **`@Mock`, `@Spy`, `@InjectMocks`, and `@Captor`** get initialised, and it replaces the old `MockitoAnnotations.openMocks(this)` in a `@BeforeEach`. It also turns on **strict stubs**, so an unused `when(...)` fails the test with `UnnecessaryStubbingException` and unfinished stubbing is caught rather than silently ignored. You don't need it if you're building mocks manually with `Mockito.mock(OrderRepository.class)` and calling the constructor yourself — which is a perfectly good style, and arguably a more robust one, since a constructor change becomes a compile error instead of a null field. What you must not do is combine it with `@SpringBootTest`: inside a Spring context the right tools are `@MockitoBean` and `@MockitoSpyBean`, which replace beans in the context, where `@Mock` only exists inside your test class.",

  // ===================== Q127: @SpringBootTest and slices =====================
  "What does each slice load into the context?":
    "**`@WebMvcTest`** loads the MVC layer only: your `@Controller` and `@RestController` beans, `@ControllerAdvice`, `WebMvcConfigurer`, `HttpMessageConverter`s, registered filters, and an auto-configured `MockMvc`. It deliberately excludes `@Service`, `@Repository`, and `@Component`, so every collaborator has to arrive as a `@MockitoBean`. **`@DataJpaTest`** loads the persistence layer: `@Entity` classes, Spring Data repositories, `EntityManager`, `TestEntityManager`, and a `DataSource` — by default replaced with an embedded database, which you turn off with `@AutoConfigureTestDatabase(replace = NONE)` when you're using Testcontainers. It's transactional and rolls back per test. **`@JsonTest`** loads just Jackson and your serializers, for asserting the exact JSON shape a DTO produces. **`@RestClientTest`** loads your HTTP client plus `MockRestServiceServer`. **`@SpringBootTest`** is the opposite of all of them — it loads everything.",

  "Why are slice tests faster than full `@SpringBootTest`?":
    "Two reasons, and the second matters more than people expect. First, a slice **instantiates far fewer beans** — `@WebMvcTest` skips your entire service and persistence layer, and never builds a connection pool or a Hibernate `SessionFactory`, which is where most startup time goes. Second, **context caching**: Spring caches one application context per unique test configuration and reuses it across every test class that matches. Twenty `@WebMvcTest` classes with the same mocks share one context, so you pay the startup once for the whole group. That also explains the usual disappointment — sprinkling different combinations of `@MockitoBean` types across classes creates a **new cache key each time**, so you quietly end up building six contexts instead of one and the suite gets slower even though every test is a slice. Keep mock sets consistent, and reserve `@DirtiesContext` for cases that genuinely demand it, since it evicts a context everything after has to rebuild.",

  "When must you use a full application context?":
    "When the behaviour under test **is** the wiring, and no slice contains all the pieces. A `@Transactional` rollback that has to work through the real AOP proxy. A `@Scheduled` or `@Async` method that needs the actual task executor. Security filter-chain ordering, where the question is which filter runs first. A `@ConfigurationProperties` binding you want to prove against real YAML. Anything spanning more than one layer — a request arriving over HTTP and landing as a committed row in Postgres. Add **`webEnvironment = RANDOM_PORT`** and use `TestRestTemplate` when you want a genuine HTTP round trip rather than `MockMvc`, which is the honest way to test filters, the embedded server, and real serialization. The discipline is to keep the count small and deliberate: these are the tests that turn a 40-second suite into a 20-minute one, so each should earn its place by testing something no slice can reach.",

  // ===================== Q128: Mockito annotations =====================
  "When would you use a spy instead of a mock?":
    "When you need **most of a real object's behaviour** and want to override exactly one part of it — typically a legacy class doing five things where four should stay real and the fifth hits the network or reads the clock. A `@Spy` wraps a real instance and runs real methods unless you stub them, where a `@Mock` fakes everything and returns null until told otherwise. It's genuinely useful for code you can't refactor today. But treat it as a smell on code you own: if you need half a class real and half faked, that's usually the class telling you it has two responsibilities that want separating, and the cleaner fix is to extract the awkward part behind an interface you can mock normally. There's also a practical hazard — on a spy you should stub with **`doReturn(...).when(spy).method()`**, because `when(spy.method())` calls the real method while you're setting up the stub.",

  "What is the difference between `when().thenReturn()` and `doReturn().when()`?":
    "`when(mock.getOrder()).thenReturn(order)` has to **call the method** to record the stub — which is harmless on a mock, where the call does nothing, and dangerous on a **spy**, where it executes the real implementation. Stub a spy that way and the real method runs during setup, hitting the database or throwing before the stub is even registered. `doReturn(order).when(spy).getOrder()` never invokes the real method, so it's the correct form on spies. There's a second case where `do...` is mandatory: **void methods**. You can't write `when(mock.send(msg))` when `send` returns nothing, so you use `doNothing().when(mock).send(msg)` or `doThrow(new MailException()).when(mock).send(msg)`. The trade-off is type safety — `when().thenReturn()` is checked by the compiler, while `doReturn()` takes an `Object` and a type mismatch only shows up at runtime. So: `when().thenReturn()` by default on mocks, `doReturn().when()` for spies and void methods.",

  "How do you verify interactions (`verify`, `times`, `never`)?":
    "`verify(paymentGateway).charge(order)` asserts the call happened exactly once — `times(1)` is the default and doesn't need writing. For anything else you're explicit: `verify(gateway, times(3)).charge(any())`, `verify(gateway, never()).refund(any())`, plus `atLeastOnce()`, `atMost(2)`, and `only()`. `verifyNoInteractions(mock)` asserts the collaborator was never touched at all, and `verifyNoMoreInteractions(mock)` asserts nothing happened beyond what you already verified. Use **`ArgumentCaptor`** when you care about *what* was passed rather than just that it was: capture the `Order` handed to `charge()` and assert on its amount and currency. The judgement call is **how much to verify**. Verification asserts on implementation, so over-verifying makes every refactor break tests that were never really about behaviour. Verify the interactions that are the point of the method — the charge actually happened, the email was not sent twice — and let the return value carry the rest.",

  // ===================== Q129: Mocking REST calls =====================
  "How do you mock a Feign client vs WebClient?":
    "A **Feign client is an interface**, which makes it the easy case: `@Mock InventoryClient` in a unit test, or `@MockitoBean InventoryClient` inside a Spring context, and you stub the method like any other bean. Nothing HTTP is involved. **`WebClient` is a concrete class with a fluent chain**, so mocking it directly means stubbing `get()` then `uri()` then `retrieve()` then `bodyToMono()`, each returning the next mock — brittle, unreadable, and it breaks the moment someone reorders the chain. Don't do it. Point the `WebClient` at a **stub HTTP server** instead: WireMock or OkHttp's `MockWebServer`, injecting the base URL from the test. That's less code and it actually exercises serialization and status handling. The same reasoning applies to `RestClient` and `RestTemplate`, where `MockRestServiceServer` (via `@RestClientTest`) is the built-in option. The general rule: mock interfaces you own, stub the transport for concrete clients.",

  "What is WireMock, and when do you prefer it over pure Mockito?":
    "WireMock is a **stub HTTP server** you run inside the test. You declare what it should return for a given request — `stubFor(get(\"/api/stock/SKU-1\").willReturn(okJson(...)))` — and your client makes a genuine HTTP call to it. Prefer it whenever the thing under test **is the HTTP interaction**: URL and query-parameter construction, headers and auth, status-code handling, timeouts, retries, and JSON deserialization into your DTO. A Mockito stub can't check any of those, because it replaces the client instead of exercising it — which is exactly how a field-name mismatch between `{\"quantity\":0}` and a DTO expecting `available` survives a fully green suite. Use plain Mockito when you're testing **your own logic** and the client is incidental, since it's much faster. Rough split: Mockito for service-layer branching, WireMock for the client class and its failure paths.",

  "How do you test timeout and error handling paths?":
    "Make the stub server misbehave on purpose. WireMock can return a **503**, add a **fixed delay longer than your read timeout**, drop the connection mid-response with a fault, or return valid JSON in the wrong shape — and each of those exercises a different branch you otherwise never run. That's the point: your `ErrorDecoder`, retry policy, circuit breaker, and fallback exist entirely for these paths, so a suite that only tests the happy path leaves all of them unverified. For the **circuit breaker** specifically, drive enough failing calls to cross `minimumNumberOfCalls` and the failure-rate threshold, then assert the next call is rejected without reaching the server — WireMock's request count proves no traffic went out. Set **short timeouts in the test profile** (200ms rather than 5s) so failure tests stay fast. And assert on the **mapped domain exception**, not the raw `FeignException`, since translating downstream failures into something your callers can act on is what you're actually testing.",

  // ===================== Q130: MockMvc =====================
  "How do you assert JSON paths and status codes with MockMvc?":
    "Chain `andExpect(...)` calls off `perform(...)`. Status comes from `status().isOk()`, `status().isNotFound()`, `status().isCreated()`, and body assertions come from **`jsonPath`** — `jsonPath(\"$.status\").value(\"CONFIRMED\")` for a field, `jsonPath(\"$.items\", hasSize(3))` for a collection, `jsonPath(\"$.items[0].sku\").value(\"SKU-1\")` for an element, and `jsonPath(\"$.internalCost\").doesNotExist()` to prove a field is **not** leaking into the response. That last one is worth using deliberately, because accidentally serializing an internal field is a real bug that positive assertions never catch. Add `header().string(\"Location\", ...)` for created resources and `content().contentType(APPLICATION_JSON)` for the content type. Use `andDo(print())` while debugging to dump the whole request and response. For a large payload, `@JsonTest` with `JacksonTester` and a golden JSON file reads better than twenty chained `jsonPath` assertions.",

  "What is the difference between standalone setup and full Spring context?":
    "`MockMvcBuilders.standaloneSetup(new OrderController(mockService))` builds a minimal MVC environment around **one controller instance you construct yourself** — no application context, so it starts almost instantly. The cost is that everything configured *around* the controller is missing: your `@ControllerAdvice` isn't registered, custom `HttpMessageConverter`s and argument resolvers aren't applied, `@Valid` may behave differently, and there's no security filter chain at all. That means a test asserting the 404 your exception handler returns will **pass under `@WebMvcTest` and fail standalone**, and a test of a secured endpoint proves nothing because no security is running. `@WebMvcTest` loads a real, if narrow, application context with all of that wired as it is in production, and it benefits from context caching so the startup cost is paid once across many test classes. Default to `@WebMvcTest`; keep standalone for a controller with genuinely no framework interaction.",

  "How do you test secured endpoints with MockMvc?":
    "Add **`spring-security-test`** and annotate the test with **`@WithMockUser(roles = \"ADMIN\")`**, which puts an authenticated principal in the security context for that request. `@WithAnonymousUser` covers the unauthenticated case, and `@WithUserDetails(\"alice\")` loads a real user through your `UserDetailsService` when the authorities matter. For per-request control use the request post-processors: `.with(user(\"alice\").roles(\"ADMIN\"))`, `.with(jwt().authorities(...))` for a resource server, and **`.with(csrf())`** — which you'll need on every POST, PUT, and DELETE, because without a CSRF token Spring Security returns **403 and the test failure looks like an authorization bug** when it's really a missing token. Test both directions: that an authorized user succeeds *and* that an anonymous or wrong-role caller is rejected. Only the negative test proves the rule is actually enforced. Note that `@WebMvcTest` does apply your security configuration, so these tests are meaningful in the slice.",

  // ===================== Q131: Testcontainers =====================
  "How do you spin up Postgres/Kafka in integration tests?":
    "Declare the container as a **static field** and let Boot wire it: `@Container @ServiceConnection static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>(\"postgres:16-alpine\")` on a class annotated `@SpringBootTest @Testcontainers`. **`@ServiceConnection`** (Boot 3.1+) reads the container's host, port, and credentials and points `spring.datasource.*` at it automatically — before that you wrote a `@DynamicPropertySource` method by hand, which still works and is what you'll see in older code. Kafka is the identical pattern with `KafkaContainer`, and Boot sets `spring.kafka.bootstrap-servers`. **Static matters**: a non-static field starts and stops a fresh database per test method, turning a fast suite into a slow one. For a container shared across many test classes, use a singleton — a static field on an abstract base class or a holder started once in a static initialiser — so the whole suite pays a single startup.",

  "What are the trade-offs vs H2 in-memory databases?":
    "H2 is **faster to start and needs no Docker**, which is its entire case, and it's fine for a repository test doing simple CRUD. What you're trading away is fidelity: H2 in Postgres-compatibility mode still has no `jsonb`, no real `ON CONFLICT ... DO UPDATE`, different behaviour on partial indexes and window-function edge cases, and different locking and constraint-violation semantics. So a native query can fail to parse in production while your tests are green, and Flyway migrations written for Postgres often can't even run against H2, which means **your migrations go untested** — and a broken migration takes the whole deploy down. Testcontainers costs you Docker as a hard dependency on every machine and in CI, plus seconds of startup per container. The reasonable split most teams land on: Testcontainers for anything touching real SQL, migrations, or database-specific features, and H2 only where you've decided the fidelity genuinely doesn't matter.",

  "How do you reuse containers across tests for speed?":
    "Three levels. A **static `@Container` field** reuses one container across every test method in that class, which is the baseline and the single biggest win over a non-static field. A **singleton container** — a static field on an abstract base class that all your integration tests extend, started once and never stopped, letting Ryuk clean up at JVM exit — shares one instance across the whole suite. Then there's **reuse mode**: `withReuse(true)` on the container plus `testcontainers.reuse.enable=true` in `~/.testcontainers.properties` keeps the container **alive after the JVM exits**, so the next run attaches to the one that's already up and skips startup entirely. That's a big local-development win, but it deliberately does **not** work in CI unless enabled there, and you shouldn't enable it: a reused container carries state between runs, which is exactly the non-determinism you don't want in a build. The matching concern is data — with a shared container, clean up between tests via transaction rollback or a truncate script, or tests start leaking into each other.",

  // ===================== Q132: Test data setup and teardown =====================
  "What does `@Transactional` on a test class do for rollback?":
    "Spring starts a transaction before each test method and **rolls it back when the method ends**, so anything the test wrote disappears and the next test sees a clean database with no cleanup code. `@DataJpaTest` includes it already; on `@SpringBootTest` you add it yourself. Override with `@Commit` (or the older `@Rollback(false)`) on the rare test that needs the data to persist. Two traps worth knowing. First, the rollback **hides flush-time failures**: because the transaction never commits, a constraint violation or a mapping error that would fire on commit can go unnoticed — call `entityManager.flush()` explicitly when you want the database to actually validate the write. Second, it **only covers the test thread**. Code that commits on another thread — a `@SpringBootTest(webEnvironment = RANDOM_PORT)` request handled by the server, an `@Async` method, a `REQUIRES_NEW` propagation — commits for real and survives the test, so those need explicit cleanup.",

  "How do you use `@Sql` scripts or `@BeforeEach` fixtures?":
    "**`@Sql(\"/fixtures/orders.sql\")`** runs a script before the test method, at class or method level, and `@Sql(scripts = \"/cleanup.sql\", executionPhase = AFTER_TEST_METHOD)` runs one after. Use it for **bulk reference data** — a currency table, a hundred rows of fixture — or for states that are awkward to build through the entity model, like a legacy row with a null column your entity doesn't allow. Use **`@BeforeEach`** with plain Java construction when the setup is small and the shape of the object matters to the reader; a test whose data lives in another file is harder to follow, and that readability cost is real. Two practical notes: keep fixture scripts **idempotent or transactional** so a rollback genuinely restores the starting state, and prefer builders or object mothers (`anOrder().withStatus(PENDING).build()`) over copy-pasted setup blocks, so adding a required field doesn't mean editing forty tests.",

  "When is `@DirtiesContext` necessary, and why is it expensive?":
    "It's necessary when a test **mutates shared singleton state** that would leak into later tests — reconfiguring a bean at runtime, poisoning an in-memory cache or registry, mutating a static, or shutting down something in the context. Marking the test with `@DirtiesContext` tells Spring the cached context is no longer trustworthy, so it closes it and builds a fresh one. The expense comes from **context caching**, which is the main reason a Spring test suite is tolerable at all: contexts are cached by configuration and reused across every matching test class. Evicting one means the next class that would have reused it pays full startup again — connection pools, Hibernate, the lot — so a single careless `@DirtiesContext` can add minutes to a suite, and a few scattered through it can dominate the runtime. Reach for the cheaper fix first: reset the offending state in an `@AfterEach`, or don't hold mutable state in a singleton. `@DirtiesContext` is the correct answer when a test genuinely can't undo what it did.",
};
