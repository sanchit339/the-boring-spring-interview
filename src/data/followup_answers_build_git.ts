/**
 * Answer bank for follow-up questions — Build Tools, Git & DevOps (Q133–Q142).
 * Keyed by the exact follow-up `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - Kept deliberately tighter than the deeper Spring categories
 */
export const followupAnswersBuildGit: Record<string, string> = {

  // ===================== Q133: Maven vs Gradle =====================
  "What are pros of Gradle's incremental builds and Kotlin DSL?":
    "**Incremental builds** mean Gradle tracks the inputs and outputs of every task and skips anything whose inputs haven't changed, so a rebuild after a one-file edit recompiles that file rather than the module. Add the **build cache** and the long-running **daemon** and you get large savings on multi-module projects — Maven re-runs phases far more eagerly. The **Kotlin DSL** (`build.gradle.kts`) gives you a typed build script, so your IDE autocompletes task names and configuration blocks and a typo is a compile error instead of a runtime failure halfway through the build. The honest caveat: on a single Spring Boot service that builds in 30 seconds you won't notice any of it, and a Groovy or Kotlin build script can grow into code that needs its own review.",

  "Why do many Spring Boot projects still default to Maven?":
    "Because the rigidity is the feature. Maven's XML has **one obvious way to do things**, so any developer opening a `pom.xml` immediately knows the build, where a Gradle script can encode arbitrary logic that becomes its own thing to maintain. **start.spring.io defaults to Maven**, most Spring documentation and tutorials show Maven, and `spring-boot-starter-parent` handles version management with no configuration at all. Tooling support is also uniformly excellent because the format is completely predictable. For a typical service the build takes seconds either way, so the tiebreaker is familiarity rather than speed.",

  "How do you run a Boot app with each tool?":
    "`./mvnw spring-boot:run` or `./gradlew bootRun` — both compile and start the app with the embedded server, and both support live restarts when DevTools is on the classpath. To build the deployable artifact instead: `./mvnw clean package` or `./gradlew build`, each producing an executable fat JAR you run with `java -jar target/order-service-1.0.0.jar`. Pass Spring arguments after the jar name (`--server.port=9090`) and JVM arguments before it (`-Xmx512m`). Always use the **wrapper** scripts (`mvnw`/`gradlew`) rather than a locally installed `mvn` or `gradle`, so CI and every developer's laptop build with the exact same tool version.",

  // ===================== Q134: Maven lifecycle =====================
  "What is the difference between `package` and `install`?":
    "`package` compiles, tests, and builds the JAR into **`target/`**, where only this project can see it. `install` does all of that and then **copies the JAR into your local `~/.m2` repository**, which makes it resolvable as a dependency by other projects on the same machine. So you need `install` when you're building a shared library and want to test it against a consumer locally before publishing. For a deployable service, `package` is all you need — and `deploy` is the step beyond both, uploading to a remote or company repository like Nexus or Artifactory for everyone else to use.",

  "How do plugins bind to lifecycle phases?":
    "A phase on its own does nothing — it's an empty slot, and **plugin goals bind to it** to do the actual work. Packaging type supplies the default bindings: for a `jar` project, `maven-compiler-plugin:compile` binds to `compile`, `maven-surefire-plugin:test` binds to `test`, and `maven-jar-plugin:jar` binds to `package`. You add your own in the `<build><plugins>` section with an `<execution>` naming a `<phase>` and a `<goal>`. That's exactly how Spring Boot works: `spring-boot-maven-plugin:repackage` binds to `package`, taking the plain JAR Maven just built and rewriting it into an executable fat JAR. It's also why `mvn package` on a Boot project gives you something `java -jar` can run, while a plain Java project doesn't.",

  "What does `mvn clean verify` run?":
    "`clean` deletes `target/` so nothing stale survives, then `verify` runs **every phase up to and including verify**: validate, compile, test, package, integration-test, and verify. So you get a full compile, unit tests via Surefire, the packaged JAR, and integration tests via Failsafe. It's the standard CI command precisely because it's the most complete check that **doesn't** write anything to your local `~/.m2` — that's `install`, which is undesirable in CI because it makes builds depend on machine state. The Surefire/Failsafe split is why it matters: Surefire runs `*Test` classes and fails the build immediately, while Failsafe runs `*IT` classes and defers failure to `verify` so teardown still happens.",

  // ===================== Q135: Dependency management =====================
  "What is nearest-wins and dependency mediation?":
    "**Dependency mediation** is Maven's rule for picking one version when the tree asks for several, and the rule is **nearest-wins**: the version at the shallowest depth from your POM. A jar you declare directly is at depth 1 and always beats anything transitive. When two candidates sit at the same depth, the one **declared first** in your POM wins. Note that this is depth-based, not newest-based — Maven will happily choose an older version because it sits one level closer, which is a common source of surprise. Gradle behaves differently and picks the **highest** version by default. Either way, `mvn dependency:tree` shows you the winner and marks the losers as omitted for conflict.",

  "How does `dependencyManagement` / BOM help (e.g., Spring Boot parent)?":
    "`<dependencyManagement>` declares **which version to use if this dependency appears**, without adding it to your build. It overrides nearest-wins entirely, so you get one authoritative version regardless of tree depth. A **BOM** is a POM that contains nothing but such a block, published for you to import. `spring-boot-starter-parent` carries one pinning hundreds of libraries — Jackson, Hibernate, Tomcat, SLF4J — to versions Spring actually tested together, which is why you declare Spring dependencies **without a `<version>` tag**. If you already have a corporate parent POM, import `spring-boot-dependencies` with `<type>pom</type><scope>import</scope>` to get the same version management without using up the parent slot.",

  "How do you find and exclude transitive dependencies?":
    "Find them with **`mvn dependency:tree`**, and narrow a large tree with `-Dincludes=com.fasterxml.jackson.core` to follow one library. `mvn dependency:analyze` additionally flags dependencies you use but haven't declared, and ones you declared but never use. To remove a specific transitive jar, add an `<exclusions>` block to the dependency that drags it in, naming just the `groupId` and `artifactId` — an exclusion takes no `<version>`. The usual reasons are a duplicate logging implementation (two SLF4J bindings on the classpath log a warning and pick one arbitrarily) or a jar with a known CVE. Pinning the version in `<dependencyManagement>` is often the better fix, since excluding something the library genuinely needs turns into `ClassNotFoundException` at runtime.",

  // ===================== Q136: merge vs rebase =====================
  "When is rebase dangerous on shared branches?":
    "The moment anyone else has pulled the commits you're about to rebase. Rebasing **rewrites history** — each replayed commit is a new object with a new hash — so after a force-push the remote holds commits your colleague has never seen, while they hold commits that no longer exist upstream. Their next pull merges the two versions together and you end up with every change duplicated, or someone reverts the wrong copy. The rule is simple: **rebase only commits that exist nowhere but your machine.** Rebasing your own unpushed feature branch onto `main` is routine and safe; rebasing `main` itself, or a branch two people are working on, is how you lose an afternoon. If you must force-push a shared branch, use `--force-with-lease`, which refuses when the remote has moved since you last fetched.",

  "What does a linear history buy you?":
    "A `git log` you can actually read, where every commit is a real state the project passed through in order, rather than a lattice of merge commits recording who synced when. That makes **`git bisect`** genuinely effective — it can binary-search commits to find the one that introduced a bug and converge quickly, where a history dense with merges gives it far messier ground. `git revert` is also simpler, since reverting a normal commit is unambiguous while reverting a merge commit requires telling Git which parent you meant. The counter-argument is that linear history is a **partial fiction**: the branches really did happen in parallel, and rebasing discards that. Most teams settle on rebasing feature branches to stay current, then squashing or merging the PR so `main` records one clean entry per feature.",

  "How do you resolve conflicts in each workflow?":
    "In a **merge** you hit conflicts once. Git stops, you fix every conflicted file, `git add` them, and `git commit` completes the merge with a single resolution. In a **rebase** Git replays your commits one at a time, so it stops **at each commit that conflicts** — a five-commit branch can present you the same conflict repeatedly as each commit is applied. The cycle is: fix, `git add`, `git rebase --continue`. That repetition is the main practical argument for merging a long branch rather than rebasing it. Both offer a clean exit: `git merge --abort` and `git rebase --abort` return you exactly to where you started. `git rerere` (reuse recorded resolution) can replay resolutions you've already made, which takes some of the pain out of a long rebase.",

  // ===================== Q137: Merge conflicts =====================
  "What markers appear in conflicted files?":
    "Three. `<<<<<<< HEAD` opens the block holding **your current side**, `=======` divides the two versions, and `>>>>>>> branch-name` closes the block holding the **incoming** change. Everything between them is both candidate versions written into the file, which is why the file won't compile until you fix it. The catch is that **HEAD means something different during a rebase**: because Git is replaying your commits onto the target branch, HEAD is the *target*, and the bottom block is your own work — the reverse of what people assume. That inversion is how developers resolve backwards and delete their own change. Check `git status`, which names both sides explicitly, and configure `merge.conflictStyle=zdiff3` to also show the common ancestor, which usually makes the right resolution obvious.",

  "How do you abort a merge or rebase mid-conflict?":
    "`git merge --abort` during a merge and `git rebase --abort` during a rebase — both restore your branch to exactly the state before you started, discarding partial resolutions. They're completely safe, so use them freely the moment a conflict looks worse than expected; there's no prize for pushing through a bad rebase. During a rebase you also have `git rebase --skip`, which drops the commit currently being applied — useful when the change is already present upstream, but check first, because you're discarding work. If you've finished a merge and immediately regret it, `git reset --hard ORIG_HEAD` rewinds to the pre-merge position. And `git reflog` finds commits after almost any mistake, which is the thing worth knowing: Git very rarely loses committed work.",

  "How do code reviews help prevent painful conflicts?":
    "Indirectly but effectively, because the review process pushes toward **small, short-lived branches** — and conflict pain scales with how long a branch lives and how much it touches. A PR that's reviewed and merged within a day barely diverges from `main`; a branch open for three weeks conflicts with everything. Review also surfaces two people restructuring the same area before both finish, which is the expensive kind of conflict — semantic, where both sides merge cleanly and the result is still broken. Reviewers spot renamed methods and moved responsibilities that Git happily merges without complaint. The other half is tooling rather than review: agree formatting in the build (Spotless, an EditorConfig, a shared IDE profile) so nobody's editor reformats a file and collides with every other change in it.",

  // ===================== Q138: fetch vs pull =====================
  "What does `git pull --rebase` do?":
    "It fetches from the remote and then **rebases your local commits on top** of what it fetched, instead of merging. A plain `git pull` on a branch where you have unpushed commits creates a merge commit every single time, so a busy shared branch fills with \"Merge branch 'main' of github.com...\" entries that carry no information. `--rebase` replays your commits onto the updated branch, keeping history linear. Make it the default with `git config --global pull.rebase true`. The usual rebase warning doesn't apply here, because you're only rewriting **your own unpushed commits** — nobody else has them. If a conflict comes up mid-rebase you resolve, `git add`, and `git rebase --continue`, exactly as with any rebase.",

  "Why might you prefer fetch + inspect before merging?":
    "Because `fetch` changes nothing, so it gives you a look before you commit to anything. After fetching you can run `git log --oneline HEAD..origin/main` to see exactly what landed, `git diff HEAD origin/main` to see what would change, and then decide whether to merge, rebase, or wait. That matters most when you're **mid-way through your own work** — a `pull` that drops a conflict into a half-finished change is far more painful than one you chose the timing of. It's also how you check whether someone force-pushed a rewritten branch before you overwrite your own work. `pull` is fine on a clean branch you're just keeping current; `fetch` first is the habit for anything else.",

  "What is a fast-forward merge?":
    "When your branch has **no commits of its own** and the remote has simply moved ahead, Git doesn't need a merge commit — the histories haven't diverged, so it just slides your branch pointer forward to the newer commit. That's a fast-forward, and it's why pulling on an untouched `main` produces no merge commit and the output says \"Fast-forward\". Once you have a local commit the branches have genuinely diverged and Git must create a merge commit (or you rebase instead). You can force the distinction: `--ff-only` makes the operation fail rather than create a merge commit, which is a good safety setting for `pull`, while `--no-ff` forces a merge commit even when a fast-forward is possible, so a feature branch shows up as a distinct unit in history.",

  // ===================== Q139: CI/CD =====================
  "What stages would you put in a Spring Boot pipeline?":
    "Roughly six, ordered so the cheapest checks fail first. **Build and unit test** (`./mvnw clean verify` with a dependency cache), **static analysis** for linting and a vulnerability scan, **integration tests** against Testcontainers, **build the Docker image** and tag it with the commit SHA, **push to a registry**, then **deploy to staging** and — after tests or an approval — **production**. Failing fast matters: a compile error should stop the pipeline in 60 seconds rather than after a 10-minute integration suite. Tag images with the **commit SHA, never `latest`**, so you can always say which code is running and roll back to something specific. And the whole thing is only worth having if it's the **only** path to production and a red build blocks the merge.",

  "How do you keep secrets in CI?":
    "Never in the repository. Use the CI provider's secret store — GitHub Actions encrypted secrets, Jenkins credentials, GitLab CI variables — and inject them as environment variables at run time, scoped per environment so a PR build can't reach production credentials. For cloud deployments, prefer **short-lived credentials over static ones**: GitHub Actions can assume an AWS role via OIDC so there's no long-lived key to leak at all. Providers mask secrets in logs, but that's best-effort pattern matching, so don't echo them or pass them as command-line arguments where they show up in a process list. Add a scanner like **gitleaks** to the pipeline so an accidental commit fails the build. And if one does leak, **rotate it** — deleting the commit doesn't help, since the value is in the reflog and on every existing clone.",

  "What is the difference between continuous delivery and continuous deployment?":
    "Both keep every green build **deployable**; the difference is whether a human approves the release. **Continuous delivery** stops at a gate — the artifact is tested, staged, and ready, and someone clicks to promote it. **Continuous deployment** removes the gate, so a merge to `main` that passes the pipeline goes to production automatically, with no human in the loop. Most teams with a change-management or compliance process run delivery. Deployment only works if you genuinely trust your tests, and it needs supporting practice around it: **feature flags** to decouple release from deploy, fast automated rollback, and good enough monitoring that a bad change is caught in minutes. The shared prerequisite is that CI is trustworthy — a suite people routinely re-run until it goes green makes either model dangerous.",

  // ===================== Q140: Docker =====================
  "What would a multi-stage Dockerfile for a Boot JAR look like at a high level?":
    "Two stages. The **build stage** starts from a Maven-plus-JDK image, copies `pom.xml` first and resolves dependencies, then copies `src` and runs `mvn package` — that ordering means a code-only change reuses the cached dependency layer instead of re-downloading everything. The **runtime stage** starts from a JRE-only image, copies **just the JAR** across with `COPY --from=build`, and sets `ENTRYPOINT [\"java\",\"-jar\",\"app.jar\"]`. Nothing from the build stage ships except that one file, so no source code, no Maven, no compiler in the final image. Add a non-root user with `USER app` while you're there. The alternative is skipping the Dockerfile entirely with `./mvnw spring-boot:build-image`, which uses Cloud Native Buildpacks to produce an optimised layered image.",

  "Why use a JRE-only base image in the final stage?":
    "**Size and attack surface.** A JDK image carries a compiler, debugger, and assorted tooling you have no use for at runtime — dropping to a JRE typically takes an image from around 500MB to around 200MB, and less again on Alpine. Smaller images pull faster on every deploy and every autoscale event, which matters when Kubernetes is starting pods under load. The security half is just as important: every binary in the image is something a CVE scanner will flag and something an attacker could use if they get code execution. A compiler and a shell inside a production container are useful to nobody but them. Taken further, distroless or Alpine images strip the shell entirely — more secure, marginally more awkward to debug, since `kubectl exec` into a shell is no longer an option.",

  "How do you pass env vars / profiles into a container?":
    "With `-e` flags on `docker run`, an `environment:` block in compose, or `env`/`envFrom` in a Kubernetes manifest — never baked into the image, because the whole point is one artifact promoted unchanged through every environment. `docker run -e SPRING_PROFILES_ACTIVE=prod -e SPRING_DATASOURCE_URL=jdbc:... order-service:abc123`. Spring's **relaxed binding** maps the underscored uppercase names onto dotted property keys, so `SPRING_DATASOURCE_URL` sets `spring.datasource.url` with no extra configuration. Environment variables outrank `application.yml`, which is what lets the platform override anything baked in. For **secrets**, use Kubernetes Secrets or a secret manager rather than plain env vars where you can — env vars show up in `docker inspect` and in crash dumps.",

  // ===================== Q141: Dockerfile vs compose =====================
  "When do you use compose for local dev with DB + app + Redis?":
    "When getting a new developer productive should be `git clone` followed by `docker compose up`, with no local Postgres install and no version drift between laptops. It's also the right tool for demos and for running the stack in CI when you need real dependencies. Everyone gets the **same** Postgres 16 and Redis 7, matching production, and `docker compose down -v` resets everything to clean. Two things to get right: use `depends_on` with `condition: service_healthy` plus a healthcheck, because a container being *started* isn't the same as the database being *ready* and your app will otherwise crash on boot; and keep a named volume for the database so a restart doesn't wipe your test data. For integration tests specifically, **Testcontainers is usually the better fit**, since the test itself owns the container lifecycle.",

  "How do volumes and networks work in compose?":
    "Compose puts all services on a **shared default network** and registers each under its **service name**, so the app reaches the database at `db:5432`. That's the piece people get wrong: inside a container `localhost` is that container, not your machine. The `ports:` mapping exists only to reach a container **from your host** — containers talking to each other don't need it, and leaving it off a database is good practice. **Volumes** persist data outside the container's writable layer, so a named volume like `pgdata:/var/lib/postgresql/data` survives `docker compose down` while `down -v` deletes it deliberately. A **bind mount** (`./config:/app/config`) maps a host directory in instead, which is handy for editing config without rebuilding the image.",

  "Is compose typically used in production Kubernetes environments?":
    "No. Compose has no scheduling across nodes, no self-healing, no rolling updates, no autoscaling, and no real multi-host story — it's a single-machine tool. On Kubernetes those jobs belong to **Deployments, Services, ConfigMaps, and Ingress**, and you'd write manifests (usually templated with Helm or Kustomize) rather than a compose file. Compose stays genuinely useful for **local development and CI**, and that's not a lesser use — a fast local stack is worth a lot. Tools exist to convert compose files into manifests (`kompose`), but the output is normally a starting point rather than something you'd run untouched, since it can't infer probes, resource limits, or rollout strategy. Docker Swarm can run compose files in production, but it lost the orchestration argument to Kubernetes years ago.",

  // ===================== Q142: Kubernetes =====================
  "What are Pod, Deployment, Service, and Ingress?":
    "A **Pod** is the smallest deployable unit — one or more containers that share a network namespace and storage, so they reach each other on `localhost`. Pods are disposable and get a new IP each time, which is why you never address one directly. A **Deployment** manages a set of identical pods: you declare `replicas: 3` and an image, and it keeps three running, replacing any that die and performing **rolling updates** when the image changes. A **Service** gives that changing set a **stable DNS name and virtual IP**, load balancing across whichever pods are currently ready — that's the cluster-internal service discovery that replaces Eureka. An **Ingress** sits at the cluster edge and routes external HTTP traffic to Services by host and path, usually terminating TLS on the way in.",

  "How do liveness and readiness probes relate to Spring Actuator?":
    "Actuator exposes **`/actuator/health/liveness`** and **`/actuator/health/readiness`** as separate endpoints, which is exactly the split Kubernetes wants — Boot enables them automatically when it detects Kubernetes, or you set `management.endpoint.health.probes.enabled=true`. **Liveness failing restarts the container**, so it should answer only \"is this JVM unrecoverable?\". **Readiness failing removes the pod from the Service** load balancer without restarting it, so it answers \"can I serve traffic right now?\". The classic outage is putting a **database check in the liveness probe**: the DB blips, every pod fails liveness at once, the whole fleet restarts simultaneously, and now a recovering database faces a thundering herd of reconnecting pods. Dependency checks belong in readiness. Add `startupProbe` for a slow-booting app so liveness doesn't kill it before it's finished starting.",

  "What is horizontal pod autoscaling at a high level?":
    "A **HorizontalPodAutoscaler** watches a metric — CPU utilisation by default, or a custom Micrometer metric like requests per second — and adjusts the Deployment's `replicas` between a floor and a ceiling to hold a target. Traffic doubles, CPU rises past the target, more pods start; traffic drops, it scales back down. The hard requirement is that your app is **stateless**, because any pod can be created or destroyed at any moment: no in-memory sessions, no in-memory rate-limit counters, no local file state. It also needs accurate **resource requests**, since utilisation is measured against the request, and a fast startup — an app taking 90 seconds to boot scales up long after the spike that needed it. Scale-down is deliberately slower than scale-up to avoid thrashing. Horizontal scaling adds pods; **vertical** scaling makes each pod bigger, and horizontal is what Kubernetes is built around.",
};
