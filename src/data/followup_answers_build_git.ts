/**
 * Answer bank for followup questions — Build Tools, Git & DevOps (Q133–Q142).
 * Keyed by the exact followup `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - Kept deliberately tighter than the deeper Spring categories
 */
export const followupAnswersBuildGit: Record<string, string> = {

  // ===================== Q133: Maven vs Gradle =====================
  "Does Gradle's speed advantage matter on a single Boot service?":
    "Honestly, no. On one service that builds in 30 seconds you won't notice any of it, which is why the choice usually comes down to familiarity instead.\n\nWhere it does pay off is **multi-module projects**. Gradle tracks the inputs and outputs of every task and skips anything whose inputs haven't changed, so a rebuild after a one-file edit recompiles that file rather than the module. Add the **build cache** and the long-running **daemon** and the savings compound, where Maven re-runs phases far more eagerly.\n\nThe **Kotlin DSL** (`build.gradle.kts`) is the other draw: a typed build script, so your IDE autocompletes task names and configuration blocks and a typo is a compile error instead of a runtime failure halfway through the build. The cost sits on the same axis — a Groovy or Kotlin build script can grow into code that needs its own review.",

  "How do you run a Boot app with each tool?":
    "`./mvnw spring-boot:run` or `./gradlew bootRun` — both compile and start the app with the embedded server, and both support live restarts when DevTools is on the classpath. To build the deployable artifact instead: `./mvnw clean package` or `./gradlew build`, each producing an executable fat JAR you run with `java -jar target/order-service-1.0.0.jar`. Pass Spring arguments after the jar name (`--server.port=9090`) and JVM arguments before it (`-Xmx512m`). Always use the **wrapper** scripts (`mvnw`/`gradlew`) rather than a locally installed `mvn` or `gradle`, so CI and every developer's laptop build with the exact same tool version.",

  // ===================== Q134: Maven lifecycle =====================
  "What is the difference between `package` and `install`?":
    "`package` compiles, tests, and builds the JAR into **`target/`**, where only this project can see it. `install` does all of that and then **copies the JAR into your local `~/.m2` repository**, which makes it resolvable as a dependency by other projects on the same machine. So you need `install` when you're building a shared library and want to test it against a consumer locally before publishing. For a deployable service, `package` is all you need — and `deploy` is the step beyond both, uploading to a remote or company repository like Nexus or Artifactory for everyone else to use.",

  "How do plugins bind to lifecycle phases?":
    "A phase on its own does nothing — it's an empty slot, and **plugin goals bind to it** to do the actual work. Packaging type supplies the default bindings: for a `jar` project, `maven-compiler-plugin:compile` binds to `compile`, `maven-surefire-plugin:test` binds to `test`, and `maven-jar-plugin:jar` binds to `package`. You add your own in the `<build><plugins>` section with an `<execution>` naming a `<phase>` and a `<goal>`. That's exactly how Spring Boot works: `spring-boot-maven-plugin:repackage` binds to `package`, taking the plain JAR Maven just built and rewriting it into an executable fat JAR. It's also why `mvn package` on a Boot project gives you something `java -jar` can run, while a plain Java project doesn't.",

  "Why does CI run `mvn clean verify` rather than `install`?":
    "Because `verify` is the most complete check that **doesn't write anything into the local `~/.m2`**. `install` does, which makes the build depend on machine state — exactly what you don't want on a shared or ephemeral CI agent.\n\n`clean verify` deletes `target/` so nothing stale survives, then runs **every phase up to and including verify**: validate, compile, test, package, integration-test, verify. That gives you a full compile, unit tests via Surefire, the packaged JAR, and integration tests via Failsafe.\n\nThe Surefire/Failsafe split is why the final phase matters. Surefire runs `*Test` classes and fails the build immediately; Failsafe runs `*IT` classes and defers failure to `verify`, so teardown still happens before the build goes red.",

  // ===================== Q135: Dependency management =====================
  "Does Maven pick the newest version when two libraries disagree?":
    "No — it picks the **nearest**, and that's the surprise. **Dependency mediation** resolves a conflict by depth from your POM, so a jar you declare directly sits at depth 1 and beats anything transitive, and Maven will happily choose an *older* version because it's one level closer. When two candidates sit at the same depth, the one **declared first** wins.\n\nGradle behaves differently and picks the **highest** version by default, which is worth knowing if you move between the two and wonder why the same dependency graph resolves differently.\n\nEither way, `mvn dependency:tree` shows you the winner and marks the losers as omitted for conflict.",

  "Your company already has a parent POM. How do you still get Boot's version management?":
    "Import `spring-boot-dependencies` as a **BOM** — `<type>pom</type><scope>import</scope>` inside your own `<dependencyManagement>` — and you get the whole version-management block without spending the single parent slot.\n\nThat block is the substance of what `spring-boot-starter-parent` provides. `<dependencyManagement>` declares **which version to use if this dependency appears**, without adding it to your build, and it overrides nearest-wins entirely, so one authoritative version wins regardless of tree depth. Boot's pins hundreds of libraries — Jackson, Hibernate, Tomcat, SLF4J — to versions Spring actually tested together, which is why you declare Spring dependencies **without a `<version>` tag**.\n\nWhat you give up by not inheriting the parent is the rest of it: plugin configuration, the Java version, UTF-8 encoding, resource filtering. Those you set yourself, which is a small and one-off cost.",

  "When is excluding a transitive dependency the wrong fix?":
    "When the library genuinely needs it. An `<exclusions>` block removes the jar from the classpath entirely, so if the code path that uses it ever runs you get `ClassNotFoundException` at runtime rather than anything at build time. Pinning the version in `<dependencyManagement>` is usually the better move — you keep the dependency and control which version wins.\n\nExclusion is right when you want the jar genuinely **gone**: a duplicate logging implementation (two SLF4J bindings on the classpath log a warning and pick one arbitrarily), or a transitive jar with a known CVE and no fixed version available.\n\nTo see what's actually there, **`mvn dependency:tree`**, narrowed with `-Dincludes=com.fasterxml.jackson.core` to follow one library. `mvn dependency:analyze` additionally flags dependencies you use but never declared, and ones you declared but never use. An exclusion names only the `groupId` and `artifactId` — it takes no `<version>`.",

  // ===================== Q136: merge vs rebase =====================
  "What happens to a colleague when you rebase a branch they've already pulled?":
    "Their history and yours stop agreeing, and their next pull duplicates everything. Rebasing **rewrites history** — each replayed commit is a new object with a new hash — so after a force-push the remote holds commits your colleague has never seen while they still hold commits that no longer exist upstream. Pulling merges the two versions together, so every change appears twice, or somebody reverts the wrong copy.\n\nThe rule that avoids all of it: **rebase only commits that exist nowhere but your machine.** Rebasing your own unpushed feature branch onto `main` is routine and safe; rebasing `main` itself, or a branch two people are working on, is how you lose an afternoon.\n\nIf you must force-push a shared branch, use **`--force-with-lease`**, which refuses when the remote has moved since you last fetched — so at least you can't overwrite work you never saw.",

  "What does a linear history buy you?":
    "A `git log` you can actually read, where every commit is a real state the project passed through in order, rather than a lattice of merge commits recording who synced when. That makes **`git bisect`** genuinely effective — it can binary-search commits to find the one that introduced a bug and converge quickly, where a history dense with merges gives it far messier ground. `git revert` is also simpler, since reverting a normal commit is unambiguous while reverting a merge commit requires telling Git which parent you meant. The counter-argument is that linear history is a **partial fiction**: the branches really did happen in parallel, and rebasing discards that. Most teams settle on rebasing feature branches to stay current, then squashing or merging the PR so `main` records one clean entry per feature.",

  "How do you resolve conflicts in each workflow?":
    "In a **merge** you hit conflicts once. Git stops, you fix every conflicted file, `git add` them, and `git commit` completes the merge with a single resolution. In a **rebase** Git replays your commits one at a time, so it stops **at each commit that conflicts** — a five-commit branch can present you the same conflict repeatedly as each commit is applied. The cycle is: fix, `git add`, `git rebase --continue`. That repetition is the main practical argument for merging a long branch rather than rebasing it. Both offer a clean exit: `git merge --abort` and `git rebase --abort` return you exactly to where you started. `git rerere` (reuse recorded resolution) can replay resolutions you've already made, which takes some of the pain out of a long rebase.",

  // ===================== Q137: Merge conflicts =====================
  "During a rebase, which side of a conflict is HEAD?":
    "**The target branch, not yours** — the reverse of what people assume. Git is replaying your commits onto `main`, so `main` is what's checked out as HEAD and your own work arrives as the *incoming* side at the bottom of the block. That inversion is how developers resolve backwards and delete their own change.\n\nIn a normal merge it's the other way round: `<<<<<<< HEAD` opens **your current side**, `=======` divides the two versions, and `>>>>>>> branch-name` closes the **incoming** one. Everything between them is both candidates written into the file, which is why it won't compile until you fix it.\n\nDon't rely on remembering which case you're in. `git status` names both sides explicitly, and setting `merge.conflictStyle=zdiff3` also shows the common ancestor, which usually makes the right resolution obvious.",

  "You're halfway through a bad rebase. What can Git recover?":
    "Effectively everything, which is why you should bail early rather than push through. **`git rebase --abort`** (or `git merge --abort` in a merge) restores the branch to exactly the state before you started and discards partial resolutions. There's no prize for finishing a rebase that's going badly.\n\nIf you've already completed a merge and immediately regret it, **`git reset --hard ORIG_HEAD`** rewinds to the pre-merge position. And **`git reflog`** finds commits after almost any mistake — that's the thing worth internalising, since Git very rarely loses committed work, it just stops pointing at it.\n\nOne to use carefully: `git rebase --skip` drops the commit currently being applied. That's right when the change is already present upstream and wrong the rest of the time, because you're discarding work — look at what's in it first.",

  "Which conflicts does Git merge cleanly and still leave broken?":
    "**Semantic ones.** Two people restructure the same area — one renames a method, the other adds a caller for the old name — and both sides merge without a single marker, because the changed lines never overlap. Git had no opinion, and no amount of careful conflict resolution would have caught it.\n\nThat's what review actually prevents. A reviewer sees the renamed method and the moved responsibility that Git merged happily, and the process itself pushes toward **small, short-lived branches** — a PR merged within a day barely diverges from `main`, where a branch open for three weeks conflicts with everything.\n\nThe other half is tooling rather than people: agree formatting in the build (Spotless, an EditorConfig, a shared IDE profile) so nobody's editor reformats a file and collides with every other change in it.",

  // ===================== Q138: fetch vs pull =====================
  "What does `git pull --rebase` do?":
    "It fetches from the remote and then **rebases your local commits on top** of what it fetched, instead of merging. A plain `git pull` on a branch where you have unpushed commits creates a merge commit every single time, so a busy shared branch fills with \"Merge branch 'main' of github.com...\" entries that carry no information. `--rebase` replays your commits onto the updated branch, keeping history linear. Make it the default with `git config --global pull.rebase true`. The usual rebase warning doesn't apply here, because you're only rewriting **your own unpushed commits** — nobody else has them. If a conflict comes up mid-rebase you resolve, `git add`, and `git rebase --continue`, exactly as with any rebase.",

  "What do you run between a fetch and deciding to merge?":
    "`git log --oneline HEAD..origin/main` to see exactly what landed, and `git diff HEAD origin/main` to see what would change. Then you decide whether to merge, rebase, or wait — which is the point, since `fetch` changed nothing and left the decision with you.\n\nThat matters most when you're **mid-way through your own work**. A `pull` that drops a conflict into a half-finished change is far more painful than one whose timing you chose.\n\nIt's also how you notice someone force-pushed a rewritten branch before you overwrite your own work with it. `pull` is fine on a clean branch you're just keeping current; `fetch` first is the habit for everything else.",

  "What is a fast-forward merge?":
    "When your branch has **no commits of its own** and the remote has simply moved ahead, Git doesn't need a merge commit — the histories haven't diverged, so it just slides your branch pointer forward to the newer commit. That's a fast-forward, and it's why pulling on an untouched `main` produces no merge commit and the output says \"Fast-forward\". Once you have a local commit the branches have genuinely diverged and Git must create a merge commit (or you rebase instead). You can force the distinction: `--ff-only` makes the operation fail rather than create a merge commit, which is a good safety setting for `pull`, while `--no-ff` forces a merge commit even when a fast-forward is possible, so a feature branch shows up as a distinct unit in history.",

  // ===================== Q139: CI/CD =====================
  "Why tag images with the commit SHA rather than `latest`?":
    "Because `latest` can't answer \"what is actually running in production?\" and can't be rolled back to. Tag with the **commit SHA** and every deployed artifact maps to exactly one commit, so a rollback means redeploying a specific known image rather than hoping the registry still holds the previous build.\n\nIt matters because the image is built once and promoted unchanged through staging to production. If the tag is mutable, staging and production can silently be running different code under the same name.\n\nThe stages around it are roughly six, ordered so the cheapest checks fail first: **build and unit test** (`./mvnw clean verify` with a dependency cache), **static analysis** plus a vulnerability scan, **integration tests** against Testcontainers, **build and tag the image**, **push to a registry**, then **staging** and — after tests or an approval — **production**. A compile error should stop the pipeline in 60 seconds, not after a 10-minute integration suite.",

  "How do you keep secrets in CI?":
    "Never in the repository. Use the CI provider's secret store — GitHub Actions encrypted secrets, Jenkins credentials, GitLab CI variables — and inject them as environment variables at run time, scoped per environment so a PR build can't reach production credentials. For cloud deployments, prefer **short-lived credentials over static ones**: GitHub Actions can assume an AWS role via OIDC so there's no long-lived key to leak at all. Providers mask secrets in logs, but that's best-effort pattern matching, so don't echo them or pass them as command-line arguments where they show up in a process list. Add a scanner like **gitleaks** to the pipeline so an accidental commit fails the build. And if one does leak, **rotate it** — deleting the commit doesn't help, since the value is in the reflog and on every existing clone.",

  "What has to be true before continuous deployment is safe?":
    "Tests you genuinely trust, and three things around them: **feature flags** so releasing is decoupled from deploying, **fast automated rollback**, and monitoring good enough to catch a bad change in minutes rather than from a customer email. Without those, removing the human gate just ships faults faster.\n\nThe gate is the only difference between the two models. Both keep every green build **deployable**; **continuous delivery** stops at an approval where someone clicks to promote, and **continuous deployment** goes straight to production on a merge to `main`.\n\nMost teams with a change-management or compliance process run delivery, and that's a legitimate answer rather than a lesser one. The shared prerequisite either way is trustworthy CI — a suite people routinely re-run until it goes green makes both models dangerous.",

  // ===================== Q140: Docker =====================
  "Why copy `pom.xml` before `src` in a multi-stage build?":
    "**Layer caching.** Each instruction is a layer, and Docker reuses cached layers until one of their inputs changes. Copy `pom.xml` first and resolve dependencies as their own layer, and a code-only change reuses that layer instead of re-downloading every dependency on every build.\n\nThat's the build stage: start from a Maven-plus-JDK image, copy `pom.xml`, resolve dependencies, then copy `src` and run `mvn package`. The **runtime stage** starts fresh from a JRE-only image, copies **just the JAR** across with `COPY --from=build`, and sets `ENTRYPOINT [\"java\",\"-jar\",\"app.jar\"]`. Nothing else ships — no source, no Maven, no compiler — and you add a non-root `USER app` while you're there.\n\nThe alternative is skipping the Dockerfile entirely with `./mvnw spring-boot:build-image`, which uses Cloud Native Buildpacks to produce an optimised layered image.",

  "Why use a JRE-only base image in the final stage?":
    "**Size and attack surface.** A JDK image carries a compiler, debugger, and assorted tooling you have no use for at runtime — dropping to a JRE typically takes an image from around 500MB to around 200MB, and less again on Alpine. Smaller images pull faster on every deploy and every autoscale event, which matters when Kubernetes is starting pods under load. The security half is just as important: every binary in the image is something a CVE scanner will flag and something an attacker could use if they get code execution. A compiler and a shell inside a production container are useful to nobody but them. Taken further, distroless or Alpine images strip the shell entirely — more secure, marginally more awkward to debug, since `kubectl exec` into a shell is no longer an option.",

  "How do you pass env vars / profiles into a container?":
    "With `-e` flags on `docker run`, an `environment:` block in compose, or `env`/`envFrom` in a Kubernetes manifest — never baked into the image, because the whole point is one artifact promoted unchanged through every environment. `docker run -e SPRING_PROFILES_ACTIVE=prod -e SPRING_DATASOURCE_URL=jdbc:... order-service:abc123`. Spring's **relaxed binding** maps the underscored uppercase names onto dotted property keys, so `SPRING_DATASOURCE_URL` sets `spring.datasource.url` with no extra configuration. Environment variables outrank `application.yml`, which is what lets the platform override anything baked in. For **secrets**, use Kubernetes Secrets or a secret manager rather than plain env vars where you can — env vars show up in `docker inspect` and in crash dumps.",

  // ===================== Q141: Dockerfile vs compose =====================
  "Your app crashes on boot because Postgres isn't ready. How do you fix that in compose?":
    "`depends_on` with **`condition: service_healthy`**, plus a `healthcheck` on the database service. Plain `depends_on` waits only for the container to be **started**, which isn't the same as Postgres being **ready** to accept connections — and that gap is exactly what your app is crashing into.\n\nThe other thing to get right is a **named volume** for the database (`pgdata:/var/lib/postgresql/data`), so a restart doesn't wipe your test data while `docker compose down -v` still resets everything deliberately.\n\nThat setup is what makes compose worth having locally: a new developer gets productive with `git clone` and `docker compose up`, no local Postgres install, and everyone on the same Postgres 16 and Redis 7 as production. For integration tests specifically, **Testcontainers is usually the better fit**, since the test itself owns the container lifecycle.",

  "How do volumes and networks work in compose?":
    "Compose puts all services on a **shared default network** and registers each under its **service name**, so the app reaches the database at `db:5432`. That's the piece people get wrong: inside a container `localhost` is that container, not your machine. The `ports:` mapping exists only to reach a container **from your host** — containers talking to each other don't need it, and leaving it off a database is good practice. **Volumes** persist data outside the container's writable layer, so a named volume like `pgdata:/var/lib/postgresql/data` survives `docker compose down` while `down -v` deletes it deliberately. A **bind mount** (`./config:/app/config`) maps a host directory in instead, which is handy for editing config without rebuilding the image.",

  // ===================== Q142: Kubernetes =====================
  "How do liveness and readiness probes relate to Spring Actuator?":
    "Actuator exposes **`/actuator/health/liveness`** and **`/actuator/health/readiness`** as separate endpoints, which is exactly the split Kubernetes wants — Boot enables them automatically when it detects Kubernetes, or you set `management.endpoint.health.probes.enabled=true`. **Liveness failing restarts the container**, so it should answer only \"is this JVM unrecoverable?\". **Readiness failing removes the pod from the Service** load balancer without restarting it, so it answers \"can I serve traffic right now?\". The classic outage is putting a **database check in the liveness probe**: the DB blips, every pod fails liveness at once, the whole fleet restarts simultaneously, and now a recovering database faces a thundering herd of reconnecting pods. Dependency checks belong in readiness. Add `startupProbe` for a slow-booting app so liveness doesn't kill it before it's finished starting.",

  "What is horizontal pod autoscaling at a high level?":
    "A **HorizontalPodAutoscaler** watches a metric — CPU utilisation by default, or a custom Micrometer metric like requests per second — and adjusts the Deployment's `replicas` between a floor and a ceiling to hold a target. Traffic doubles, CPU rises past the target, more pods start; traffic drops, it scales back down. The hard requirement is that your app is **stateless**, because any pod can be created or destroyed at any moment: no in-memory sessions, no in-memory rate-limit counters, no local file state. It also needs accurate **resource requests**, since utilisation is measured against the request, and a fast startup — an app taking 90 seconds to boot scales up long after the spike that needed it. Scale-down is deliberately slower than scale-up to avoid thrashing. Horizontal scaling adds pods; **vertical** scaling makes each pod bigger, and horizontal is what Kubernetes is built around.",
};
