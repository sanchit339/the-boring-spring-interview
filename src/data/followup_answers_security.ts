/**
 * Answer bank for followup questions — Security category (Q103–Q112).
 * Keyed by the exact followup `text` so answers merge into the FollowUp
 * objects at runtime without touching questions.ts.
 *
 * Style (per answer_rules.md):
 *  - Length follows the question — no sentence quota, no padding
 *  - Bold the terms that actually matter
 *  - No GPT fluff, no "In conclusion", active voice, contractions
 *  - BAD/GOOD code only where it proves understanding
 */
export const followupAnswersSecurity: Record<string, string> = {

  // ===================== Q103: What is Spring Security =====================
  "What is the security filter chain at a high level?":
    "It's an **ordered list of servlet filters** that every request walks through before `DispatcherServlet` sees it, each with one job. `SecurityContextHolderFilter` restores the authentication from the session and `CsrfFilter` validates the token. The authentication filters (form login, HTTP Basic, your JWT filter) then try to establish who the caller is, and `ExceptionTranslationFilter` turns thrown security exceptions into 401/403 responses. `AuthorizationFilter` runs last to apply the `authorizeHttpRequests` rules. **Order is everything** — a filter that sets the `SecurityContext` after the authorization filter has already run does nothing at all. Set `logging.level.org.springframework.security=DEBUG` and Boot prints the whole resolved chain at startup, which is the fastest way to see what you actually configured.",

  "How does Spring Security run inside a servlet container that knows nothing about Spring beans?":
    "Through one bridge filter called **`DelegatingFilterProxy`**. Boot registers a single standard servlet filter named `springSecurityFilterChain` with the container; that filter holds no logic and simply looks up the matching **Spring bean** from the `ApplicationContext` and delegates every call to it. Behind it sits `FilterChainProxy`, which picks the right `SecurityFilterChain` for the request and runs your Spring-managed filters in order. That indirection is why your filters can be `@Component`s with `@Autowired` dependencies — the container only ever knows about one filter, and Spring manages everything past it.",

  "What do you lose the moment you declare your own `SecurityFilterChain`?":
    "All of it, and silently. Boot's auto-configuration backs off the moment you define a `SecurityFilterChain` bean, so you inherit responsibility for every default it was providing.\n\nThose defaults are worth knowing precisely: **every endpoint authenticated**, a generated form login page, HTTP Basic, and a random password printed to the console at startup. Plus **CSRF protection on**, **session fixation protection** (the session id changes at login), and security headers including `X-Content-Type-Options`, `X-Frame-Options: DENY` and `Cache-Control: no-store`.\n\nThe design is deliberately **fail-closed** — you open things up explicitly rather than remembering to lock them down. Which is why copying half a config off the internet is dangerous: you keep the parts you understood and quietly drop session fixation protection and the headers along with the login form you meant to remove.",

  // ===================== Q104: Authentication vs authorization =====================
  "Where does each happen in a typical request to a secured API?":
    "Both happen in the filter chain, **before your controller runs**, and in that fixed order. Authentication happens early — your JWT filter or `UsernamePasswordAuthenticationFilter` reads the credential, validates it, and stores an `Authentication` in the `SecurityContextHolder`. Authorization happens at the end of the chain in `AuthorizationFilter`, which applies the `authorizeHttpRequests` matchers to the now-known principal. A second, finer authorization pass can run **inside** the service layer via `@PreAuthorize` on the bean. That's where you check things the URL can't express — like whether this user owns the record they just asked for.",

  "What exactly does the `Authentication` object hold after a successful login?":
    "Three things: the **principal** (usually a `UserDetails` or, for JWT, the username or a `Jwt` object), the **credentials** (which you should null out after authenticating so a password never lingers in memory), and a collection of **`GrantedAuthority`** strings that drive every later access check. Authorities are just strings — `ROLE_ADMIN`, `orders:read`, `SCOPE_profile` — and Spring attaches no meaning beyond exact matching. It also carries `authenticated=true` and an optional `details` object with the remote IP and session id. Grab it in a controller with `@AuthenticationPrincipal` rather than reaching into `SecurityContextHolder` by hand.",

  // ===================== Q105: JWT authentication =====================
  "What stops a client from editing the claims in its own JWT?":
    "The **signature**. The third segment is an HMAC or RSA signature over the header and payload, computed with a key only the server holds. Change one character of the payload and the recomputed signature no longer matches, so `parseAndVerify` throws and the request is rejected. Nothing stops a client from *reading* the claims — a JWT is base64url-encoded, **not encrypted**, so treat everything in it as public and never put a password, card number, or PII there. Two forgery traps to know: never accept `alg: none`, and **pin the expected algorithm** rather than trusting the header. Otherwise an attacker can hand you an HMAC token signed with your own public RSA key and a naive library will verify it.",

  "Where should the token live on the client, and what attack does each choice expose you to?":
    "`localStorage` is the easy option and it's readable by any JavaScript on the page, so a **single XSS bug hands over the token** — and unlike a cookie, you can't mark it `HttpOnly`. An `HttpOnly` cookie is invisible to JavaScript so XSS can't steal it, but the browser now sends it automatically, which reopens **CSRF** and forces you to add CSRF tokens or `SameSite=Strict`. The pragmatic answer most teams land on: the **refresh token in an `HttpOnly`, `Secure`, `SameSite=Strict` cookie** scoped to the refresh endpoint, and the short-lived access token held **in memory only** (a JS variable). A page reload then silently re-fetches it. There's no storage option that's safe on its own — you're picking which attack you'd rather defend against, and you still have to fix the XSS.",

  "How do you handle token refresh and expiration?":
    "Issue two tokens at login: a **short-lived access token** (5–15 minutes, stateless, never revoked) and a **long-lived refresh token** (days or weeks) that you **store server-side** so it can actually be revoked. When the API returns 401, the client posts the refresh token to `/api/auth/refresh`, gets a new pair, and retries the original request — an HTTP interceptor does this transparently so users never see it. **Rotate** the refresh token on every use and invalidate the old one; if a rotated token is ever replayed, that's a stolen-token signal and you kill the whole family. This is the design that buys back revocation: logging a user out invalidates the refresh token, and the access token dies on its own within minutes.",

  // ===================== Q106: Session vs token =====================
  "How does horizontal scaling differ for sticky sessions vs JWT?":
    "With **sticky sessions** the load balancer must pin each user to the instance holding their session, so traffic distributes unevenly, a node dying logs out everyone on it, and rolling deploys drop sessions unless you add a shared store like `spring-session-data-redis`. That store then becomes a dependency on the hot path — every request costs a Redis round trip, and Redis going down takes the whole app with it. With **JWT** the load balancer round-robins freely because verification is local and stateless; adding instances is pure capacity, no coordination. The catch is that key rotation now becomes the distributed problem: every instance needs the new signing key, and it has to accept the old one until the last token issued under it expires.",

  "What is session fixation, and how is it mitigated?":
    "An attacker gets a valid session id first — often by feeding the victim a link containing one — waits for the victim to **log in on that same session**, and then reuses the id they already know, now carrying an authenticated user. The fix is to **issue a brand-new session id at the moment of successful authentication** and discard the old one, so whatever the attacker planted becomes worthless. Spring Security does this by default with `sessionManagement().sessionFixation().changeSessionId()`, which is why the protection is one of the things you can accidentally lose by writing your own `SecurityFilterChain` and copying half a config off the internet. Stateless JWT APIs sidestep the attack entirely, since there's no session to fixate.",

  // ===================== Q107: Securing REST APIs =====================
  "How do you permit public endpoints like `/login` and `/actuator/health`?":
    "List them **before** the catch-all in `authorizeHttpRequests`, since matchers evaluate top-down and the first match wins: `.requestMatchers(\"/api/auth/**\", \"/actuator/health\").permitAll()` followed by `.anyRequest().authenticated()`. Be as narrow as you can — `permitAll()` on `/api/**` opens everything under it, and an HTTP-method-specific matcher like `.requestMatchers(HttpMethod.GET, \"/api/products/**\").permitAll()` keeps the reads public while `POST` stays protected. Never open `/actuator/**` wholesale: `/actuator/env` prints your configuration including credentials and `/actuator/heapdump` hands out memory contents, so expose `health` and `info` and keep the rest behind `hasRole(\"ADMIN\")` or on a separate management port.",

  "What breaks if you add a JWT filter but leave form login and sessions enabled?":
    "The app starts fine and then misbehaves in ways that look unrelated. Any unauthenticated call returns a **302 redirect to `/login`** with an HTML body instead of a 401, so your mobile client or SPA tries to parse a login page as JSON. Sessions keep getting created — one `JSESSIONID` per caller even though nothing uses it — so memory grows under load and you've paid for statelessness without getting it. **CSRF stays on** as well, so every `POST` without a `_csrf` token fails with 403 and looks like an authorization bug. The fix is the three lines people copy without reading: disable `formLogin`, set `SessionCreationPolicy.STATELESS`, and disable CSRF because no cookie carries your credential.",

  "Why doesn't `@RestControllerAdvice` catch your 401s?":
    "Because they never reach a controller. Authentication failures are handled **inside the filter chain**, before `DispatcherServlet` runs, so there's no handler for `@ControllerAdvice` to wrap — no global exception handler fixes a 401 however you write it.\n\nThe component to override is the **`AuthenticationEntryPoint`**, in `exceptionHandling`. It decides what happens when an unauthenticated caller hits a protected endpoint, and the default `LoginUrlAuthenticationEntryPoint` is what sends the 302 to `/login` that your SPA then tries to parse as JSON. Set the status to 401, set `Content-Type: application/json`, and write your own body.\n\nDo the same with **`accessDeniedHandler`**, or authenticated-but-denied calls still return Spring's default HTML error page instead of a JSON 403.",

  // ===================== Q108: SecurityFilterChain =====================
  "Why did Spring Security move away from `WebSecurityConfigurerAdapter`?":
    "Because it forced configuration through **inheritance**, and inheritance is a bad fit for composition. You got one adapter class per configuration, subclassing to override `configure(HttpSecurity)` and its overloads, with no clean way to define multiple independent chains or to have a library contribute one. The bean-based model is just `@Bean SecurityFilterChain` — you can declare several, order them with `@Order`, inject dependencies as method parameters, and a starter can add its own chain without touching yours. It was **deprecated in 5.7 and removed in 6.0**, so most tutorials you'll find online won't compile against Spring Boot 3. The migration is mechanical — drop `extends`, return `http.build()`, expose `AuthenticationManager` and `PasswordEncoder` as beans.",

  "Your JWT filter never runs and API clients get redirected to a login form. Why?":
    "A broader chain claimed the request first. `FilterChainProxy` evaluates `SecurityFilterChain` beans in `@Order` sequence and **the first one whose matcher hits handles the request — the rest never run**. Put the catch-all web chain at `@Order(1)` and it swallows `/api/**` too, so your API chain and its JWT filter are skipped entirely. Nothing warns you at startup.\n\nThe rules aren't cumulative either, so a request matched by the API chain gets nothing from the web chain — anything you assumed it inherited isn't there.\n\nThe fix is ordering most-specific-first: `@Order(1)` with `securityMatcher(\"/api/**\")` for the stateless JWT API, then `@Order(2)` with no matcher as the catch-all for the session-based admin UI.",

  "Where does your custom JWT filter sit in the chain?":
    "Before the authentication filters and well before authorization — `addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class)` is the conventional spot. That places it **after** `SecurityContextHolderFilter` and `ExceptionTranslationFilter` (so the context is ready and thrown security exceptions still translate to proper 401/403 responses) and **before** `AuthorizationFilter`, which is the deadline. Set the `SecurityContext` after that filter has run and it changes nothing, every request is denied, and the config looks correct. Extend **`OncePerRequestFilter`** so it doesn't re-run on forwards and error dispatches, and always call `chain.doFilter(...)` even when the token is invalid. Clear the context and let the authorization filter produce the 401, rather than writing the response yourself.",

  // ===================== Q109: CSRF =====================
  "Why is CSRF often disabled for pure stateless JWT APIs?":
    "Because CSRF only works when the browser attaches your credential **automatically**, and a JWT the client pulls from storage and sets as an `Authorization` header isn't automatic. An attacker's page can force the victim's browser to fire a `POST` at your API, but it can't add that header. The same-origin policy blocks reading your storage and cross-origin requests with custom headers require a CORS preflight your server won't approve. So there's nothing to forge and the CSRF token adds friction with no benefit. The condition, not the token format, is what matters: this reasoning collapses the moment the JWT moves into a cookie.",

  "When would disabling CSRF actually get your app exploited?":
    "Any time the credential rides along automatically — a `JSESSIONID` session cookie, HTTP Basic, or **a JWT you decided to store in a cookie**. That last one is the common self-inflicted wound: a team reads \"JWTs don't need CSRF\", disables it, then later moves the token into an `HttpOnly` cookie to dodge XSS and never re-enables the protection. Now `evil.com` can auto-submit a form at your API and the browser attaches the token, so a state-changing request lands fully authenticated. If your credential lives in a cookie you need CSRF tokens **or** `SameSite=Strict` — and `SameSite` alone is a weaker guarantee, since it varies by browser and doesn't cover every navigation case.",

  "Why is `GET` exempt from CSRF checks?":
    "Because it's assumed to be safe. CSRF protection covers the state-changing verbs — `POST`, `PUT`, `PATCH`, `DELETE` — and lets `GET` through untouched.\n\nWhich is exactly why a `GET` endpoint must never change state. A plain `<img src=\"https://yourbank.com/transfer?to=attacker\">` on any page the victim visits fires an authenticated request from their browser, and no token is ever checked. Picking the wrong verb bypasses the entire mechanism.\n\nThe protection itself is the **synchronizer token pattern**: the server generates a random token per session, stores it, and embeds it in every form it renders (Thymeleaf adds the hidden `_csrf` field for you). `CsrfFilter` compares what comes back and returns **403** on a mismatch. Its security rests entirely on the **same-origin policy** — `evil.com` can make your browser send a request, but it can't read the page holding your token.",

  // ===================== Q110: RBAC =====================
  "What is the difference between roles and authorities in Spring Security?":
    "There's **no structural difference** — both are just `GrantedAuthority` strings. A \"role\" is nothing more than an authority that starts with the `ROLE_` prefix by convention. The only real distinction lives in the helper methods: **`hasRole(\"ADMIN\")` prepends `ROLE_` for you, `hasAuthority(\"ADMIN\")` compares exactly what you pass**. That mismatch is the classic bug — store `ADMIN` in your DB and `hasRole(\"ADMIN\")` silently fails while `hasAuthority(\"ADMIN\")` works, and swapping the annotation \"fixes\" it without anyone understanding why. Conceptually, teams use roles for coarse identity (`ROLE_ADMIN`) and authorities for fine-grained permissions (`orders:write`); pick one storage convention and stick to it everywhere.",

  "`@EnableMethodSecurity` is on and `@PreAuthorize` still isn't firing. Why?":
    "**Self-invocation.** Method security runs through Spring AOP proxies, so calling an annotated method from another method of the same bean never crosses the proxy and the check never runs — the same trap as `@Transactional`. Move the method onto a different bean.\n\nOn a fresh app the other cause is that method security is **off by default**: without `@EnableMethodSecurity` on a `@Configuration` class, the annotations are inert metadata.\n\nBoth are the worst kind of failure — no error, no warning, no log line, and the code reads as secured so review passes. Prove it with a test instead: call a `@PreAuthorize(\"hasRole('ADMIN')\")` method as a plain user with `@WithMockUser` and assert it **throws** `AccessDeniedException`.",

  "How do you check that a user owns the specific record they're requesting?":
    "URL rules can't do it — `/api/orders/99` matches the same pattern for everyone, so `hasRole(\"USER\")` happily lets one customer read another's order. That's **broken object-level authorization**, the top item on the OWASP API list, and it needs a check against the data. When the owner is in the arguments, use SpEL: `@PreAuthorize(\"hasRole('ADMIN') or #customerId == authentication.name\")`. When you only learn the owner after loading the row, use `@PostAuthorize(\"returnObject.customerId == authentication.name\")` — but note the method body already ran, so never use it on anything that writes. For anything non-trivial, push the logic into a bean and call it from the expression (`@PreAuthorize(\"@orderGuard.canAccess(#id, authentication)\")`) rather than growing SpEL you can't unit-test; simplest of all is often scoping the query itself with `findByIdAndCustomerId(...)`.",

  // ===================== Q111: Password storage =====================
  "Where does the salt live if the database column only stores one hash string?":
    "**Inside that string.** BCrypt output like `$2a$10$N9qo8uLOickgx2ZMRZoMye...` is a structured format: `$2a$` is the algorithm version, `10` is the cost factor, then a **22-character base64 salt**, then the 31-character hash. `matches()` parses the salt and cost back out, re-hashes the supplied password with them, and compares — no separate salt column, no extra lookup. The salt is **not secret** and doesn't need to be; its job is to make every hash unique so identical passwords produce different rows. That kills rainbow tables and stops an attacker from cracking one hash and unlocking every account that shared it. Never write your own salting scheme — BCrypt, Argon2, and scrypt all generate and embed one for you.",

  "How do you switch hashing algorithms without forcing every user to reset their password?":
    "Use **`DelegatingPasswordEncoder`** (what `PasswordEncoderFactories.createDelegatingPasswordEncoder()` returns and Boot's default). It stores an algorithm prefix in the hash — `{bcrypt}$2a$10$...`, `{argon2}$argon2id$...` — and reads that prefix on verification to pick the matching encoder, so **old and new algorithms coexist in the same column**. Change the default to Argon2 and new signups get Argon2 while legacy `{bcrypt}` rows still verify normally. Then migrate lazily: inside your successful-login path, check whether the stored hash uses the old prefix and, if so, **re-encode the raw password you already have in hand** and save it. Within a few weeks of normal traffic most of the table has rolled over, and you can force a reset on whatever's left. Legacy rows with no prefix at all need a one-time `UPDATE` to tag them (`{noop}` or `{md5}`) before any of this works.",

  // ===================== Q112: OAuth2 =====================
  "Why couldn't people just use OAuth2 for login?":
    "Because OAuth2 says nothing standard about **who the user is**. It answers \"may this app access that resource?\" and hands back an access token that's deliberately opaque to the client. There's no defined way to ask whose account it belongs to, so every provider invented its own incompatible userinfo endpoint — which is why \"log in with OAuth2\" was a decade of subtly broken implementations.\n\n**OIDC** is the thin standard layer that fixes exactly that: an **`id_token`** (a JWT with standard claims like `sub`, `email`, `name` and `iss`), a discovery document, and a fixed userinfo endpoint.\n\nThe short version: **OAuth2 gives you a valet key, OIDC gives you an ID card.** If you're implementing \"Sign in with Google\", you're using OIDC whether you call it that or not.",

  "Explain authorization code flow at a high level.":
    "Your app redirects the browser to the authorization server with your client id, requested scopes, redirect URI, and a **`state`** value. The user authenticates and consents **there** — your app never sees the password — and the server redirects back to your callback with a short-lived, single-use **authorization code**. Your backend then exchanges that code for tokens over a **back-channel HTTPS POST** using its client secret, so the tokens never appear in a URL, browser history, or `Referer` header. That back-channel step is the entire point of the code: it proves the app receiving the tokens is the one registered for that client id. Validate `state` on return or you've left a CSRF hole in your login flow, and for public clients with no secret (SPAs, mobile) add **PKCE**. The deprecated implicit flow that used to serve them returned tokens straight through the browser.",

  "What is a resource server vs an authorization server?":
    "The **authorization server** owns identity: it hosts the login UI, authenticates users, gets consent, and **issues and signs** tokens — Keycloak, Okta, Auth0, Google, or Spring Authorization Server. The **resource server** owns data: it's your API, it never sees a password, and it only ever **validates** an incoming token and maps its scopes to access rules. The split is what makes delegation work — one authorization server can issue tokens that a dozen independent APIs accept, each verifying against the issuer's published public keys with no shared session. In Spring Boot you pick a side with a starter: `spring-boot-starter-oauth2-resource-server` plus an `issuer-uri` for the API (Boot fetches the JWK set and validates signature, issuer, and expiry for you). Or `spring-boot-starter-oauth2-client` for the app that logs users in. Building your own authorization server is a security project, not a sprint task — almost every team should buy or self-host an existing one.",
};
